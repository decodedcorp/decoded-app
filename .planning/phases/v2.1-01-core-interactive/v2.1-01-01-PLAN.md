---
phase: v2.1-01-core-interactive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/tag.tsx
  - packages/web/lib/design-system/action-button.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "User can see category tags with decoded.pen styling (pill shape, category-specific colors)"
    - "User can click tags to trigger filter actions"
    - "User sees visual feedback from action buttons with hover/active states"
    - "ActionButton renders correctly in 3 variants: Default (semi-transparent), Solid (primary), Outline (bordered)"
  artifacts:
    - path: "packages/web/lib/design-system/tag.tsx"
      provides: "Tag component with 6 category variants"
      exports: ["Tag", "tagVariants", "TagProps"]
    - path: "packages/web/lib/design-system/action-button.tsx"
      provides: "ActionButton component with 3 variants"
      exports: ["ActionButton", "actionButtonVariants", "ActionButtonProps"]
    - path: "packages/web/lib/design-system/index.ts"
      provides: "Barrel exports for Tag and ActionButton"
      contains: "export.*Tag"
  key_links:
    - from: "packages/web/lib/design-system/tag.tsx"
      to: "packages/web/lib/design-system/tokens.ts"
      via: "design token references for colors"
      pattern: "colors\\."
    - from: "packages/web/lib/design-system/action-button.tsx"
      to: "lucide-react"
      via: "icon prop rendering"
      pattern: "lucide-react"
---

<objective>
Implement Tag and ActionButton components following decoded.pen design specifications.

Purpose: Provide interactive UI elements for content filtering (tags) and user actions (like, bookmark, share buttons) that match the decoded.pen design system.

Output:
- `tag.tsx` - Category tag component with 6 color variants
- `action-button.tsx` - Action button with Default/Solid/Outline variants
- Updated `index.ts` with barrel exports
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.1.md
@.planning/STATE.md

# Design System Reference (follow these patterns)
@packages/web/lib/design-system/input.tsx
@packages/web/lib/design-system/card.tsx
@packages/web/lib/design-system/tokens.ts
@packages/web/lib/design-system/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tag component with category variants</name>
  <files>packages/web/lib/design-system/tag.tsx</files>
  <action>
Create Tag component using CVA pattern matching existing design-system components.

Design specs from decoded.pen:
- Tag/All: height 32px, fill #1A1A2E (dark), cornerRadius 9999 (pill), padding [0, 16], fontFamily Inter, fontSize 12px, fontWeight 500, text white
- Tag/Latest: fill #FEF3C7 (amber-100), text #92400E (amber-800)
- Tag/Clothing: fill #FCE7F3 (pink-100), text #831843 (pink-900)
- Tag/Accessories: fill #EDE9FE (violet-100), text #5B21B6 (violet-800)
- Tag/Shoes: fill #E0F2FE (sky-100), text #075985 (sky-800)
- Tag/Bags: fill #F5F5F4 (stone-100), text #57534E (stone-600)

Implementation:
1. Use cva with 'category' variant for the 6 tag types
2. Base styles: h-8, rounded-full, px-4, text-xs, font-medium, inline-flex items-center justify-center
3. Add 'size' variant for sm/md/lg options
4. Add onClick prop for filter interaction
5. Add 'active' boolean prop for selected state styling
6. Export TagProps type and tagVariants

Pattern reference: Follow input.tsx CVA structure with forwardRef.
  </action>
  <verify>
TypeScript compiles without errors:
```bash
cd packages/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -20
```
  </verify>
  <done>
Tag component exists with 6 category variants (all, latest, clothing, accessories, shoes, bags), renders as pill-shaped buttons with category-specific colors matching decoded.pen specs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ActionButton component with 3 variants</name>
  <files>packages/web/lib/design-system/action-button.tsx</files>
  <action>
Create ActionButton component for action icons (like, bookmark, share).

Design specs from decoded.pen:
- ActionButton/Default: width 40px, height 40px, fill rgba(0,0,0,0.5) (#00000080), cornerRadius 9999 (circle), icon white
- ActionButton/Solid: width 40px, height 40px, fill primary color, cornerRadius 9999, icon white
- ActionButton/Outline: width 40px, height 40px, transparent bg, stroke border color 1px, cornerRadius 9999, icon foreground

Implementation:
1. Use cva with 'variant' for default/solid/outline
2. Base styles: w-10, h-10, rounded-full, inline-flex items-center justify-center, transition-colors
3. Accept 'icon' prop (ReactNode) for flexible icon rendering
4. Accept 'size' variant for sm (32px) / md (40px) / lg (48px)
5. Add hover/active state styling
6. Add disabled state
7. Use forwardRef pattern for button element
8. Export ActionButtonProps type and actionButtonVariants

Variant styles:
- default: bg-black/50 hover:bg-black/70 text-white
- solid: bg-primary hover:bg-primary/90 text-primary-foreground
- outline: bg-transparent border border-border hover:bg-accent text-foreground
  </action>
  <verify>
TypeScript compiles without errors:
```bash
cd packages/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -20
```
  </verify>
  <done>
ActionButton component exists with 3 variants (default, solid, outline), renders as circular buttons with appropriate background/border styles matching decoded.pen specs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update barrel exports in index.ts</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
Add Tag and ActionButton exports to the design system barrel file.

Add after existing exports:
```typescript
// Tag Component
export { Tag, tagVariants } from "./tag";
export type { TagProps } from "./tag";

// ActionButton Component
export { ActionButton, actionButtonVariants } from "./action-button";
export type { ActionButtonProps } from "./action-button";
```

Verify alphabetical or logical grouping is maintained.
  </action>
  <verify>
Import test passes:
```bash
cd packages/web && node -e "import('@/lib/design-system').then(m => console.log('Tag:', !!m.Tag, 'ActionButton:', !!m.ActionButton))" 2>&1 || echo "Check with tsc"
npx tsc --noEmit --skipLibCheck 2>&1 | head -20
```
  </verify>
  <done>
Tag and ActionButton are exported from design-system barrel, can be imported via `import { Tag, ActionButton } from "@/lib/design-system"`.
  </done>
</task>

</tasks>

<verification>
Run full type check to ensure no regressions:
```bash
cd packages/web && npx tsc --noEmit --skipLibCheck
```

Verify components can be imported:
```bash
cd packages/web && npx tsc --noEmit --skipLibCheck && echo "All types valid"
```
</verification>

<success_criteria>
1. Tag component renders with 6 category variants matching decoded.pen colors
2. ActionButton component renders with 3 variants (default, solid, outline)
3. Both components use CVA pattern consistent with existing design-system
4. TypeScript compilation succeeds with no errors
5. Components are exported from design-system barrel
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-01-core-interactive/v2.1-01-01-SUMMARY.md`
</output>
