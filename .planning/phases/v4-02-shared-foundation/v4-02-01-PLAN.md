---
phase: v4-02-shared-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/_shared/component-registry.md
  - specs/_shared/data-models.md
  - specs/_shared/api-contracts.md
autonomous: true

must_haves:
  truths:
    - "AI agent can look up any design system component's file path, props, and variants without reading source code"
    - "AI agent can find the TypeScript type for any data entity (post, spot, solution, image, user) by referencing data-models.md"
    - "AI agent can determine the exact request/response shape for any user-facing API route by referencing api-contracts.md"
    - "All 35+ design system components listed with verified file paths under packages/web/lib/design-system/"
    - "All 18 user-facing API routes (non-admin) documented with method, path, params, and response shape"
  artifacts:
    - path: "specs/_shared/component-registry.md"
      provides: "Complete design system component catalog with file paths, props, variants"
      contains: "Heading|Text|Card|ProductCard|GridCard|FeedCardBase|Hotspot|SpotCard|SpotDetail|NavBar|Tabs"
    - path: "specs/_shared/data-models.md"
      provides: "Canonical data shapes from TypeScript types with source file references"
      contains: "Post|Spot|Solution|Image|User|Item|Category"
    - path: "specs/_shared/api-contracts.md"
      provides: "API endpoint contracts for all user-facing routes"
      contains: "/api/v1/posts|/api/v1/spots|/api/v1/solutions|/api/v1/users|/api/v1/categories"
  key_links:
    - from: "specs/_shared/component-registry.md"
      to: "packages/web/lib/design-system/*.tsx"
      via: "file path references in Component Map"
      pattern: "packages/web/lib/design-system/"
    - from: "specs/_shared/data-models.md"
      to: "packages/web/lib/api/types.ts"
      via: "source file path reference"
      pattern: "lib/api/types.ts"
    - from: "specs/_shared/api-contracts.md"
      to: "packages/web/app/api/v1/**"
      via: "route path references"
      pattern: "/api/v1/"
---

<objective>
Create the three foundational shared reference documents: component registry (35+ design system components), data models (TypeScript type catalog), and API contracts (18 user-facing routes). These are the primary lookup tables that all subsequent screen specs will reference.

Purpose: Enable AI agents to find correct file paths, types, and API shapes without reading source code directly — eliminating guessed paths and stale references.
Output: 3 markdown files in `specs/_shared/`
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v4-01-archive-foundation/v4-01-02-SUMMARY.md
@.planning/phases/v4-02-shared-foundation/v4-02-CONTEXT.md

# Source files to read for component registry
@packages/web/lib/design-system/index.ts
@packages/web/lib/design-system/tokens.ts

# Source files to read for data models
@packages/web/lib/api/types.ts
@packages/web/lib/supabase/types.ts
@packages/web/lib/components/detail/types.ts

# Source files to read for API contracts
@packages/web/lib/api/client.ts
@packages/web/lib/api/posts.ts
@packages/web/lib/api/solutions.ts
@packages/web/lib/api/spots.ts
@packages/web/lib/api/users.ts
@packages/web/lib/api/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create component-registry.md — Design System Catalog</name>
  <files>specs/_shared/component-registry.md</files>
  <action>
Create `specs/_shared/component-registry.md` — the complete design system component catalog.

**Structure (Hybrid format per CONTEXT.md decision):**

1. **Summary Table** at top — one row per component: Name, File Path, Category (Primitive/Composite/Feature), Role (1-line summary)
2. **Detailed Sections** grouped by category — for each component: core props, notable variants, usage notes

**Steps:**

1. Read every `.tsx` and `.ts` file in `packages/web/lib/design-system/` (34 component files + tokens.ts + index.ts)
2. For each component, extract:
   - Exported component name(s) and their props interface
   - CVA variants (if any) — list variant names and their options
   - Core required props (the must-provide props)
   - Notable optional props that change behavior
3. Classify into three tiers:
   - **Primitive:** tokens, Heading, Text, Input, SearchInput, Card (base), Tag, Badge, Divider, ActionButton, LoadingSpinner, SkeletonCard
   - **Composite:** ProductCard, GridCard, FeedCardBase, ProfileHeaderCard, ArtistCard, StatCard, SpotCard, ShopCarouselCard, LeaderItem, RankingItem, SpotDetail, LoginCard, OAuthButton, GuestButton, StepIndicator, Hotspot, BottomSheet
   - **Feature:** DesktopHeader, MobileHeader, DesktopFooter, NavBar, NavItem, SectionHeader, Tabs
4. Write the document:
   - Summary table with ALL components (35+ rows)
   - Then grouped sections (Primitive → Composite → Feature)
   - Each component section: 3-8 lines max — name, file, core props table, variants list
   - For complex components (Card with composable slots, Hotspot with brand colors, SpotDetail with shop links), include a brief usage snippet (2-4 lines of JSX)
   - For simple components, skip usage example

**File path format:** `packages/web/lib/design-system/{filename}.tsx`
**Import path:** All components imported from `@/lib/design-system`

**IMPORTANT:**
- Verify every file path exists before writing (use `ls` or glob)
- Do NOT copy full TypeScript type definitions — list props in a compact table format
- Include the Skeleton variants where they exist (CardSkeleton, ProductCardSkeleton, etc.)
- Reference `tokens.ts` for design token values (colors, spacing, typography, shadows)

**Anti-features to exclude (per v4.0 decisions):**
- No i18n sections
- No version history
- No full TypeScript interface copies
- No implementation checklists
  </action>
  <verify>
- File exists at `specs/_shared/component-registry.md`
- Summary table has 35+ component rows
- Every file path in the document maps to an actual file: `for f in $(grep -oP 'packages/web/lib/design-system/\S+\.tsx?' specs/_shared/component-registry.md | sort -u); do test -f "$f" && echo "OK: $f" || echo "MISSING: $f"; done`
- Grouped sections cover Primitive, Composite, Feature tiers
- No TypeScript interface blocks (only prop tables)
  </verify>
  <done>
Component registry contains 35+ design system components organized by tier (Primitive/Composite/Feature), each with verified file path, core props, and variant list. AI agents can look up any component without reading source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create data-models.md — TypeScript Type Catalog</name>
  <files>specs/_shared/data-models.md</files>
  <action>
Create `specs/_shared/data-models.md` — canonical data shapes used across the application.

**Steps:**

1. Read the primary type sources:
   - `packages/web/lib/api/types.ts` (447 lines) — API response types
   - `packages/web/lib/supabase/types.ts` (656 lines) — Supabase generated types
   - `packages/web/lib/components/detail/types.ts` — detail view types
   - `packages/web/lib/components/fashion-scan/types.ts` — fashion scan types
2. For each major entity, document:
   - Entity name and source file path
   - Key fields (name, type, description) — in a table, NOT as copied TypeScript
   - Relationships to other entities (e.g., Post has many Spots, Spot has many Solutions)
3. Organize by domain:
   - **Content:** Post, Image, PostImage
   - **Discovery:** Spot, Item, Solution
   - **User:** User, Profile, Activity, Stats
   - **System:** Category, Badge

**Format for each entity:**

```markdown
### Post
**Source:** `packages/web/lib/api/types.ts` → `Post`

| Field | Type | Description |
|-------|------|-------------|
| id | string | UUID primary key |
| title | string | Post title |
| ... | ... | ... |

**Relations:** Post → PostImage[] → Image[], Post → Spot[]
```

**IMPORTANT:**
- Document the SHAPES that screens actually consume (API response types), not raw Supabase table types
- Note where API types differ from Supabase types (the table names mismatch: `post` vs `posts`)
- Include a "Type Source Map" table at the top: Entity → Source File → Type Name
- Add a note: "For the latest type definitions, read the source file directly. This document captures the stable shape for spec reference."
- Do NOT paste full TypeScript interfaces — use field tables
  </action>
  <verify>
- File exists at `specs/_shared/data-models.md`
- Contains Type Source Map table at top
- Covers at minimum: Post, Image, Spot, Item, Solution, User, Category
- Every source file path references a real file
- No raw TypeScript interface blocks (only field tables)
  </verify>
  <done>
Data models document catalogs all major entity types with field tables, source file paths, and entity relationships. AI agents can determine data shapes without reading TypeScript source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create api-contracts.md — API Endpoint Reference</name>
  <files>specs/_shared/api-contracts.md</files>
  <action>
Create `specs/_shared/api-contracts.md` — contracts for all user-facing API routes.

**Scope:** User-facing routes only (18 routes under `/api/v1/` excluding `/api/v1/admin/*`). Admin routes are documented separately in admin specs.

**Steps:**

1. Read every route handler in `packages/web/app/api/v1/` (excluding admin/):
   - `posts/route.ts` (GET)
   - `posts/[postId]/route.ts` (GET)
   - `posts/[postId]/spots/route.ts` (GET, POST)
   - `posts/with-solution/route.ts` (GET)
   - `posts/extract-metadata/route.ts` (POST)
   - `posts/analyze/route.ts` (POST)
   - `posts/upload/route.ts` (POST)
   - `solutions/[solutionId]/route.ts` (GET, PATCH)
   - `solutions/convert-affiliate/route.ts` (POST)
   - `solutions/extract-metadata/route.ts` (POST)
   - `spots/[spotId]/route.ts` (GET, PATCH)
   - `spots/[spotId]/solutions/route.ts` (GET, POST)
   - `users/me/route.ts` (GET)
   - `users/me/activities/route.ts` (GET)
   - `users/me/stats/route.ts` (GET)
   - `users/[userId]/route.ts` (GET)
   - `categories/route.ts` (GET)
2. For each route, document (Full OpenAPI style per CONTEXT.md decision):
   - HTTP method + path
   - Query parameters (name, type, required, description)
   - Request body shape (for POST/PATCH)
   - Response shape (success + error)
   - Auth requirement (public / authenticated)
   - Route handler file path
3. Also read the API client functions in `packages/web/lib/api/`:
   - `client.ts` — base fetch wrapper
   - `posts.ts`, `solutions.ts`, `spots.ts`, `users.ts`, `categories.ts`
   - Document the client function name that corresponds to each route

**Format:**

```markdown
### GET /api/v1/posts

**Client:** `fetchPosts()` in `packages/web/lib/api/posts.ts`
**Handler:** `packages/web/app/api/v1/posts/route.ts`
**Auth:** Public

| Param | Type | Required | Description |
|-------|------|----------|-------------|
| page | number | no | Page number (default: 1) |
| limit | number | no | Items per page (default: 20) |

**Response 200:**
```json
{ "data": [...], "total": 100, "page": 1 }
```
```

**Organization:** Group by resource (Posts, Solutions, Spots, Users, Categories)

**IMPORTANT:**
- Read actual route handler code to determine exact params and response shapes
- Do NOT guess response shapes — read the code
- Note any routes that proxy to an external backend API vs direct Supabase queries
- Include the `debug-env` route only if it's not dev-only (check the code)
  </action>
  <verify>
- File exists at `specs/_shared/api-contracts.md`
- Lists all 18 user-facing API routes (excluding admin)
- Each route has: method, path, params, response shape, auth, handler file path
- Every handler file path references a real file
- Client function names match actual exports in `lib/api/*.ts`
  </verify>
  <done>
API contracts document covers all 18 user-facing routes with full OpenAPI-style documentation: methods, params, request/response shapes, auth requirements, and both handler and client file paths. AI agents can determine exact API behavior without reading route handlers.
  </done>
</task>

</tasks>

<verification>
1. `specs/_shared/` directory contains 3 new files: component-registry.md, data-models.md, api-contracts.md
2. Component registry covers 35+ components with verified file paths
3. Data models covers all major entities (Post, Image, Spot, Item, Solution, User, Category)
4. API contracts covers all 18 user-facing routes with method, params, and response shapes
5. No file path in any document points to a non-existent file
6. No raw TypeScript interface blocks in any document (field tables only)
7. Total document sizes are reasonable (each under ~400 lines to remain useful as context)
</verification>

<success_criteria>
- 3 files created in `specs/_shared/`
- All file paths verified against filesystem
- Component registry has summary table + detailed sections for 35+ components
- Data models has Type Source Map + entity field tables for 7+ entities
- API contracts has full endpoint documentation for 18 routes grouped by resource
- Documents follow v4.0 format rules (no anti-features, verified paths)
</success_criteria>

<output>
After completion, create `.planning/phases/v4-02-shared-foundation/v4-02-01-SUMMARY.md`
</output>
