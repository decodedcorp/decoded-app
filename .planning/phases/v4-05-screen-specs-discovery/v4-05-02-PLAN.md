---
phase: v4-05-screen-specs-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/screens/discovery/SCR-DISC-03-feed.md
  - specs/screens/discovery/SCR-DISC-04-explore.md
autonomous: true

must_haves:
  truths:
    - "SCR-DISC-03 documents the feed page (/feed) with FeedCardBase usage, FeedClient, and infinite scroll data flow"
    - "SCR-DISC-03 has mobile + desktop ASCII wireframes showing social feed timeline layout"
    - "SCR-DISC-04 documents the explore page (/explore) with category filter grid, filterStore state, and responsive column switching"
    - "SCR-DISC-04 captures ExploreClient component structure, CategoryFilter, and filterStore integration"
    - "All file paths in both specs are verified against actual filesystem (no stale paths)"
  artifacts:
    - path: "specs/screens/discovery/SCR-DISC-03-feed.md"
      provides: "Feed page spec with FeedCardBase usage, infinite scroll, and timeline layout"
      contains: "Component Map"
    - path: "specs/screens/discovery/SCR-DISC-04-explore.md"
      provides: "Explore page spec with category filter grid, filterStore, and responsive columns"
      contains: "Component Map"
  key_links:
    - from: "SCR-DISC-03"
      to: "specs/_shared/store-map.md"
      via: "any feed-related state reference"
    - from: "SCR-DISC-04"
      to: "specs/_shared/store-map.md"
      via: "filterStore state shape reference"
    - from: "SCR-DISC-03"
      to: "specs/flows/FLW-01-discovery.md"
      via: "flow context cross-reference"
    - from: "SCR-DISC-04"
      to: "specs/flows/FLW-01-discovery.md"
      via: "flow context cross-reference"
---

<objective>
Write screen specs SCR-DISC-03 (Feed) and SCR-DISC-04 (Explore) for the Discovery bundle, with verified component paths and implementation status assessment.

Purpose: Complete the Discovery bundle documentation so AI agents have full coverage of all discovery screens — from home to search to feed to explore.
Output: Two spec files in `specs/screens/discovery/` with EARS requirements, wireframes, and implementation status tags.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Templates and shared foundation
@specs/shared/templates/screen-spec-template.md
@specs/_shared/component-registry.md
@specs/_shared/store-map.md
@specs/_shared/data-models.md
@specs/_shared/api-contracts.md

# Flow context (FLW-01 is the primary flow for Discovery)
@specs/flows/FLW-01-discovery.md

# Stale path reference
@specs/_archive/v2.1.0/STALE-PATHS-AUDIT.md

# Feed route and client:
@packages/web/app/feed/page.tsx
@packages/web/app/feed/FeedClient.tsx

# Feed components:
@packages/web/lib/components/feed/FeedHeader.tsx
@packages/web/lib/components/feed/FeedTabs.tsx
@packages/web/lib/components/feed/NewPostsIndicator.tsx

# Feed-related DS component:
@packages/web/lib/design-system/feed-card.tsx

# Explore route and client:
@packages/web/app/explore/page.tsx
@packages/web/app/explore/ExploreClient.tsx

# Explore components:
@packages/web/lib/components/explore/CategoryFilter.tsx
@packages/web/lib/components/explore/ExploreCardCell.tsx
@packages/web/lib/components/explore/ExploreFilterBar.tsx
@packages/web/lib/components/explore/ExploreFilterSheet.tsx
@packages/web/lib/components/explore/ExploreHeader.tsx
@packages/web/lib/components/explore/ExploreSortControls.tsx
@packages/web/lib/components/explore/FilterChip.tsx

# Stores:
@packages/shared/stores/filterStore.ts

# Hooks:
@packages/web/lib/hooks/usePosts.ts
@packages/web/lib/hooks/useImages.ts
@packages/web/lib/hooks/useCategories.ts
@packages/web/lib/hooks/useResponsiveGridSize.ts
@packages/web/lib/hooks/useMediaQuery.ts

# Shared grid component:
@packages/web/lib/components/ThiingsGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCR-DISC-03-feed.md</name>
  <files>specs/screens/discovery/SCR-DISC-03-feed.md</files>
  <action>
Write `SCR-DISC-03-feed.md` following screen-spec-template format:

**Structure:**
1. **Header** — ID: SCR-DISC-03, Route: `/feed`, Status: implemented
2. **Purpose** — One sentence: user browses a social feed timeline of decoded posts with infinite scroll and tab-based filtering
3. **Component Map** — Table with verified paths:
   - FeedClient (`packages/web/app/feed/FeedClient.tsx`) — verify
   - FeedHeader (`packages/web/lib/components/feed/FeedHeader.tsx`) — verify
   - FeedTabs (`packages/web/lib/components/feed/FeedTabs.tsx`) — verify
   - NewPostsIndicator (`packages/web/lib/components/feed/NewPostsIndicator.tsx`) — verify
   - FeedCardBase (DS: component-registry) — reference design system
   - GridCard (DS: component-registry) — check if used in feed
   - usePosts hook (`packages/web/lib/hooks/usePosts.ts`) — verify
   - Verify ALL paths with `ls`
   - **CRITICAL: Read FeedClient.tsx to understand** what components it uses, how it fetches data (useInfinitePosts? usePosts?), and what the actual infinite scroll mechanism is (IntersectionObserver? react-query infinite? manual pagination?)

4. **Layout — Mobile (default)** — ASCII wireframe showing:
   - MobileHeader
   - FeedHeader with title/tabs
   - FeedTabs (tab bar for filtering — research actual tab options)
   - NewPostsIndicator (floating indicator for new content)
   - Feed timeline: vertical list of FeedCardBase cards
   - Infinite scroll trigger at bottom
   - NavBar at bottom

5. **Layout — Desktop** — ASCII wireframe:
   - Centered feed column (max-width constrained)
   - Wider card layout
   - Breakpoint table: `| Element | Mobile | Desktop |`

6. **Requirements** — EARS format with status tags:
   - Data Loading: "When feed page loads, the system shall fetch initial page of posts" — check actual data fetching in FeedClient
   - Infinite Scroll: "When user scrolls near the bottom, the system shall fetch the next page of posts" — check impl (useInfiniteQuery? IntersectionObserver?)
   - Tabs: "When user taps a feed tab, the system shall filter displayed posts by [actual tab categories]" — read FeedTabs to determine actual tab options
   - New Posts: "When new posts are available, the system shall show NewPostsIndicator" — check impl
   - Card Interaction: "When user taps a feed card, the system shall navigate to /posts/[id] detail page"
   - Tag each `✅` or `⚠️ NOT-IMPL`

7. **State** — Document state management:
   - React Query for post data (cache key, pagination)
   - Any local state for tab selection
   - Check if filterStore is used or if feed has its own state

8. **Data Flow** — Infinite scroll sequence:
   - Initial fetch → render cards → scroll → next page fetch → append → repeat
   - Document the actual hook and API endpoint used

9. **Navigation** — Entry/exit matching FLW-01:
   - Entry: bottom nav tab, direct URL
   - Exit: card tap → detail (SCR-VIEW-01), header actions

10. **Error & Empty States** — Empty feed, API error, loading skeletons

**Line budget:** Target 200 lines. Max 250.

**Stale path handling:** No feed-specific stale paths in STALE-PATHS-AUDIT. Verify all paths fresh.

**Cross-references:**
- `See: SCR-DISC-01 — Home page (sibling discovery screen)`
- `See: SCR-VIEW-01 — Post detail (card tap destination)`
- `See: FLW-01 — Discovery Flow (feed entry in navigation contract)`
  </action>
  <verify>
1. `wc -l specs/screens/discovery/SCR-DISC-03-feed.md` — must be ≤250 lines
2. Grep for `packages/web/` paths; verify each with `ls`
3. File documents infinite scroll mechanism (actual implementation, not assumed)
4. File documents FeedCardBase usage from design system
5. EARS requirements present with status tags
6. File contains mobile + desktop wireframes
  </verify>
  <done>
SCR-DISC-03-feed.md exists with ≤250 lines, all file paths verified, infinite scroll data flow documented from actual code, FeedCardBase usage noted, and EARS requirements tagged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SCR-DISC-04-explore.md</name>
  <files>specs/screens/discovery/SCR-DISC-04-explore.md</files>
  <action>
Write `SCR-DISC-04-explore.md` following screen-spec-template format:

**Structure:**
1. **Header** — ID: SCR-DISC-04, Route: `/explore`, Status: implemented
2. **Purpose** — One sentence: user browses content through category-filtered grid with responsive columns and sort controls
3. **Component Map** — Table with verified paths:
   - ExploreClient (`packages/web/app/explore/ExploreClient.tsx`) — verify
   - ExploreHeader (`packages/web/lib/components/explore/ExploreHeader.tsx`) — verify
   - CategoryFilter (`packages/web/lib/components/explore/CategoryFilter.tsx`) — verify
   - ExploreFilterBar (`packages/web/lib/components/explore/ExploreFilterBar.tsx`) — verify
   - ExploreFilterSheet (`packages/web/lib/components/explore/ExploreFilterSheet.tsx`) — verify
   - ExploreSortControls (`packages/web/lib/components/explore/ExploreSortControls.tsx`) — verify
   - ExploreCardCell (`packages/web/lib/components/explore/ExploreCardCell.tsx`) — verify
   - FilterChip (`packages/web/lib/components/explore/FilterChip.tsx`) — verify
   - ThiingsGrid (`packages/web/lib/components/ThiingsGrid.tsx`) — verify (NOTE: moved from grid/ThiingsGrid.tsx per STALE-PATHS-AUDIT)
   - useCategories hook (`packages/web/lib/hooks/useCategories.ts`) — verify
   - useImages or usePosts hook — check which is used in ExploreClient
   - useResponsiveGridSize hook (`packages/web/lib/hooks/useResponsiveGridSize.ts`) — verify
   - filterStore (`packages/shared/stores/filterStore.ts`) — verify
   - GridCard (DS: component-registry)
   - Tag (DS: component-registry)
   - Verify ALL paths with `ls`
   - **CRITICAL: Read ExploreClient.tsx to understand** actual data flow — does it use filterStore? What API does it call? How does category selection work?

4. **Layout — Mobile (default)** — ASCII wireframe showing:
   - MobileHeader
   - ExploreHeader with title
   - CategoryFilter (horizontal scrollable chip bar)
   - ExploreFilterBar / ExploreSortControls
   - Content grid: 2-column grid of cards (ExploreCardCell)
   - Infinite scroll or pagination at bottom
   - NavBar at bottom

5. **Layout — Desktop** — ASCII wireframe:
   - DesktopHeader
   - Wider filter bar
   - 3-4 column grid
   - ExploreFilterSheet as sidebar panel (not bottom sheet)
   - Breakpoint table: `| Element | Mobile | Desktop |`
     - Grid: 2col → 3-4col
     - FilterSheet: BottomSheet → Side panel
     - Card size: compact → standard

6. **Requirements** — EARS format with status tags:
   - Data Loading: "When explore page loads, the system shall fetch categories and initial content grid" — check actual impl
   - Category Filter: "When user selects a category chip, the system shall update filterStore and re-fetch content filtered by that category" — check if filterStore is actually used or if it's URL params
   - Sort: "When user changes sort option, the system shall re-fetch content with new sort order" — check impl
   - Responsive Grid: "When viewport width changes, the system shall adjust grid columns via useResponsiveGridSize" — check impl
   - Filter Sheet: "When user taps filter button (mobile), the system shall open ExploreFilterSheet as bottom sheet" — check impl
   - Card Interaction: "When user taps an explore card, the system shall navigate to content detail page"
   - Tag each `✅` or `⚠️ NOT-IMPL`
   - **Stale filter components:** STALE-PATHS-AUDIT lists 7 missing filter/ components (DesktopFilterBar, FilterBreadcrumb, FilterChip, FilterDropdown, FilterOption, HierarchicalFilter, MobileFilterSheet). These were in OLD spec SCR-DISC-02-filter.md. The NEW explore implementation uses explore/ components instead. Document this difference. Mark old filter/ components as `⚠️ NOT-IMPL` only if their features are referenced.

7. **State** — filterStore integration:
   - Document filterStore fields used: selectedCategory, sortBy, etc.
   - React Query cache for explore content
   - useResponsiveGridSize for column count

8. **Responsive Grid** — Brief column switching logic:
   - Mobile (< 768px): 2 columns
   - Tablet (768-1024px): 3 columns
   - Desktop (> 1024px): 4 columns
   - Verify actual breakpoints from useResponsiveGridSize

9. **Navigation** — Entry/exit matching FLW-01
10. **Error & Empty States** — No content for filter, API error, loading grid skeleton

**Line budget:** Target 200 lines. Max 250.

**Stale path handling:** STALE-PATHS-AUDIT lists:
- ThiingsGrid: moved from `grid/ThiingsGrid.tsx` → `ThiingsGrid.tsx` — use corrected path
- 7 filter/ components: all missing — the explore/ components are the current implementation. Do NOT include old filter/ paths. If features from those components exist in explore/ components, note the replacement.

**Cross-references:**
- `See: SCR-DISC-01 — Home page (sibling discovery screen)`
- `See: SCR-DISC-02 — Search overlay (search icon from header)`
- `See: SCR-VIEW-01 — Post detail (card tap destination)`
- `See: FLW-01 — Discovery Flow (explore entry in navigation contract)`
  </action>
  <verify>
1. `wc -l specs/screens/discovery/SCR-DISC-04-explore.md` — must be ≤250 lines
2. Grep for `packages/web/` paths; verify each with `ls`
3. File documents filterStore integration (actual implementation, not assumed)
4. File documents responsive grid column switching
5. File correctly references ThiingsGrid at new path (not old grid/ThiingsGrid.tsx)
6. EARS requirements present with status tags
7. File contains mobile + desktop wireframes with breakpoint table
  </verify>
  <done>
SCR-DISC-04-explore.md exists with ≤250 lines, all file paths verified, filterStore integration documented from actual code, responsive grid columns specified, ThiingsGrid path corrected, and EARS requirements tagged.
  </done>
</task>

</tasks>

<verification>
1. Both files exist in `specs/screens/discovery/`
2. Combined line count ≤500 (250 + 250)
3. Zero stale file paths — every `packages/web/` reference verified against filesystem
4. ThiingsGrid path corrected from stale `grid/ThiingsGrid.tsx` to `ThiingsGrid.tsx`
5. Both specs cross-reference other DISC specs and FLW-01
6. EARS requirements with implementation status tags in both
7. SCR-DISC-03 documents actual infinite scroll mechanism
8. SCR-DISC-04 documents filterStore integration and responsive grid columns
</verification>

<success_criteria>
- SCR-DISC-03 covers feed timeline with FeedCardBase, infinite scroll, and tab filtering
- SCR-DISC-04 covers explore grid with category filters, filterStore, and responsive column switching
- All component file paths verified (zero stale paths)
- ThiingsGrid and old filter/ components handled correctly (path fix and NOT-IMPL)
- An AI agent can use these specs to understand and modify the feed and explore features
</success_criteria>

<output>
After completion, create `.planning/phases/v4-05-screen-specs-discovery/v4-05-02-SUMMARY.md`
</output>
