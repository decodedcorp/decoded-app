---
phase: v4-05-screen-specs-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/screens/discovery/SCR-DISC-01-home.md
  - specs/screens/discovery/SCR-DISC-02-search.md
autonomous: true

must_haves:
  truths:
    - "SCR-DISC-01 documents the home page route (/) with verified component file paths including HomeClient and all main/ section components"
    - "SCR-DISC-01 has mobile + desktop ASCII wireframes showing Hero carousel, trending, best sections, celebrity grid"
    - "SCR-DISC-01 replaces stale HomeClient path (lib/components/HomeClient.tsx -> app/HomeClient.tsx) and marks CardCell/CardSkeleton as NOT-IMPL"
    - "SCR-DISC-02 documents full-screen search overlay with searchStore state transitions and multi-tab result layout"
    - "SCR-DISC-02 has mobile + desktop wireframes showing SearchOverlay, SearchTabs, and result sections"
    - "All file paths in both specs are verified against actual filesystem (no stale paths from STALE-PATHS-AUDIT)"
  artifacts:
    - path: "specs/screens/discovery/SCR-DISC-01-home.md"
      provides: "Home page spec with component map, wireframes, EARS requirements"
      contains: "Component Map"
    - path: "specs/screens/discovery/SCR-DISC-02-search.md"
      provides: "Search overlay spec with searchStore state transitions and multi-tab results"
      contains: "State Transitions"
  key_links:
    - from: "SCR-DISC-01"
      to: "SCR-DISC-02"
      via: "cross-reference: search bar tap triggers SCR-DISC-02 overlay"
    - from: "SCR-DISC-02"
      to: "specs/_shared/store-map.md"
      via: "searchStore state shape reference"
    - from: "SCR-DISC-01"
      to: "specs/flows/FLW-01-discovery.md"
      via: "flow context cross-reference"
---

<objective>
Write screen specs SCR-DISC-01 (Home) and SCR-DISC-02 (Search) for the Discovery bundle, with all component file paths verified against the actual codebase and stale paths from STALE-PATHS-AUDIT corrected.

Purpose: Enable AI agents to modify home and search screens without file path errors, referencing correct components and understanding navigation and state contracts.
Output: Two spec files in `specs/screens/discovery/` following v4.0 screen spec template with EARS requirements, mobile+desktop wireframes, and implementation status tags.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Templates and shared foundation
@specs/shared/templates/screen-spec-template.md
@specs/_shared/component-registry.md
@specs/_shared/store-map.md
@specs/_shared/data-models.md
@specs/_shared/api-contracts.md

# Flow context (FLW-01 is the PRIMARY flow for Discovery)
@specs/flows/FLW-01-discovery.md

# Stale path reference (Discovery stale paths to verify/replace)
@specs/_archive/v2.1.0/STALE-PATHS-AUDIT.md

# Home page route and client:
@packages/web/app/page.tsx
@packages/web/app/HomeClient.tsx

# Key home components (verify each path exists before including in spec):
@packages/web/lib/components/main/HeroCarousel.tsx
@packages/web/lib/components/main/HeroSection.tsx
@packages/web/lib/components/main/HomeAnimatedContent.tsx
@packages/web/lib/components/main/TrendingSection.tsx
@packages/web/lib/components/main/BestSection.tsx
@packages/web/lib/components/main/CelebritySection.tsx
@packages/web/lib/components/main/CelebrityGrid.tsx
@packages/web/lib/components/main/ArtistSpotlightSection.tsx
@packages/web/lib/components/main/DecodedPickSection.tsx
@packages/web/lib/components/main/DiscoverSection.tsx
@packages/web/lib/components/main/TodayDecodedSection.tsx
@packages/web/lib/components/main/WhatsNewSection.tsx
@packages/web/lib/components/main/CTASection.tsx
@packages/web/lib/components/main/SearchSection.tsx
@packages/web/lib/components/main/MainFooter.tsx
@packages/web/lib/components/main/SectionHeader.tsx
@packages/web/lib/components/main/ItemCard.tsx
@packages/web/lib/components/main/StyleCard.tsx

# Search route and client:
@packages/web/app/search/page.tsx
@packages/web/app/search/SearchPageClient.tsx

# Key search components:
@packages/web/lib/components/search/SearchOverlay.tsx
@packages/web/lib/components/search/SearchInput.tsx
@packages/web/lib/components/search/SearchTabs.tsx
@packages/web/lib/components/search/SearchResults.tsx
@packages/web/lib/components/search/SearchSuggestions.tsx
@packages/web/lib/components/search/RecentSearches.tsx
@packages/web/lib/components/search/TrendingSearches.tsx
@packages/web/lib/components/search/EmptySearchState.tsx
@packages/web/lib/components/search/ItemResultSection.tsx
@packages/web/lib/components/search/MediaResultSection.tsx
@packages/web/lib/components/search/MediaResultCard.tsx
@packages/web/lib/components/search/PeopleResultSection.tsx
@packages/web/lib/components/search/PersonResultCard.tsx

# Stores:
@packages/shared/stores/searchStore.ts

# Hooks:
@packages/web/lib/hooks/useSearch.ts
@packages/web/lib/hooks/useSearchURLSync.ts
@packages/web/lib/hooks/useScrollAnimation.ts

# Animation:
@packages/web/lib/hooks/useScrollAnimation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCR-DISC-01-home.md</name>
  <files>specs/screens/discovery/SCR-DISC-01-home.md</files>
  <action>
Create directory `specs/screens/discovery/` if it doesn't exist.

Write `SCR-DISC-01-home.md` following the screen-spec-template format with these specifics:

**Structure:**
1. **Header** — ID: SCR-DISC-01, Route: `/`, Status: implemented
2. **Purpose** — One sentence: user views curated home feed with hero carousel, trending content, best picks, and celebrity/artist spotlights
3. **Component Map** — Table with Region | Component | File | Props/Notes columns
   - Verify EVERY file path with `ls` before including — no stale paths allowed
   - **CRITICAL stale path fix:** HomeClient was at `lib/components/HomeClient.tsx` — it is NOW at `packages/web/app/HomeClient.tsx` — use the verified new path
   - CardCell and CardSkeleton from STALE-PATHS-AUDIT are missing — do NOT include them; mark as `⚠️ NOT-IMPL` if features require them
   - For design system components (SectionHeader, ArtistCard, GridCard, etc.), reference as "DS: component-registry"
   - For non-DS components, use full verified path (e.g., `packages/web/lib/components/main/HeroCarousel.tsx`)
   - List all actual files in `packages/web/lib/components/main/` — these are the home sections
   - Component tree depth: 2 levels (page → HomeClient → section components → their DS components)

4. **Layout — Mobile (default)** — ASCII wireframe showing:
   - MobileHeader with search icon (tap → SCR-DISC-02)
   - HeroCarousel (full-width swipeable carousel)
   - TrendingSection
   - BestSection
   - CelebritySection / CelebrityGrid
   - ArtistSpotlightSection
   - DecodedPickSection / DiscoverSection / TodayDecodedSection / WhatsNewSection
   - CTASection
   - MainFooter
   - Component-level detail: show buttons, text labels, card positions

5. **Layout — Desktop (>=768px)** — ASCII wireframe showing desktop adaptation:
   - DesktopHeader with search bar
   - Centered content area with max-width
   - Grid layouts (more columns for card sections)
   - Breakpoint table: `| Element | Mobile | Desktop |`

6. **Requirements** — EARS format with implementation status tags:
   - Data Loading: "When home page loads, the system shall render pre-fetched sections (SSR/SSG)" — check if page.tsx uses server-side data or client fetch
   - GSAP Scroll Animations: "When user scrolls into a section, the system shall trigger GSAP scroll-based entrance animations via useScrollAnimation" — tag `✅` or check impl
   - Hero: "When hero carousel is visible, the system shall auto-rotate slides with swipe support" — check impl
   - Navigation: "When user taps search icon/bar, the system shall navigate to /search (SCR-DISC-02)"
   - Navigation: "When user taps a content card, the system shall navigate to /posts/[id] detail view"
   - Navigation: "When user taps a celebrity/artist card, the system shall navigate to explore with artist filter"
   - Tag each `✅` or `⚠️ NOT-IMPL` based on actual code

7. **State** — Minimal (home is primarily server-rendered); document any client state used
8. **Navigation** — Table matching FLW-01 entry/exit points for home
9. **Error & Empty States** — Loading skeleton, API error, empty sections

**Line budget:** Target 250 lines (justification: 18 section components + both wireframes). Max 300.

**Stale path handling:** Cross-reference STALE-PATHS-AUDIT.md for SCR-DISC-01 entries:
- HomeClient: moved from `lib/components/HomeClient.tsx` → `app/HomeClient.tsx` — use new path
- CardCell, CardSkeleton: missing — mark as `⚠️ NOT-IMPL`

**Cross-references (1 line each):**
- `See: SCR-DISC-02 — Search overlay (search icon tap from header)`
- `See: SCR-DISC-03 — Feed (bottom nav tab)`
- `See: SCR-DISC-04 — Explore (bottom nav tab)`
- `See: FLW-01 — Discovery Flow (navigation contract)`
  </action>
  <verify>
1. `wc -l specs/screens/discovery/SCR-DISC-01-home.md` — must be ≤300 lines
2. Grep for `packages/web/` paths in the file; verify each with `ls` — zero mismatches
3. File contains corrected HomeClient path (`app/HomeClient.tsx`, NOT `lib/components/HomeClient.tsx`)
4. File contains "Mobile" and "Desktop" ASCII wireframe sections
5. File contains EARS-format requirements with `✅` / `⚠️ NOT-IMPL` / `📋 PLANNED` tags
6. No stale paths from STALE-PATHS-AUDIT.md appear without correction
  </verify>
  <done>
SCR-DISC-01-home.md exists with ≤300 lines, all file paths verified (including corrected HomeClient path), both wireframes present, EARS requirements tagged, and cross-references to DISC-02/03/04/FLW-01 included.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SCR-DISC-02-search.md</name>
  <files>specs/screens/discovery/SCR-DISC-02-search.md</files>
  <action>
Write `SCR-DISC-02-search.md` following screen-spec-template format with these specifics:

**Structure:**
1. **Header** — ID: SCR-DISC-02, Route: `/search`, Status: implemented
2. **Purpose** — One sentence: user searches across content, people, and items via full-screen overlay with debounced query, multi-tab results, and search suggestions
3. **Component Map** — Table with verified paths:
   - SearchPageClient (`packages/web/app/search/SearchPageClient.tsx`) — verify
   - SearchOverlay (`packages/web/lib/components/search/SearchOverlay.tsx`) — verify
   - SearchInput (`packages/web/lib/components/search/SearchInput.tsx`) — verify (NOTE: separate from DS SearchInput)
   - SearchTabs (`packages/web/lib/components/search/SearchTabs.tsx`) — verify
   - SearchResults (`packages/web/lib/components/search/SearchResults.tsx`) — verify
   - SearchSuggestions (`packages/web/lib/components/search/SearchSuggestions.tsx`) — verify
   - RecentSearches (`packages/web/lib/components/search/RecentSearches.tsx`) — verify
   - TrendingSearches (`packages/web/lib/components/search/TrendingSearches.tsx`) — verify
   - EmptySearchState (`packages/web/lib/components/search/EmptySearchState.tsx`) — verify
   - ItemResultSection (`packages/web/lib/components/search/ItemResultSection.tsx`) — verify
   - MediaResultSection / MediaResultCard — verify
   - PeopleResultSection / PersonResultCard — verify
   - useSearch hook (`packages/web/lib/hooks/useSearch.ts`) — verify
   - useSearchURLSync hook (`packages/web/lib/hooks/useSearchURLSync.ts`) — verify
   - searchStore (`packages/shared/stores/searchStore.ts`) — verify
   - Verify ALL paths with `ls`

4. **Layout — Mobile (default)** — ASCII wireframe showing:
   - Full-screen overlay (covers entire viewport)
   - Search input with auto-focus at top
   - Pre-search state: RecentSearches + TrendingSearches
   - Active search state: SearchTabs (All / People / Items / Media) + result sections
   - Empty state: EmptySearchState

5. **Layout — Desktop** — ASCII wireframe:
   - Centered overlay or full-page layout
   - Multi-column result grid
   - Breakpoint table: `| Element | Mobile | Desktop |`

6. **State Transitions** — searchStore state flow:
   - idle (no query) → searching (query entered, debounce) → results (API response) → empty (no matches)
   - Document searchStore fields: query, results, isSearching, selectedTab
   - Document useSearchURLSync: how query syncs to URL params

7. **Requirements** — EARS format with status tags:
   - "When user types in search input, the system shall debounce input (300ms) and update searchStore.query" — check impl
   - "When searchStore.query changes, the system shall fetch results via search API" — check impl
   - "When results are loaded, the system shall display them in tab-separated sections (All, People, Items, Media)" — check impl
   - "When user taps a search tab, the system shall filter visible results to that category" — check impl
   - "When user taps a result card, the system shall navigate to the corresponding detail page" — check impl
   - "When search input is empty, the system shall show recent searches and trending searches" — check impl
   - "When user taps a recent/trending search term, the system shall populate the input and trigger search" — check impl
   - URL sync: "When search page loads with ?q= parameter, the system shall populate query from URL" — check impl
   - Tag each `✅` or `⚠️ NOT-IMPL`

8. **Navigation** — Entry/exit matching FLW-01:
   - Entry: header search icon tap, /search direct URL
   - Exit: result tap → detail, back → previous page

9. **Error & Empty States** — No results, API error, loading skeletons

**Line budget:** Target 200 lines. Max 250.

**Stale path handling:** STALE-PATHS-AUDIT has no missing search components (all exist). But verify each path anyway.

**Cross-references:**
- `See: SCR-DISC-01 — Home page (search icon entry point)`
- `See: FLW-01 — Discovery Flow (search state transitions)`
  </action>
  <verify>
1. `wc -l specs/screens/discovery/SCR-DISC-02-search.md` — must be ≤250 lines
2. Grep for `packages/web/` paths; verify each with `ls`
3. File contains searchStore state transition documentation
4. File contains multi-tab result layout in wireframes
5. File contains EARS requirements with implementation status tags
6. File contains mobile + desktop wireframe sections
  </verify>
  <done>
SCR-DISC-02-search.md exists with ≤250 lines, all file paths verified, searchStore state transitions documented, multi-tab results in wireframes, EARS requirements tagged, and cross-references included.
  </done>
</task>

</tasks>

<verification>
1. Both files exist in `specs/screens/discovery/`
2. Combined line count ≤550 (300 + 250)
3. Zero stale file paths — every `packages/web/` reference verified against filesystem
4. HomeClient path corrected from stale `lib/components/HomeClient.tsx` to `app/HomeClient.tsx`
5. Both specs cross-reference each other and FLW-01
6. EARS requirements present with `✅` / `⚠️ NOT-IMPL` / `📋 PLANNED` tags
7. Both specs have mobile + desktop wireframes
8. searchStore state transitions documented in SCR-DISC-02
</verification>

<success_criteria>
- SCR-DISC-01-home.md covers the home page with all 18 section components mapped to verified paths
- SCR-DISC-02-search.md covers full-screen search overlay with searchStore state flow and multi-tab results
- All component file paths are verified (zero stale paths)
- Stale HomeClient path corrected; missing CardCell/CardSkeleton marked as NOT-IMPL
- An AI agent reading these specs can identify exactly which files to modify for any home or search change
</success_criteria>

<output>
After completion, create `.planning/phases/v4-05-screen-specs-discovery/v4-05-01-SUMMARY.md`
</output>
