---
phase: v2-03-card-components
plan: 03
type: execute
wave: 2
depends_on: ["v2-03-01"]
files_modified:
  - packages/web/lib/design-system/feed-card.tsx
  - packages/web/lib/design-system/profile-header-card.tsx
  - packages/web/lib/components/FeedCard.tsx
  - packages/web/lib/components/profile/ProfileHeader.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "FeedCard displays image with engagement overlay (item count badge)"
    - "FeedCard hover state shows shadow elevation"
    - "ProfileHeaderCard displays avatar, name, username, bio, and action buttons"
    - "ProfileHeaderCard skeleton shows loading state for user data"
    - "Existing FeedCard and ProfileHeader functionality is preserved"
  artifacts:
    - path: "packages/web/lib/design-system/feed-card.tsx"
      provides: "FeedCard design-system component"
      exports: ["FeedCard", "FeedCardSkeleton"]
    - path: "packages/web/lib/design-system/profile-header-card.tsx"
      provides: "ProfileHeaderCard component"
      exports: ["ProfileHeaderCard", "ProfileHeaderCardSkeleton"]
    - path: "packages/web/lib/components/FeedCard.tsx"
      provides: "Refactored FeedCard using design-system Card"
      exports: ["FeedCard", "FeedCardSkeleton", "FeedCardItem"]
    - path: "packages/web/lib/components/profile/ProfileHeader.tsx"
      provides: "Refactored ProfileHeader using design-system Card"
      exports: ["ProfileHeader"]
  key_links:
    - from: "packages/web/lib/design-system/feed-card.tsx"
      to: "packages/web/lib/design-system/card.tsx"
      via: "imports Card for composition"
      pattern: "import.*Card.*from.*card"
    - from: "packages/web/lib/components/FeedCard.tsx"
      to: "packages/web/lib/design-system"
      via: "imports Card or FeedCard base"
      pattern: "import.*from.*design-system"
---

<objective>
Create FeedCard and ProfileHeaderCard components, then refactor existing components to use them.

Purpose: Implement content stream cards (FeedCard) and user profile display (ProfileHeaderCard) with consistent design system styling while preserving all existing functionality including GSAP Flip animations and store integrations.

Output:
- FeedCard design-system component with image and overlay support
- ProfileHeaderCard for user profile display with avatar, stats, actions
- Refactored existing FeedCard.tsx preserving FLIP animation
- Refactored ProfileHeader.tsx preserving store connections
- Skeleton variants for loading states
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/phases/v2-03-card-components/v2-03-CONTEXT.md

# Prior plan (base Card component)
@.planning/phases/v2-03-card-components/v2-03-01-PLAN.md

# Existing components to refactor
@packages/web/lib/components/FeedCard.tsx
@packages/web/lib/components/profile/ProfileHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeedCard design-system component</name>
  <files>packages/web/lib/design-system/feed-card.tsx</files>
  <action>
Create FeedCard component in design-system that can be used as base:

1. **FeedCard props (design-system version):**
   - `imageUrl`: Optional string for image
   - `aspectRatio`: Aspect ratio string (default "4/5" like Instagram)
   - `overlay`: Optional ReactNode for badges/counts
   - `link`: Optional string href
   - `onClick`: Optional click handler
   - `priority`: Boolean for eager loading
   - `alt`: String for image alt
   - `children`: Optional ReactNode for footer content

2. **Layout structure:**
   ```
   ┌─────────────────────┐
   │                     │
   │       IMAGE         │ 4:5 aspect ratio
   │   (with overlay)    │
   │                     │
   │           [BADGE]   │ Overlay bottom-right
   ├─────────────────────┤
   │   [Optional Footer] │ Children slot
   └─────────────────────┘
   ```

3. **Styling:**
   - Use Card with interactive variant
   - Image container with aspect-[4/5]
   - Overlay positioned absolute for badges
   - Rounded corners with overflow-hidden
   - Hover shows shadow-lg elevation

4. **FeedCardSkeleton:**
   - Image placeholder with shimmer
   - Optional footer placeholder based on showFooter prop

Note: This is the design-system base. The actual FeedCard.tsx in components/ will use this and add GSAP Flip logic.

Export FeedCard (design system version), FeedCardProps, FeedCardSkeleton.
  </action>
  <verify>
Run TypeScript check:
```bash
cd packages/web && npx tsc --noEmit
```
  </verify>
  <done>FeedCard design-system component renders with image and overlay support. FeedCardSkeleton shows loading state.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProfileHeaderCard component</name>
  <files>packages/web/lib/design-system/profile-header-card.tsx</files>
  <action>
Create ProfileHeaderCard component for user profile display:

1. **ProfileHeaderCard props:**
   - `avatarUrl`: Optional string for avatar image
   - `displayName`: String for user's display name
   - `username`: String for @username
   - `bio`: Optional string for user bio
   - `actions`: Optional ReactNode for action buttons (settings, logout)
   - `stats`: Optional array of { label: string, value: string | number } for stats display
   - `onAvatarClick`: Optional click handler for avatar

2. **Layout structure:**
   ```
   ┌────────────────────────────────────────┐
   │  ┌──────┐                              │
   │  │Avatar│  Display Name        [Actions]│
   │  │      │  @username                    │
   │  └──────┘  Bio text that can be        │
   │            two lines max...             │
   ├────────────────────────────────────────┤
   │  [Stat 1]  [Stat 2]  [Stat 3]          │ Optional stats row
   └────────────────────────────────────────┘
   ```

3. **Styling:**
   - Use Card component as base
   - Avatar: 60px mobile, 80px desktop with rounded-full
   - Avatar placeholder shows initials when no image
   - Display name: text-lg md:text-xl font-bold
   - Username: text-sm text-muted-foreground
   - Bio: text-sm text-muted-foreground line-clamp-2
   - Stats: Flex row with dividers, centered values

4. **ProfileHeaderCardSkeleton:**
   - Avatar placeholder circle with shimmer
   - Name/username text placeholders
   - Bio placeholder lines
   - Stats placeholder blocks

Export ProfileHeaderCard, ProfileHeaderCardProps, ProfileHeaderCardSkeleton.
  </action>
  <verify>
Run TypeScript check:
```bash
cd packages/web && npx tsc --noEmit
```
  </verify>
  <done>ProfileHeaderCard renders avatar, name, username, bio, actions, and optional stats. ProfileHeaderCardSkeleton shows loading state.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor existing components and update exports</name>
  <files>packages/web/lib/components/FeedCard.tsx, packages/web/lib/components/profile/ProfileHeader.tsx, packages/web/lib/design-system/index.ts</files>
  <action>
Refactor existing components to use Card base and update design-system exports:

1. **FeedCard.tsx refactor:**
   - Import Card from '@/lib/design-system' (NOT the feed-card.tsx, use base Card)
   - Replace article element styling with Card component
   - PRESERVE all existing functionality:
     - GSAP Flip animation setup
     - useTransitionStore integration
     - handleClick for FLIP state capture
     - FeedCardItem interface
     - FeedCardSkeleton export
   - Use Card variant="default" with interactive styling

2. **ProfileHeader.tsx refactor:**
   - Import Card from '@/lib/design-system'
   - Replace motion.section with Card component (keep motion wrapper for animation)
   - PRESERVE all existing functionality:
     - useProfileStore integration
     - useAuthStore integration
     - handleSettingsClick
     - handleLogout
     - Avatar image/initials logic
   - Add Card wrapper with appropriate padding

3. **Update design-system/index.ts:**
   Add new component exports:
   ```typescript
   // Feed Card (design-system base)
   export { FeedCard as FeedCardBase, FeedCardSkeleton as FeedCardBaseSkeleton } from './feed-card';
   export type { FeedCardProps as FeedCardBaseProps } from './feed-card';

   // Profile Header Card
   export { ProfileHeaderCard, ProfileHeaderCardSkeleton } from './profile-header-card';
   export type { ProfileHeaderCardProps } from './profile-header-card';
   ```
  </action>
  <verify>
Run TypeScript check and lint:
```bash
cd packages/web && npx tsc --noEmit && npx eslint lib/components/FeedCard.tsx lib/components/profile/ProfileHeader.tsx lib/design-system/feed-card.tsx lib/design-system/profile-header-card.tsx --max-warnings 0
```
  </verify>
  <done>Existing FeedCard and ProfileHeader use Card component while preserving all functionality. New design-system components exported correctly.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **TypeScript compilation:**
```bash
cd packages/web && npx tsc --noEmit
```
Expected: No errors

2. **Import test:**
```bash
cd packages/web && node -e "
const m = require('./lib/design-system');
['FeedCardBase', 'FeedCardBaseSkeleton', 'ProfileHeaderCard', 'ProfileHeaderCardSkeleton'].forEach(e => console.log(e + ':', typeof m[e]));
"
```
Expected: All exports show 'function'

3. **Lint check:**
```bash
cd packages/web && npx eslint lib/design-system/feed-card.tsx lib/design-system/profile-header-card.tsx lib/components/FeedCard.tsx lib/components/profile/ProfileHeader.tsx --max-warnings 0
```
Expected: No lint errors

4. **Functionality preservation check:**
```bash
cd packages/web && grep -l "useTransitionStore\|Flip" lib/components/FeedCard.tsx
cd packages/web && grep -l "useProfileStore\|useAuthStore" lib/components/profile/ProfileHeader.tsx
```
Expected: Both files found (existing integrations preserved)
</verification>

<success_criteria>
- FeedCard design-system component exists with image/overlay support
- ProfileHeaderCard design-system component exists with avatar/stats layout
- Existing FeedCard.tsx uses Card component and preserves GSAP Flip
- Existing ProfileHeader.tsx uses Card component and preserves store integrations
- All skeleton variants work correctly
- Components exported from design-system barrel
- TypeScript compiles without errors
- ESLint passes without warnings
</success_criteria>

<output>
After completion, create `.planning/phases/v2-03-card-components/v2-03-03-SUMMARY.md`
</output>
