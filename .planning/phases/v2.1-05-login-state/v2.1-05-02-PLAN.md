---
phase: v2.1-05-login-state
plan: 02
type: execute
wave: 2
depends_on: ["v2.1-05-01"]
files_modified:
  - packages/web/lib/design-system/login-card.tsx
  - packages/web/lib/design-system/loading-spinner.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "LoginCard displays logo, welcome text, OAuth buttons, divider, and guest button"
    - "LoginCard has glassmorphism effect with backdrop blur"
    - "LoadingSpinner shows pill-shaped loading indicator with text"
  artifacts:
    - path: "packages/web/lib/design-system/login-card.tsx"
      provides: "Complete login card wrapper using OAuthButton, GuestButton, Divider"
      exports: ["LoginCard", "LoginCardProps"]
    - path: "packages/web/lib/design-system/loading-spinner.tsx"
      provides: "Pill-shaped loading spinner with text"
      exports: ["LoadingSpinner", "LoadingSpinnerProps"]
  key_links:
    - from: "packages/web/lib/design-system/login-card.tsx"
      to: "oauth-button.tsx, guest-button.tsx, divider.tsx"
      via: "import"
      pattern: "import.*OAuthButton|GuestButton|Divider.*from"
    - from: "packages/web/lib/design-system/index.ts"
      to: "login-card.tsx, loading-spinner.tsx"
      via: "barrel export"
      pattern: "export.*LoginCard|LoadingSpinner"
---

<objective>
Create LoginCard wrapper component and LoadingSpinner for async states

Purpose: Provide a complete login experience with glassmorphism styling, integrating OAuth buttons, guest option, and loading states matching decoded.pen design
Output: LoginCard component composing Plan 01 components, plus LoadingSpinner for loading states
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference existing implementation
@packages/web/lib/components/auth/LoginCard.tsx
@packages/web/lib/design-system/oauth-button.tsx
@packages/web/lib/design-system/guest-button.tsx
@packages/web/lib/design-system/divider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoadingSpinner component</name>
  <files>packages/web/lib/design-system/loading-spinner.tsx</files>
  <action>
Create LoadingSpinner component following decoded.pen specs:
- fill #1F1F1FCC (dark with opacity), cornerRadius 9999 (pill shape)
- padding [8, 16], gap 8
- Lucide loader icon 16px with animate-spin
- text "Loading more..." Inter 14px medium

Implementation:
1. Props: text (default: "Loading..."), className
2. Structure: inline-flex items-center gap-2 px-4 py-2 rounded-full bg-neutral-900/80
3. Icon: Loader2 from lucide-react, h-4 w-4 animate-spin
4. Text: text-sm font-medium text-white

Use forwardRef pattern. Simple component, no CVA needed (single variant).

Example usage:
```tsx
<LoadingSpinner />
<LoadingSpinner text="Loading more..." />
```
  </action>
  <verify>
TypeScript compiles: `cd packages/web && npx tsc --noEmit`
File exists with correct exports
  </verify>
  <done>
LoadingSpinner component with pill shape and animated loader icon
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LoginCard component</name>
  <files>packages/web/lib/design-system/login-card.tsx</files>
  <action>
Create LoginCard component migrating from components/auth/LoginCard.tsx to design-system.

decoded.pen specs:
- fill #FFFFFF0D (white 5% opacity), cornerRadius 16
- background_blur radius 24
- padding [32, 24, 16, 24] (top, right, bottom, left)
- Logo "decoded" in Playfair Display 30px bold #d9fc69

Implementation:
1. Props interface:
   - onOAuthLogin: (provider: "kakao" | "google" | "apple") => void
   - onGuestLogin: () => void
   - loadingProvider?: "kakao" | "google" | "apple" | null
   - isLoading?: boolean
   - error?: string | null
   - className?: string

2. Structure:
   - Container: w-full max-w-sm rounded-2xl bg-white/5 backdrop-blur-xl p-6 pt-8 pb-4
   - Logo section: "decoded" text-3xl font-bold text-[#d9fc69] (use Playfair Display font-family if available, else serif)
   - Welcome text: "Welcome to Decoded" text-lg font-medium text-white/90
   - Subtitle: "Discover what they're wearing" text-sm text-white/60
   - Error display: red bg with error message (if error prop)
   - OAuth buttons: OAuthButton for each provider with loadingProvider state
   - Divider component
   - GuestButton component
   - Footer: Terms and Privacy links

3. Import OAuthButton, GuestButton, Divider from "./oauth-button", "./guest-button", "./divider"
4. Use "use client" directive (interactive component)

Reference existing: packages/web/lib/components/auth/LoginCard.tsx for structure
  </action>
  <verify>
TypeScript compiles: `cd packages/web && npx tsc --noEmit`
File exists with correct exports
  </verify>
  <done>
LoginCard component with glassmorphism effect, composing OAuthButton, GuestButton, and Divider
  </done>
</task>

<task type="auto">
  <name>Task 3: Export LoginCard and LoadingSpinner from barrel</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
Add exports to design-system/index.ts after the Divider exports:

```typescript
// LoginCard Component
export { LoginCard } from "./login-card";
export type { LoginCardProps } from "./login-card";

// LoadingSpinner Component
export { LoadingSpinner } from "./loading-spinner";
export type { LoadingSpinnerProps } from "./loading-spinner";
```
  </action>
  <verify>
TypeScript check: `cd packages/web && npx tsc --noEmit`
  </verify>
  <done>
LoginCard and LoadingSpinner exported from @/lib/design-system barrel
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. LoginCard renders with glassmorphism effect (bg-white/5 backdrop-blur-xl)
3. LoginCard composes OAuthButton, Divider, GuestButton correctly
4. LoadingSpinner shows pill-shaped indicator with spinning icon
5. Both components importable from "@/lib/design-system"
</verification>

<success_criteria>
- LoginCard displays decoded logo in primary color (#d9fc69)
- LoginCard shows OAuth buttons, divider, and guest button
- LoginCard has glassmorphism effect (transparent with blur)
- LoadingSpinner shows pill with loader icon and text
- All components importable from "@/lib/design-system"
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-05-login-state/v2.1-05-02-SUMMARY.md`
</output>
