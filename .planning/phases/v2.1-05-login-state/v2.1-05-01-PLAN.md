---
phase: v2.1-05-login-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/oauth-button.tsx
  - packages/web/lib/design-system/guest-button.tsx
  - packages/web/lib/design-system/divider.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "OAuthButton renders with correct provider branding (Kakao yellow, Google white, Apple black)"
    - "GuestButton renders with transparent background and white border"
    - "Divider displays 'or' text centered between two horizontal lines"
  artifacts:
    - path: "packages/web/lib/design-system/oauth-button.tsx"
      provides: "OAuth button with provider variants using CVA"
      exports: ["OAuthButton", "oauthButtonVariants", "OAuthButtonProps"]
    - path: "packages/web/lib/design-system/guest-button.tsx"
      provides: "Guest continue button with decoded.pen styling"
      exports: ["GuestButton", "GuestButtonProps"]
    - path: "packages/web/lib/design-system/divider.tsx"
      provides: "Or divider with lines and centered text"
      exports: ["Divider", "DividerProps"]
  key_links:
    - from: "packages/web/lib/design-system/index.ts"
      to: "oauth-button.tsx, guest-button.tsx, divider.tsx"
      via: "barrel export"
      pattern: "export.*OAuthButton|GuestButton|Divider"
---

<objective>
Create OAuthButton, GuestButton, and Divider components for the login flow

Purpose: Provide branded OAuth sign-in buttons matching decoded.pen specs for Kakao, Google, and Apple, plus guest continuation option with styled divider
Output: Three new design-system components with CVA variants
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference existing implementations
@packages/web/lib/components/auth/OAuthButton.tsx
@packages/web/lib/design-system/action-button.tsx
@packages/web/lib/design-system/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OAuthButton component with CVA variants</name>
  <files>packages/web/lib/design-system/oauth-button.tsx</files>
  <action>
Create OAuthButton component migrating from components/auth/OAuthButton.tsx to design-system with CVA pattern.

decoded.pen specs:
- Kakao: fill #FEE500, text #191919, height 52px, cornerRadius 12px
- Google: fill #FFFFFF, text #374151, height 52px, cornerRadius 12px
- Apple: fill #000000, text #FFFFFF, height 52px, cornerRadius 12px

Implementation:
1. Create oauthButtonVariants with CVA
   - variant: "kakao" | "google" | "apple"
   - Base: h-[52px] rounded-xl w-full max-w-[320px] flex items-center justify-center gap-3 font-medium transition-colors
   - kakao: bg-[#FEE500] text-[#191919] hover:bg-[#FDD800]
   - google: bg-white text-gray-700 border border-gray-300 hover:bg-gray-50
   - apple: bg-black text-white hover:bg-gray-900

2. Props: provider (required), onClick, isLoading, disabled, className
3. Include SVG icons inline for each provider (copy from existing component)
4. Show Loader2 spinner when isLoading
5. Display "Continue with {Provider}" label

Reference existing: packages/web/lib/components/auth/OAuthButton.tsx for icons and structure
Follow pattern: packages/web/lib/design-system/action-button.tsx for CVA setup
  </action>
  <verify>
TypeScript compiles: `cd packages/web && npx tsc --noEmit`
File exists with correct exports
  </verify>
  <done>
OAuthButton component with 3 provider variants (kakao, google, apple) matching decoded.pen specs
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GuestButton and Divider components</name>
  <files>
packages/web/lib/design-system/guest-button.tsx
packages/web/lib/design-system/divider.tsx
  </files>
  <action>
Create GuestButton component:
- decoded.pen specs: transparent bg, stroke border 1px #FFFFFF33, text #FFFFFFB3, height 52px, cornerRadius 12px
- Props: onClick, disabled, isLoading, className, children (default: "Continue as Guest")
- Tailwind: h-[52px] w-full max-w-[320px] rounded-xl bg-transparent border border-white/20 text-white/70 hover:bg-white/10 hover:text-white
- Show Loader2 spinner when isLoading

Create Divider component:
- decoded.pen specs: width fill, two lines #FFFFFF33, text "or" #FFFFFF66 fontSize 12
- Props: text (default: "or"), className
- Structure: flex with two hr lines and centered span
- Tailwind: relative flex items-center w-full
  - Lines: flex-1 border-t border-white/20
  - Text: px-3 text-xs text-white/40

Both use forwardRef pattern consistent with design-system components.
  </action>
  <verify>
TypeScript compiles: `cd packages/web && npx tsc --noEmit`
Both files exist with correct exports
  </verify>
  <done>
GuestButton with transparent bordered styling and Divider with "or" text matching decoded.pen
  </done>
</task>

<task type="auto">
  <name>Task 3: Export new components from design-system barrel</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
Add exports to design-system/index.ts:

After the last export block (SpotDetail), add:

```typescript
// OAuthButton Component
export { OAuthButton, oauthButtonVariants } from "./oauth-button";
export type { OAuthButtonProps } from "./oauth-button";

// GuestButton Component
export { GuestButton } from "./guest-button";
export type { GuestButtonProps } from "./guest-button";

// Divider Component
export { Divider } from "./divider";
export type { DividerProps } from "./divider";
```

Maintain alphabetical grouping style consistent with existing exports.
  </action>
  <verify>
Import check: `cd packages/web && node -e "require('./lib/design-system')" 2>&1 || echo "ESM module"`
TypeScript check: `cd packages/web && npx tsc --noEmit`
  </verify>
  <done>
OAuthButton, GuestButton, and Divider exported from @/lib/design-system barrel
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. All three components export correctly from design-system
3. OAuthButton has kakao/google/apple variants with correct colors
4. GuestButton has transparent bg with white/20 border
5. Divider shows "or" text centered between lines
</verification>

<success_criteria>
- OAuthButton renders Kakao (yellow), Google (white), Apple (black) variants
- GuestButton renders transparent with white border
- Divider renders "or" between two horizontal lines
- All components importable from "@/lib/design-system"
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-05-login-state/v2.1-05-01-SUMMARY.md`
</output>
