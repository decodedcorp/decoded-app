---
phase: v2.1-05-login-state
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/skeleton-card.tsx
  - packages/web/lib/design-system/bottom-sheet.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "SkeletonCard displays animated placeholder matching decoded.pen specs"
    - "BottomSheet has dark background (#242424) with proper handle styling"
    - "BottomSheet handle is 40x4px with #3D3D3D fill and rounded corners"
  artifacts:
    - path: "packages/web/lib/design-system/skeleton-card.tsx"
      provides: "Skeleton card with configurable layout for loading states"
      exports: ["SkeletonCard", "SkeletonCardProps"]
    - path: "packages/web/lib/design-system/bottom-sheet.tsx"
      provides: "Bottom sheet with decoded.pen styling and snap points"
      exports: ["BottomSheet", "BottomSheetProps"]
  key_links:
    - from: "packages/web/lib/design-system/index.ts"
      to: "skeleton-card.tsx, bottom-sheet.tsx"
      via: "barrel export"
      pattern: "export.*SkeletonCard|BottomSheet"
---

<objective>
Create SkeletonCard loading component and upgrade BottomSheet to decoded.pen specs

Purpose: Provide consistent loading states and bottom sheet interactions matching decoded.pen design system
Output: SkeletonCard for content loading, upgraded BottomSheet with proper styling
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference existing implementation
@packages/web/lib/components/ui/BottomSheet.tsx
@packages/web/lib/design-system/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkeletonCard component</name>
  <files>packages/web/lib/design-system/skeleton-card.tsx</files>
  <action>
Create SkeletonCard component following decoded.pen specs:
- width 180, height 225, fill $--card (#1F1F1F), cornerRadius 12
- stroke border 1px
- imageSkeleton fill #3D3D3D
- badgeSkeleton 80x20 #52525280 rounded

Implementation with CVA:
1. skeletonCardVariants with size variants:
   - sm: w-[140px] h-[175px]
   - md: w-[180px] h-[225px] (default)
   - lg: w-[220px] h-[275px]

2. Props interface:
   - size?: "sm" | "md" | "lg"
   - showImage?: boolean (default: true)
   - showBadge?: boolean (default: true)
   - showTitle?: boolean (default: true)
   - showSubtitle?: boolean (default: true)
   - className?: string

3. Structure:
   - Container: rounded-xl bg-card border border-border overflow-hidden
   - Image placeholder: aspect-[4/5] bg-neutral-700 animate-pulse
   - Content area: p-3 space-y-2
   - Badge placeholder: h-5 w-20 rounded bg-neutral-600/50 animate-pulse
   - Title placeholder: h-4 w-full rounded bg-neutral-700 animate-pulse
   - Subtitle placeholder: h-3 w-3/4 rounded bg-neutral-700 animate-pulse

4. Use forwardRef pattern
5. Include displayName

NOTE: This is a NEW file, not modifying existing CardSkeleton in card.tsx. SkeletonCard follows decoded.pen specs specifically for the card grid loading state.
  </action>
  <verify>
TypeScript compiles: `cd packages/web && npx tsc --noEmit`
File exists with correct exports
  </verify>
  <done>
SkeletonCard component with size variants and configurable placeholder elements
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BottomSheet design-system component</name>
  <files>packages/web/lib/design-system/bottom-sheet.tsx</files>
  <action>
Create BottomSheet component in design-system, migrating from components/ui/BottomSheet.tsx with decoded.pen styling upgrades.

decoded.pen specs:
- fill #242424, cornerRadius [20, 20, 0, 0]
- handle: width 40, height 4, fill #3D3D3D, cornerRadius 2
- title: Inter 18px semibold white
- padding 20, gap 16

Implementation:
1. Props interface:
   - isOpen: boolean
   - onClose?: () => void
   - snapPoints?: number[] (default: [0.3, 0.6, 0.9])
   - defaultSnapPoint?: number (default: 0.3)
   - children: ReactNode
   - header?: ReactNode
   - title?: string
   - onSnapChange?: (snapPoint: number) => void
   - className?: string

2. Visual upgrades from existing:
   - Container: bg-[#242424] rounded-t-[20px] (was bg-background rounded-t-2xl)
   - Handle: w-10 h-1 bg-[#3D3D3D] rounded-sm (was bg-muted-foreground/30 rounded-full)
   - Title: text-lg font-semibold text-white (when title prop provided)
   - Padding: p-5 gap-4 (was px-4, py-3)

3. Keep existing drag functionality:
   - Touch and mouse drag handlers
   - Snap point finding and animation
   - isDragging state for smooth transitions

4. Add overlay backdrop when open:
   - Fixed overlay behind sheet
   - bg-black/50 backdrop
   - onClick triggers onClose

5. Use "use client" directive
6. Use forwardRef pattern

Reference: packages/web/lib/components/ui/BottomSheet.tsx for drag logic
  </action>
  <verify>
TypeScript compiles: `cd packages/web && npx tsc --noEmit`
File exists with correct exports
  </verify>
  <done>
BottomSheet component with decoded.pen styling (#242424 bg, proper handle, overlay)
  </done>
</task>

<task type="auto">
  <name>Task 3: Export SkeletonCard and BottomSheet from barrel</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
Add exports to design-system/index.ts after LoadingSpinner exports:

```typescript
// SkeletonCard Component
export { SkeletonCard, skeletonCardVariants } from "./skeleton-card";
export type { SkeletonCardProps } from "./skeleton-card";

// BottomSheet Component
export { BottomSheet } from "./bottom-sheet";
export type { BottomSheetProps } from "./bottom-sheet";
```
  </action>
  <verify>
TypeScript check: `cd packages/web && npx tsc --noEmit`
  </verify>
  <done>
SkeletonCard and BottomSheet exported from @/lib/design-system barrel
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. SkeletonCard renders with #3D3D3D image placeholder and animated pulse
3. BottomSheet has #242424 background and #3D3D3D handle
4. BottomSheet handle is 40x4px with rounded corners
5. Both components importable from "@/lib/design-system"
</verification>

<success_criteria>
- SkeletonCard shows image, badge, title, subtitle placeholders with pulse animation
- SkeletonCard has sm/md/lg size variants
- BottomSheet has dark background (#242424) matching decoded.pen
- BottomSheet handle is 40x4px #3D3D3D with rounded corners
- BottomSheet includes backdrop overlay
- All components importable from "@/lib/design-system"
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-05-login-state/v2.1-05-03-SUMMARY.md`
</output>
