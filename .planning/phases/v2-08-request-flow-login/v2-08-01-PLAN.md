---
phase: v2-08-request-flow-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/stores/requestStore.ts
  - packages/web/lib/components/request/StepIndicator.tsx
  - packages/web/lib/components/request/RequestFlowHeader.tsx
  - packages/web/lib/components/request/DropZone.tsx
  - packages/web/lib/components/request/RequestModal.tsx
  - packages/web/lib/components/request/DetailsStep.tsx
  - packages/web/lib/components/request/SubmitStep.tsx
autonomous: true

must_haves:
  truths:
    - "User sees 3-step indicator (Upload, Detect, Details) without connecting lines"
    - "Request flow header displays at 56px height with centered StepIndicator"
    - "DropZone displays with decoded.pen dashed border style and design tokens"
    - "Submit button appears in Details step footer (not separate step)"
  artifacts:
    - path: "packages/web/lib/stores/requestStore.ts"
      provides: "3-step flow state management"
      contains: "RequestStep = 1 | 2 | 3"
    - path: "packages/web/lib/components/request/StepIndicator.tsx"
      provides: "3-dot step indicator without connecting lines"
      contains: "gap-2"
    - path: "packages/web/lib/components/request/RequestFlowHeader.tsx"
      provides: "56px height header with decoded.pen styling"
      contains: "h-14"
    - path: "packages/web/lib/components/request/DropZone.tsx"
      provides: "decoded.pen styled upload area"
      contains: "border-dashed"
  key_links:
    - from: "packages/web/lib/components/request/RequestModal.tsx"
      to: "packages/web/lib/stores/requestStore.ts"
      via: "useRequestStore with 3-step flow"
      pattern: "currentStep.*[123]"
    - from: "packages/web/lib/components/request/DetailsStep.tsx"
      to: "submit button"
      via: "integrated submit in Details"
      pattern: "Submit|submit"
---

<objective>
Apply decoded.pen design system to Request flow components and consolidate 4-step flow into 3 steps (merging Submit into Details).

Purpose: Align Request flow UI with decoded.pen design specifications while simplifying user experience by reducing steps.
Output: Updated StepIndicator (3 dots), RequestFlowHeader (56px), DropZone (decoded.pen style), and integrated Submit in DetailsStep.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/phases/v2-08-request-flow-login/v2-08-CONTEXT.md
@.planning/phases/v2-08-request-flow-login/v2-08-RESEARCH.md
@packages/web/lib/stores/requestStore.ts
@packages/web/lib/components/request/StepIndicator.tsx
@packages/web/lib/components/request/RequestFlowHeader.tsx
@packages/web/lib/components/request/DropZone.tsx
@packages/web/lib/components/request/RequestModal.tsx
@packages/web/lib/components/request/DetailsStep.tsx
@packages/web/lib/components/request/SubmitStep.tsx
@packages/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RequestStore and StepIndicator for 3-step flow</name>
  <files>
    packages/web/lib/stores/requestStore.ts
    packages/web/lib/components/request/StepIndicator.tsx
  </files>
  <action>
Update requestStore.ts:
- Change `RequestStep = 1 | 2 | 3 | 4` to `RequestStep = 1 | 2 | 3`
- Update `canProceedToNextStep` case 3 to return true (Submit merged into Details)
- Remove case 4 from switch statement
- Update any type casts to use `1 | 2 | 3` instead of `1 | 2 | 3 | 4`

Update StepIndicator.tsx:
- Change STEPS array to 3 items: `[{ step: 1, label: "Upload" }, { step: 2, label: "Detect" }, { step: 3, label: "Details" }]`
- Remove connecting lines (remove the conditional div with `h-0.5 w-4 mx-1`)
- Update dot styling per decoded.pen spec:
  - Fixed 8px size (h-2 w-2), no scale effect on active
  - Active/completed: `bg-primary` (#D9FC69)
  - Incomplete: `bg-[#3D3D3D]`
- Use `gap-2` (8px) between dots
- Update StepLabel to show "Step X of 3"
  </action>
  <verify>
- Run `yarn lint` - no errors
- Run `yarn build` - no type errors related to RequestStep
- Grep for "step: 4" in request components - should return no results
  </verify>
  <done>
- RequestStep type is `1 | 2 | 3` (no step 4)
- StepIndicator shows 3 dots without connecting lines
- Dots use Primary color for active/completed, #3D3D3D for incomplete
  </done>
</task>

<task type="auto">
  <name>Task 2: Update RequestFlowHeader and DropZone with decoded.pen styling</name>
  <files>
    packages/web/lib/components/request/RequestFlowHeader.tsx
    packages/web/lib/components/request/DropZone.tsx
  </files>
  <action>
Update RequestFlowHeader.tsx:
- Set header height to 56px (h-14)
- Layout: back button (arrow-left) | StepIndicator (centered) | close button (x)
- Remove title text (StepIndicator is centered by itself)
- Use design system colors: `bg-background` for header, `border-border` for bottom border
- Icon buttons: 40px tap target (p-2), Lucide icons h-5 w-5
- Apply `sticky top-0 z-10` for scroll behavior

Update DropZone.tsx:
- Use design system tokens for styling:
  - Border: `border-2 border-dashed border-border` (default), `border-primary` (dragging)
  - Background: `bg-background` (default), `bg-primary/5` (dragging)
  - Border radius: `rounded-xl` (12px)
  - Padding: `p-8` for content area
- Icon: Upload icon from Lucide (not Camera), centered above text
- Typography:
  - Main text: `text-base font-medium text-foreground`
  - Subtitle: `text-sm text-muted-foreground`
  - Hints: `text-xs text-muted-foreground`
- Remove keyboard shortcut hint (Ctrl+V) for cleaner UI
- Keep compact mode for add-more button
  </action>
  <verify>
- Visual inspection: Header is 56px tall with centered StepIndicator
- Visual inspection: DropZone has dashed border with proper spacing
- Run `yarn lint` - no errors
  </verify>
  <done>
- RequestFlowHeader is 56px height with back | indicator | close layout
- DropZone uses decoded.pen dashed border style with design tokens
- No title text in header (only StepIndicator)
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Submit functionality into DetailsStep and update RequestModal</name>
  <files>
    packages/web/lib/components/request/DetailsStep.tsx
    packages/web/lib/components/request/RequestModal.tsx
  </files>
  <action>
Update DetailsStep.tsx:
- Import submit-related state and actions from requestStore (isSubmitting, submitError, setSubmitting, setSubmitError)
- Import submit logic from SubmitStep.tsx (the handleSubmit function)
- Add Submit button at the bottom of the form with:
  - Full width button using design system Button component or manual styling
  - Primary variant: `bg-primary text-primary-foreground`
  - Loading state with Loader2 spinner
  - Disabled when: isSubmitting OR (!mediaSource?.type || !mediaSource?.title)
  - Text: "Submit Request" (normal), "Submitting..." (loading)
- Add error display above submit button if submitError exists
- Use onClose prop to close modal after successful submission

Update RequestModal.tsx:
- Remove Step 4 rendering (`{currentStep === 4 && <SubmitStep onClose={handleClose} />}`)
- Update STEP_TITLES to remove entry for step 4
- Update handleNext to only handle steps 1 and 2 (step 3 has its own submit)
- Remove footer "Next" button for step 3 (Submit is now in DetailsStep)
- Pass onClose prop to DetailsStep: `<DetailsStep onClose={handleClose} />`
- Update type casts from `1 | 2 | 3 | 4` to `1 | 2 | 3`
  </action>
  <verify>
- Run `yarn lint` - no errors
- Run `yarn build` - no type errors
- Check RequestModal: No reference to step 4 or SubmitStep
- Check DetailsStep: Contains Submit button and submit logic
  </verify>
  <done>
- DetailsStep includes Submit button at bottom
- RequestModal no longer renders step 4
- Submit functionality works from Details step
- Modal closes on successful submission
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `yarn lint` passes
2. `yarn build` completes without errors
3. Manual test in browser:
   - Open request modal
   - See 3 dots in header (no connecting lines)
   - Upload image (DropZone has decoded.pen styling)
   - Proceed to Detection step
   - Proceed to Details step
   - Fill required fields and click Submit
   - Modal closes after successful submission
</verification>

<success_criteria>
- StepIndicator displays 3 dots with 8px gap, no connecting lines
- Active/completed dots use Primary color, incomplete use #3D3D3D
- RequestFlowHeader is 56px height with centered StepIndicator
- DropZone uses decoded.pen dashed border style
- Submit button appears in Details step (no separate Submit step)
- All lint and type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/v2-08-request-flow-login/v2-08-01-SUMMARY.md`
</output>
