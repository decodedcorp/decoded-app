---
phase: 09-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/README.md
  - specs/_shared/injection-guide.md
  - specs/_shared/components/CMN-01-header.md
  - specs/_shared/components/CMN-02-footer.md
  - specs/_shared/components/CMN-03-mobile-nav.md
  - specs/_shared/components/CMN-03-modals.md
  - specs/_shared/components/CMN-04-toasts.md
  - specs/_shared/templates/screen-spec-template.md
  - specs/_shared/templates/flow-spec-template.md
autonomous: true

must_haves:
  truths:
    - "specs/ root contains only _archive/, _shared/, _next/, admin/, flows/, screens/, README.md"
    - "No old bundle directories exist in specs/ (creation-ai, detail-view, discovery, mobile-platform, scroll-animation, shared, system-backend, user-system)"
    - "specs/README.md references only the v4.0 structure with no stale old-bundle paths"
    - "specs/_shared/injection-guide.md references correct paths for CMN specs and templates"
    - "AI agent loading specs/ will not encounter stale monolith spec.md files"
  artifacts:
    - path: "specs/_shared/components/"
      provides: "CMN-01~04 component specs relocated from old shared/"
    - path: "specs/_shared/templates/"
      provides: "Screen and flow spec templates relocated from old shared/"
    - path: "specs/README.md"
      provides: "Updated index reflecting final v4.0 directory structure"
  key_links:
    - from: "specs/_shared/injection-guide.md"
      to: "specs/_shared/components/CMN-*.md"
      via: "path references in injection table"
      pattern: "specs/_shared/components/CMN-"
    - from: "specs/README.md"
      to: "specs/_shared/templates/"
      via: "template links"
      pattern: "_shared/templates/"
---

<objective>
Retire 8 old bundle spec directories from `specs/`, relocate unique v4.0 content from `specs/shared/` to `specs/_shared/`, update all stale references in spec files, and verify the final directory structure matches the v4.0 target layout.

Purpose: Eliminate stale monolith spec.md files so AI agents only load the new v4.0 structure — no risk of injecting outdated 60-180KB bundle files.
Output: Clean `specs/` directory with only `_archive/`, `_shared/`, `_next/`, `admin/`, `flows/`, `screens/`, `README.md`.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cleanup/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate unique shared/ content to _shared/ and delete old bundle directories</name>
  <files>
    specs/_shared/components/CMN-01-header.md
    specs/_shared/components/CMN-02-footer.md
    specs/_shared/components/CMN-03-mobile-nav.md
    specs/_shared/components/CMN-03-modals.md
    specs/_shared/components/CMN-04-toasts.md
    specs/_shared/templates/screen-spec-template.md
    specs/_shared/templates/flow-spec-template.md
  </files>
  <action>
    **Step 1: Relocate v4.0 content from `specs/shared/` to `specs/_shared/`**

    The `specs/shared/` directory contains unique v4.0 content that must be preserved:
    - `shared/components/CMN-01-header.md` (3.5KB, v4.0 rewrite — different from archive's 19KB version)
    - `shared/components/CMN-02-footer.md` (2.6KB, v4.0 rewrite)
    - `shared/components/CMN-03-mobile-nav.md` (2.8KB, v4.0 rewrite)
    - `shared/components/CMN-03-modals.md` (30KB, old format — archive has identical copy, just move)
    - `shared/components/CMN-04-toasts.md` (20KB, old format — archive has identical copy, just move)
    - `shared/templates/screen-spec-template.md` (NOT in archive — created in v4-01)
    - `shared/templates/flow-spec-template.md` (NOT in archive — created in v4-01)

    Other files in `shared/` (api-contracts.md, data-models.md, ui-id-convention.md, workflows.md, README.md) are already archived in `_archive/v2.1.0/shared/` and superseded by `_shared/` equivalents — safe to delete with the directory.

    ```bash
    mkdir -p specs/_shared/components specs/_shared/templates
    cp specs/shared/components/CMN-*.md specs/_shared/components/
    cp specs/shared/templates/*.md specs/_shared/templates/
    ```

    **Step 2: Verify archive completeness for all 8 directories**

    Verify that `specs/_archive/v2.1.0/` contains all 8 bundle directories before deletion:
    ```bash
    for dir in creation-ai detail-view discovery mobile-platform scroll-animation shared system-backend user-system; do
      ls specs/_archive/v2.1.0/$dir/ > /dev/null 2>&1 && echo "ARCHIVED: $dir" || echo "MISSING: $dir"
    done
    ```
    All 8 should show ARCHIVED. If any shows MISSING, archive it first before deleting.

    **Step 3: Delete old bundle directories**

    ```bash
    rm -rf specs/creation-ai specs/detail-view specs/discovery specs/mobile-platform specs/scroll-animation specs/shared specs/system-backend specs/user-system
    ```

    Note: `specs/discovery/DECODED_PEN_HOME_UPDATE.md` (2.3KB, dated Feb 19) is a working file — check if it contains unique planning content. If it's just a transient note, delete with the directory. If it has significant content, archive to `_archive/v2.1.0/discovery/` first.

    **Step 4: Clean up dotfiles**

    ```bash
    find specs/ -name '.DS_Store' -delete
    find specs/ -name '._*' -delete
    ```
  </action>
  <verify>
    ```bash
    # Verify only target directories remain
    ls -d specs/*/ | sort
    # Expected: specs/_archive/ specs/_next/ specs/_shared/ specs/admin/ specs/flows/ specs/screens/

    # Verify CMN files relocated
    ls specs/_shared/components/CMN-*.md
    # Expected: CMN-01-header.md, CMN-02-footer.md, CMN-03-mobile-nav.md, CMN-03-modals.md, CMN-04-toasts.md

    # Verify templates relocated
    ls specs/_shared/templates/
    # Expected: screen-spec-template.md, flow-spec-template.md

    # Verify no old bundle dirs remain
    for dir in creation-ai detail-view discovery mobile-platform scroll-animation shared system-backend user-system; do
      test -d "specs/$dir" && echo "STILL EXISTS: $dir" || echo "REMOVED: $dir"
    done
    ```
  </verify>
  <done>All 8 old bundle directories deleted. CMN-01~04 component specs and screen/flow templates relocated to `specs/_shared/`. Only `_archive/`, `_shared/`, `_next/`, `admin/`, `flows/`, `screens/` directories remain in specs/.</done>
</task>

<task type="auto">
  <name>Task 2: Update stale references in specs/README.md and specs/_shared/injection-guide.md</name>
  <files>
    specs/README.md
    specs/_shared/injection-guide.md
  </files>
  <action>
    **specs/README.md updates:**

    1. Update Directory Structure section to reflect final state:
       - Remove `shared/` entry and its sub-entries (lines 33-34)
       - Add `admin/` entry: `├── admin/               # v3.0 admin specs`
       - Remove the "Note: `_shared/`, `flows/`, and `screens/` will be populated" line (they're populated now)
       - Update `_shared/` tree to include `components/` and `templates/` subdirectories

    2. Update AI Agent Injection Guide table:
       - Line 76: Change `shared/templates/screen-spec-template.md` → `_shared/templates/screen-spec-template.md`

    3. Update Templates section:
       - Line 82: Change `./shared/templates/screen-spec-template.md` → `./_shared/templates/screen-spec-template.md`
       - Line 83: Change `./shared/templates/flow-spec-template.md` → `./_shared/templates/flow-spec-template.md`

    4. Remove the "screens/discovery" line from the directory tree if it shows as a separate old-bundle entry (line 30 shows `discovery/` under `screens/` which is actually the correct v4.0 path — verify this is the `screens/discovery/` subdir reference, not old `discovery/` bundle)

    **specs/_shared/injection-guide.md updates:**

    1. Update all `specs/shared/` references to `specs/_shared/`:
       - Line 96: `specs/shared/templates/screen-spec-template.md` → `specs/_shared/templates/screen-spec-template.md`
       - Line 129: `specs/shared/components/CMN-*.md` → `specs/_shared/components/CMN-*.md`
       - Lines 162-163: template paths → `specs/_shared/templates/`
       - Lines 164-166: CMN component paths → `specs/_shared/components/`

    2. Add `specs/admin/` entry to the injection guide's loadable spec bundles table — for admin feature work, load `specs/admin/` as a v3.0 spec bundle.
  </action>
  <verify>
    ```bash
    # No stale shared/ references in new spec files (excluding _shared/ and packages/shared/)
    grep -rn 'specs/shared/' specs/ --include='*.md' | grep -v '_archive/' | grep -v '_shared/'
    # Expected: no output

    # No old bundle directory references in README
    grep -n 'creation-ai\|detail-view\|mobile-platform\|scroll-animation\|system-backend\|user-system' specs/README.md
    # Expected: no output

    # Verify admin/ mentioned in injection-guide
    grep -n 'specs/admin/' specs/_shared/injection-guide.md
    # Expected: at least one line
    ```
  </verify>
  <done>All stale `specs/shared/` paths updated to `specs/_shared/` in README.md and injection-guide.md. Admin specs documented in injection guide. No old bundle references remain in active spec files.</done>
</task>

<task type="auto">
  <name>Task 3: Final structure verification and success criteria check</name>
  <files>specs/README.md</files>
  <action>
    Run a comprehensive verification of the final specs/ directory structure:

    1. List specs/ root — must contain ONLY: `_archive/`, `_shared/`, `_next/`, `admin/`, `flows/`, `screens/`, `README.md`
    2. Confirm no `.DS_Store` or other dotfiles in specs/ tree
    3. Grep all active spec files (excluding `_archive/`) for any remaining old-bundle references
    4. Verify `specs/README.md` directory tree matches actual filesystem
    5. Spot-check that `specs/_shared/components/CMN-01-header.md` is the v4.0 rewrite (~3.5KB, not the 19KB archive version)

    If any discrepancy found, fix it. This is the final gate before marking v4.0 complete.
  </action>
  <verify>
    ```bash
    # Final directory listing
    ls specs/
    # Expected exactly: README.md _archive _next _shared admin flows screens

    # No dotfiles
    find specs/ -name '.*' -not -path 'specs/_archive/*' | head -5
    # Expected: no output

    # No stale old-bundle references in active specs
    grep -rn 'creation-ai/\|detail-view/\|discovery/\|mobile-platform/\|scroll-animation/\|system-backend/\|user-system/' specs/ --include='*.md' | grep -v '_archive/' | grep -v 'screens/discovery/'
    # Expected: no output (screens/discovery/ is a valid v4.0 path, excluded)

    # CMN-01 is v4.0 size
    wc -c specs/_shared/components/CMN-01-header.md
    # Expected: ~3500 bytes (not ~19000)
    ```
  </verify>
  <done>Final specs/ structure verified: 7 entries at root (`_archive/`, `_shared/`, `_next/`, `admin/`, `flows/`, `screens/`, `README.md`), no stale references, no dotfiles, all CMN files are v4.0 rewrites.</done>
</task>

</tasks>

<verification>
1. `ls specs/` shows exactly: `README.md _archive _next _shared admin flows screens` (7 items)
2. `grep -rn 'specs/shared/' specs/ --include='*.md' | grep -v _archive` returns empty
3. `specs/_shared/components/` contains CMN-01 through CMN-04
4. `specs/_shared/templates/` contains screen-spec-template.md and flow-spec-template.md
5. `specs/README.md` directory tree matches actual filesystem
6. `specs/_shared/injection-guide.md` references `specs/_shared/` paths (not `specs/shared/`)
7. `specs/_shared/injection-guide.md` includes `specs/admin/` as loadable bundle
</verification>

<success_criteria>
- All 8 old bundle directories removed from specs/
- Unique v4.0 content (CMN rewrites + templates) preserved in _shared/
- All spec-file references updated to new paths
- Admin specs documented as exception in injection guide
- specs/ directory matches target structure from CONTEXT.md (with admin/ addition per user decision)
</success_criteria>

<output>
After completion, create `.planning/phases/09-cleanup/09-01-SUMMARY.md`
</output>
