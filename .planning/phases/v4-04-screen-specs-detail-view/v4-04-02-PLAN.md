---
phase: v4-04-screen-specs-detail-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/screens/detail/SCR-VIEW-03-item-solution.md
  - specs/screens/detail/SCR-VIEW-04-related-content.md
autonomous: true

must_haves:
  truths:
    - "SCR-VIEW-03 documents the item/solution detail UI and shopping link flow with verified file paths"
    - "SCR-VIEW-03 captures the affiliate URL conversion step via POST /api/v1/solutions/convert-affiliate"
    - "SCR-VIEW-04 documents related content UI ('More from this Look') with verified component paths"
    - "SCR-VIEW-04 has implementation status tags showing what recommendation logic exists vs what is planned"
    - "All file paths in both specs are verified against actual filesystem"
  artifacts:
    - path: "specs/screens/detail/SCR-VIEW-03-item-solution.md"
      provides: "Item/Solution detail spec with shopping connection flow"
      contains: "Shopping Connection"
    - path: "specs/screens/detail/SCR-VIEW-04-related-content.md"
      provides: "Related content spec with recommendation UI and data requirements"
      contains: "Related Content"
  key_links:
    - from: "SCR-VIEW-03"
      to: "SCR-VIEW-02"
      via: "cross-reference: solution panel opened from spot selection"
    - from: "SCR-VIEW-03"
      to: "specs/_shared/api-contracts.md"
      via: "affiliate conversion API reference"
    - from: "SCR-VIEW-04"
      to: "SCR-VIEW-01"
      via: "cross-reference: related content section within detail page"
---

<objective>
Write screen specs SCR-VIEW-03 (Item/Solution) and SCR-VIEW-04 (Related Content) for the Detail View bundle, with verified component paths and implementation status assessment.

Purpose: Complete the Detail View bundle documentation so AI agents have full coverage of the detail page — from page layout to interaction layer to shopping flow to related content.
Output: Two spec files in `specs/screens/detail/` with EARS requirements, wireframes, and implementation status tags.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v4-04-screen-specs-detail-view/v4-04-CONTEXT.md

# Templates and shared foundation
@specs/shared/templates/screen-spec-template.md
@specs/_shared/component-registry.md
@specs/_shared/store-map.md
@specs/_shared/data-models.md
@specs/_shared/api-contracts.md

# Flow context
@specs/flows/FLW-02-detail.md

# Stale path reference
@specs/_archive/v2.1.0/STALE-PATHS-AUDIT.md

# Key detail components to verify:
@packages/web/lib/components/detail/ItemDetailCard.tsx
@packages/web/lib/components/detail/ShopCarouselSection.tsx
@packages/web/lib/components/detail/ShopGrid.tsx
@packages/web/lib/components/detail/DecodedItemsSection.tsx
@packages/web/lib/components/detail/ItemVoting.tsx
@packages/web/lib/components/detail/RelatedImages.tsx
@packages/web/lib/components/detail/RelatedLooksSection.tsx
@packages/web/lib/components/detail/GallerySection.tsx
@packages/web/lib/components/detail/ArticleContent.tsx
@packages/web/lib/components/detail/SmartTagsBreadcrumb.tsx

# Design system components for these specs:
@packages/web/lib/design-system/spot-detail.tsx
@packages/web/lib/design-system/shop-carousel-card.tsx
@packages/web/lib/design-system/product-card.tsx
@packages/web/lib/design-system/grid-card.tsx

# Hooks:
@packages/web/lib/hooks/useSolutions.ts
@packages/web/lib/hooks/useItems.ts
@packages/web/lib/hooks/useNormalizedItems.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCR-VIEW-03-item-solution.md</name>
  <files>specs/screens/detail/SCR-VIEW-03-item-solution.md</files>
  <action>
Write `SCR-VIEW-03-item-solution.md` following screen-spec-template format:

**Structure:**
1. **Header** — ID: SCR-VIEW-03, Route: section within `/images/[id]` and `/posts/[id]`, Status: implemented
2. **Purpose** — User views detected items with solution details (brand, price, link) and clicks through to shop
3. **Component Map** — Table with verified paths:
   - ItemDetailCard (`packages/web/lib/components/detail/ItemDetailCard.tsx`) — verify
   - DecodedItemsSection (`packages/web/lib/components/detail/DecodedItemsSection.tsx`) — verify
   - ShopCarouselSection (`packages/web/lib/components/detail/ShopCarouselSection.tsx`) — verify
   - ShopGrid (`packages/web/lib/components/detail/ShopGrid.tsx`) — verify
   - ItemVoting (`packages/web/lib/components/detail/ItemVoting.tsx`) — verify
   - SpotDetail (DS: component-registry)
   - ShopCarouselCard (DS: component-registry)
   - ProductCard (DS: component-registry)
   - useSolutions, useItems, useNormalizedItems hooks — verify paths
   - Verify ALL paths with `ls`

4. **Layout — Mobile** — ASCII wireframe showing:
   - SpotDetail panel (BottomSheet) content layout:
     - Item image thumbnail + brand/name/price
     - Solution list (product cards with buy links)
     - ShopCarouselSection at bottom
   - DecodedItemsSection in page body:
     - Grid of detected items with categories

5. **Layout — Desktop** — Wireframe:
   - Side panel layout for SpotDetail
   - Wider shop grid (more columns)
   - Breakpoint table

6. **Requirements** — EARS format with status tags:
   - Data: "When spot is selected, the system shall fetch solutions via `GET /api/v1/spots/[spotId]/solutions`" `✅`
   - Data: "When solutions are loaded, the system shall display brand, name, price, and image for each solution" — check impl
   - Shopping: "When user taps a solution buy link, the system shall call `POST /api/v1/solutions/convert-affiliate` to get affiliate URL" — check impl
   - Shopping: "When affiliate URL is returned, the system shall open it in a new browser tab" — check impl
   - Voting: "When user taps vote button on an item, the system shall..." — check if ItemVoting is implemented or NOT-IMPL
   - Check which stale paths from STALE-PATHS-AUDIT map to features (BuyButton, VoteButton, VotingSection, VibeItemCard, OriginalItemCard, DualMatchSection, AddVibeModal) — mark as `⚠️ NOT-IMPL` if they don't exist

7. **Shopping Connection Flow** — Brief sequence:
   - Solution tap → affiliate conversion API → new tab with product URL
   - Reference: FLW-02 ShoppingExit state

8. **Navigation** — Within-page navigation (from SCR-VIEW-02 spot selection)
9. **Error & Empty States** — No solutions, API error, loading

**Line budget:** Target 200 lines. Max 250.

**Stale path handling:** STALE-PATHS-AUDIT lists many missing components for VIEW-03 (BuyButton, VoteButton, VotingSection, VibeItemCard, OriginalItemCard, DualMatchSection, AddVibeModal, VibeVoting). These are all `⚠️ NOT-IMPL`. Do NOT include them as current components. Instead, mention their intended functionality in requirements with `⚠️ NOT-IMPL` tag.

**Cross-references:**
- `See: SCR-VIEW-02 — Spot selection triggers solution loading`
- `See: SCR-VIEW-01 — DecodedItemsSection placement in page layout`
- `See: FLW-02 — Shopping exit flow (affiliate URL conversion)`
  </action>
  <verify>
1. `wc -l specs/screens/detail/SCR-VIEW-03-item-solution.md` — must be ≤250 lines
2. Grep for `packages/web/` paths; verify each with `ls`
3. File documents affiliate URL conversion flow
4. File marks stale/missing components as `⚠️ NOT-IMPL` (not as current)
5. EARS requirements present with status tags
  </verify>
  <done>
SCR-VIEW-03-item-solution.md exists with ≤250 lines, all current file paths verified, shopping connection flow documented, stale components marked as NOT-IMPL, and EARS requirements tagged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SCR-VIEW-04-related-content.md</name>
  <files>specs/screens/detail/SCR-VIEW-04-related-content.md</files>
  <action>
Write `SCR-VIEW-04-related-content.md` following screen-spec-template format:

**Structure:**
1. **Header** — ID: SCR-VIEW-04, Route: section within `/images/[id]` and `/posts/[id]`, Status: partially implemented
2. **Purpose** — User discovers related content ("More from this Look") via recommendation grid below main detail content
3. **Component Map** — Table with verified paths:
   - RelatedImages (`packages/web/lib/components/detail/RelatedImages.tsx`) — verify
   - RelatedLooksSection (`packages/web/lib/components/detail/RelatedLooksSection.tsx`) — verify
   - GallerySection (`packages/web/lib/components/detail/GallerySection.tsx`) — verify
   - SmartTagsBreadcrumb (`packages/web/lib/components/detail/SmartTagsBreadcrumb.tsx`) — verify
   - ArticleContent (`packages/web/lib/components/detail/ArticleContent.tsx`) — verify
   - GridCard (DS: component-registry)
   - SectionHeader (DS: component-registry)
   - Verify ALL paths with `ls`

4. **Layout — Mobile** — ASCII wireframe showing:
   - SectionHeader "More from this Look"
   - 2-column grid of related image cards
   - SmartTagsBreadcrumb (tag-based navigation)
   - ArticleContent section (if applicable)

5. **Layout — Desktop** — Wireframe:
   - 4-column grid
   - Wider content area
   - Breakpoint table: Grid 2→4col

6. **Requirements** — EARS format with status tags:
   - **CRITICAL: Research the actual implementation** — Read the component files (RelatedImages.tsx, RelatedLooksSection.tsx, GallerySection.tsx) to determine:
     - What data they fetch (API endpoint? Query key? Parameters?)
     - What recommendation logic exists (same artist? Same category? Random?)
     - Whether the recommendation is client-side filtering or server-side
   - Tag each requirement based on actual implementation found:
     - `✅` if the feature is implemented and working
     - `⚠️ NOT-IMPL` if the component exists but feature is stub/placeholder
     - `📋 PLANNED` if the component doesn't exist but was designed
   - Data Loading: "When detail page loads, the system shall fetch related content based on [actual logic found]"
   - User Interaction: "When user taps a related card, the system shall navigate to that content's detail page"
   - SmartTags: "When user taps a smart tag, the system shall..." — check what SmartTagsBreadcrumb does
   - Stale paths: CommentForm, CommentItem, CommentList, CommentSection from STALE-PATHS-AUDIT are all `⚠️ NOT-IMPL`

7. **Data Requirements** — Document what the recommendation needs:
   - Input: current post/image ID, category, artist
   - Output: related posts/images list
   - API endpoint (if exists) or data source

8. **Navigation** — Related card tap → new detail page (SCR-VIEW-01)
9. **Error & Empty States** — No related content, loading

**Line budget:** Target 180 lines. Max 200.

**CONTEXT decision note:** User was uncertain about Related Content recommendation logic implementation status. This task MUST research the actual code to determine what's implemented vs planned. Document findings honestly with status tags.

**Stale path handling:** STALE-PATHS-AUDIT lists CommentForm/CommentItem/CommentList/CommentSection and TagBreadcrumb as missing. Mark comment system as `⚠️ NOT-IMPL`. Check if SmartTagsBreadcrumb replaced TagBreadcrumb.

**Cross-references:**
- `See: SCR-VIEW-01 — Page layout containing this related content section`
- `See: FLW-01 — Discovery flow (related content creates re-entry into discovery)`
  </action>
  <verify>
1. `wc -l specs/screens/detail/SCR-VIEW-04-related-content.md` — must be ≤200 lines
2. Grep for `packages/web/` paths; verify each with `ls`
3. File documents actual recommendation logic found in code (not assumptions)
4. Implementation status tags present on all requirements
5. Comment system marked as `⚠️ NOT-IMPL`
  </verify>
  <done>
SCR-VIEW-04-related-content.md exists with ≤200 lines, all file paths verified, recommendation logic researched and documented with honest status tags, stale comment components marked NOT-IMPL.
  </done>
</task>

</tasks>

<verification>
1. Both files exist in `specs/screens/detail/`
2. Combined line count ≤450 (250 + 200)
3. Zero stale file paths — every `packages/web/` reference verified
4. Both specs cross-reference other VIEW specs and FLW-02
5. EARS requirements with implementation status tags in both
6. SCR-VIEW-03 documents affiliate URL conversion flow
7. SCR-VIEW-04 documents actual (not assumed) recommendation logic
</verification>

<success_criteria>
- SCR-VIEW-03 covers item/solution detail with shopping link flow and affiliate conversion
- SCR-VIEW-04 covers related content with researched recommendation logic
- All component file paths verified (zero stale paths)
- Stale/missing components from STALE-PATHS-AUDIT honestly tagged as NOT-IMPL
- An AI agent can use these specs to understand and modify the shopping and recommendation features
</success_criteria>

<output>
After completion, create `.planning/phases/v4-04-screen-specs-detail-view/v4-04-02-SUMMARY.md`
</output>
