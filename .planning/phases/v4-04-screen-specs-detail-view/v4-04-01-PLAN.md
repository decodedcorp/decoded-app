---
phase: v4-04-screen-specs-detail-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/screens/detail/SCR-VIEW-01-post-detail.md
  - specs/screens/detail/SCR-VIEW-02-spot-hotspot.md
autonomous: true

must_haves:
  truths:
    - "SCR-VIEW-01 documents both /images/[id] and /posts/[id] routes with verified component file paths"
    - "SCR-VIEW-01 has mobile + desktop ASCII wireframes showing component-level layout"
    - "SCR-VIEW-02 documents Hotspot interaction states with EARS requirements and implementation status tags"
    - "SCR-VIEW-02 captures transitionStore FLIP animation trigger and SpotDetail panel behavior"
    - "All file paths in both specs are verified against actual filesystem (no stale paths from STALE-PATHS-AUDIT)"
  artifacts:
    - path: "specs/screens/detail/SCR-VIEW-01-post-detail.md"
      provides: "Post/Image detail page spec with component map, wireframes, EARS requirements"
      contains: "Component Map"
    - path: "specs/screens/detail/SCR-VIEW-02-spot-hotspot.md"
      provides: "Spot/Hotspot interaction layer spec with state transitions and FLIP animation"
      contains: "Interaction States"
  key_links:
    - from: "SCR-VIEW-01"
      to: "SCR-VIEW-02"
      via: "cross-reference: Hotspot tap triggers SCR-VIEW-02 interaction layer"
    - from: "SCR-VIEW-02"
      to: "specs/_shared/store-map.md"
      via: "transitionStore state shape reference"
    - from: "SCR-VIEW-01"
      to: "specs/flows/FLW-02-detail.md"
      via: "flow context cross-reference"
---

<objective>
Write screen specs SCR-VIEW-01 (Post Detail) and SCR-VIEW-02 (Spot/Hotspot) for the Detail View bundle, with all component file paths verified against the actual codebase.

Purpose: Enable AI agents to modify detail view screens without file path errors, referencing correct components and understanding interaction contracts.
Output: Two spec files in `specs/screens/detail/` following v4.0 screen spec template with EARS requirements, mobile+desktop wireframes, and implementation status tags.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v4-04-screen-specs-detail-view/v4-04-CONTEXT.md

# Templates and shared foundation
@specs/shared/templates/screen-spec-template.md
@specs/_shared/component-registry.md
@specs/_shared/store-map.md
@specs/_shared/data-models.md
@specs/_shared/api-contracts.md

# Flow context (FLW-02 is the primary flow for detail view)
@specs/flows/FLW-02-detail.md

# Stale path reference (Detail View stale paths to verify/replace)
@specs/_archive/v2.1.0/STALE-PATHS-AUDIT.md

# Codebase files to verify against
# Route pages:
@packages/web/app/images/[id]/page.tsx
@packages/web/app/posts/[id]/page.tsx

# Key detail components (verify each path exists before including in spec):
@packages/web/lib/components/detail/ImageDetailPage.tsx
@packages/web/lib/components/detail/PostDetailPage.tsx
@packages/web/lib/components/detail/ImageDetailContent.tsx
@packages/web/lib/components/detail/PostDetailContent.tsx
@packages/web/lib/components/detail/HeroSection.tsx
@packages/web/lib/components/detail/ImageCanvas.tsx
@packages/web/lib/components/detail/InteractiveShowcase.tsx
@packages/web/lib/components/detail/MetadataTags.tsx
@packages/web/lib/components/detail/Lightbox.tsx

# Store and FLIP animation:
@packages/web/lib/stores/transitionStore.ts
@packages/web/lib/hooks/useFlipTransition.ts

# Design system components used in detail view:
@packages/web/lib/design-system/hotspot.tsx
@packages/web/lib/design-system/spot-card.tsx
@packages/web/lib/design-system/spot-detail.tsx
@packages/web/lib/design-system/bottom-sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCR-VIEW-01-post-detail.md</name>
  <files>specs/screens/detail/SCR-VIEW-01-post-detail.md</files>
  <action>
Create directory `specs/screens/detail/` if it doesn't exist.

Write `SCR-VIEW-01-post-detail.md` following the screen-spec-template format with these specifics:

**Structure:**
1. **Header** — ID: SCR-VIEW-01, Route: `/images/[id]` (primary) + `/posts/[id]` (delta), Status: implemented
2. **Purpose** — One sentence: user views full image/post with metadata, spots, and related content
3. **Component Map** — Table with Region | Component | File | Props/Notes columns
   - Verify EVERY file path with `ls` before including — no stale paths allowed
   - For design system components (Hotspot, SpotCard, SpotDetail, BottomSheet, etc.), reference as "DS: component-registry" instead of direct file paths
   - For non-DS components, use full verified path (e.g., `packages/web/lib/components/detail/HeroSection.tsx`)
   - List all actual files in `packages/web/lib/components/detail/` that are used by ImageDetailPage and PostDetailPage
   - Component tree depth: 2 levels (page → direct children → their DS components)

4. **Layout — Mobile (default)** — ASCII wireframe showing:
   - MobileHeader
   - HeroSection (full-width image with Lightbox tap)
   - Hotspot markers (cross-ref to SCR-VIEW-02)
   - MetadataTags (artist, group, context)
   - DecodedItemsSection (cross-ref to SCR-VIEW-03)
   - RelatedLooksSection / RelatedImages (cross-ref to SCR-VIEW-04)
   - Component-level detail: show buttons, text labels, icon positions

5. **Layout — Desktop (>=768px)** — ASCII wireframe showing desktop adaptation:
   - Centered content area
   - Side-by-side layout where applicable
   - Breakpoint table: `| Element | Mobile | Desktop |`

6. **Requirements** — EARS format with implementation status tags:
   - Data Loading: fetch spots, metadata on mount — tag each `✅` or `⚠️ NOT-IMPL`
   - User Interactions: image tap (Lightbox), hotspot tap (→ SCR-VIEW-02), scroll behavior
   - FLIP Animation: "When arriving from grid, the system shall play FLIP enter animation via useFlipEnter; When no originState, the system shall fade in (fallback)" — `✅`
   - State: transitionStore (FLIP), React Query for spots/solutions

7. **Navigation** — Table matching FLW-02 entry/exit points
8. **Error & Empty States** — Loading skeleton, API error, no spots
9. **Route Delta: `/posts/[id]`** — Document differences from `/images/[id]`:
   - PostDetailPage vs ImageDetailPage
   - PostDetailContent vs ImageDetailContent
   - Any layout/feature differences

**Line budget:** Target 250 lines (justification: dual-route coverage + both wireframes). Max 300.

**Stale path handling:** Cross-reference STALE-PATHS-AUDIT.md for detail-view entries. For each stale path:
- If component now exists under different name → use new verified path with note
- If component doesn't exist (was speculative) → omit from spec, mark as `⚠️ NOT-IMPL` in requirements if the feature was intended

**Cross-references (1 line each):**
- `See: SCR-VIEW-02 — Hotspot interaction layer (tap → spot-selected state → SpotDetail panel)`
- `See: SCR-VIEW-03 — Item/Solution detail and shopping connection`
- `See: SCR-VIEW-04 — Related content ("More from this Look") grid`
- `See: FLW-02 — Content Detail Flow (navigation contract)`
  </action>
  <verify>
1. `wc -l specs/screens/detail/SCR-VIEW-01-post-detail.md` — must be ≤300 lines
2. Grep for `packages/web/` paths in the file; verify each with `ls` — zero mismatches
3. File contains "Mobile" and "Desktop" ASCII wireframe sections
4. File contains EARS-format requirements with `✅` / `⚠️ NOT-IMPL` / `📋 PLANNED` tags
5. File contains `/posts/[id]` delta section
6. No stale paths from STALE-PATHS-AUDIT.md appear without `⚠️ stale` annotation
  </verify>
  <done>
SCR-VIEW-01-post-detail.md exists with ≤300 lines, all file paths verified, both wireframes present, EARS requirements tagged, route delta documented, and cross-references to VIEW-02/03/04/FLW-02 included.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SCR-VIEW-02-spot-hotspot.md</name>
  <files>specs/screens/detail/SCR-VIEW-02-spot-hotspot.md</files>
  <action>
Write `SCR-VIEW-02-spot-hotspot.md` following screen-spec-template format with these specifics:

**Structure:**
1. **Header** — ID: SCR-VIEW-02, Route: overlay within `/images/[id]` and `/posts/[id]`, Status: implemented
2. **Purpose** — One sentence: user taps hotspot markers to explore detected items and view solutions in a detail panel
3. **Component Map** — Table:
   - Hotspot (DS: component-registry) — the tap target markers on the image
   - SpotDetail (DS: component-registry) — the detail panel (BottomSheet on mobile, side panel on desktop)
   - SpotCard (DS: component-registry) — spot summary card
   - InteractiveShowcase (`packages/web/lib/components/detail/InteractiveShowcase.tsx`) — verify path
   - ImageCanvas (`packages/web/lib/components/detail/ImageCanvas.tsx`) — verify path
   - ConnectorLayer (`packages/web/lib/components/detail/ConnectorLayer.tsx`) — verify path
   - useSpotCardSync hook (`packages/web/lib/hooks/useSpotCardSync.ts`) — verify path
   - Verify all paths exist before including

4. **Layout — Mobile** — ASCII wireframe showing:
   - Image with Hotspot markers (numbered dots with brand colors)
   - Tapped state: highlighted hotspot + BottomSheet rising from bottom
   - BottomSheet contents: SpotDetail with solution list

5. **Layout — Desktop** — ASCII wireframe:
   - Image with hotspots + side panel (instead of BottomSheet)
   - Breakpoint table: `| Element | Mobile | Desktop |`
     - SpotDetail: BottomSheet → Side Panel
     - Hotspot size: compact → expanded with label

6. **Interaction States** — Table matching FLW-02 states:
   - idle → spot-selected → solution-loading → solution-ready → shopping-exit
   - Each state: what's visible, what's active, what triggered it

7. **Requirements** — EARS format with status tags:
   - "When user taps a Hotspot marker, the system shall highlight the marker and open SpotDetail panel" `✅`
   - "When SpotDetail opens, the system shall fetch `GET /api/v1/spots/[spotId]/solutions`" `✅`
   - "When solutions are loading, the system shall show skeleton placeholders in SpotDetail" — check impl
   - "When user taps outside hotspot area, the system shall deselect spot and close panel" — check impl
   - "When user selects a different hotspot while panel is open, the system shall switch to new spot's solutions" — check impl
   - transitionStore FLIP animation: "When user arrives from grid card, the system shall read originState from transitionStore and play FLIP enter animation via useFlipEnter hook" `✅`
   - "When FLIP animation completes, the system shall call transitionStore.reset()" `✅`

8. **State** — Reference transitionStore from store-map.md:
   - `transitionStore.selectedId` — tracks which spot is active (NOTE: verify if this is reused for spot selection or if separate state exists)
   - React Query cache for spot solutions

9. **FLIP Animation Sequence** — Brief description (not duplicating FLW-02):
   - Grid tap → transitionStore.setTransition(id, state, rect, imgSrc) → navigate → useFlipEnter reads → FLIP plays → reset
   - Fallback: direct URL access → fade-in (no originState)
   - Exit: useFlipExit → animate back → reset → navigate back
   - Reference: `See: FLW-02 transition table for full navigation contract`

10. **Navigation** — From/to within the detail page context

**Line budget:** Target 200 lines. Max 250.

**Cross-references:**
- `See: SCR-VIEW-01 — Page layout containing this interaction layer`
- `See: SCR-VIEW-03 — Item/Solution detail when solution link tapped`
- `See: FLW-02 — Interaction states and transition table`
  </action>
  <verify>
1. `wc -l specs/screens/detail/SCR-VIEW-02-spot-hotspot.md` — must be ≤250 lines
2. Grep for `packages/web/` paths; verify each with `ls`
3. File contains interaction states table matching FLW-02
4. File contains FLIP animation sequence section
5. File contains EARS requirements with implementation status tags
6. File contains mobile BottomSheet + desktop Side Panel breakpoint distinction
  </verify>
  <done>
SCR-VIEW-02-spot-hotspot.md exists with ≤250 lines, all file paths verified, interaction states documented, FLIP animation referenced, EARS requirements tagged, and mobile/desktop breakpoint table present.
  </done>
</task>

</tasks>

<verification>
1. Both files exist in `specs/screens/detail/`
2. Combined line count ≤550 (300 + 250)
3. Zero stale file paths — every `packages/web/` reference verified against filesystem
4. Both specs cross-reference each other and FLW-02
5. EARS requirements present with `✅` / `⚠️ NOT-IMPL` / `📋 PLANNED` tags
6. Both specs have mobile + desktop wireframes
7. transitionStore FLIP animation documented in SCR-VIEW-02
</verification>

<success_criteria>
- SCR-VIEW-01-post-detail.md covers both `/images/[id]` and `/posts/[id]` routes
- SCR-VIEW-02-spot-hotspot.md documents hotspot interaction layer with state transitions
- All component file paths are verified (zero stale paths)
- An AI agent reading these specs can identify exactly which files to modify for any detail view change
</success_criteria>

<output>
After completion, create `.planning/phases/v4-04-screen-specs-detail-view/v4-04-01-SUMMARY.md`
</output>
