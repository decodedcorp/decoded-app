---
phase: m7-01-magazine-frontend-mock
plan: 03
type: execute
wave: 3
depends_on: ["m7-01-01", "m7-01-02"]
files_modified:
  - packages/web/app/magazine/page.tsx
  - packages/web/app/magazine/layout.tsx
  - packages/web/lib/components/magazine/GenerateMyEdition.tsx
  - packages/web/lib/components/MobileNavBar.tsx
  - packages/web/lib/design-system/desktop-header.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to /magazine and see a rendered daily editorial"
    - "Magazine tab appears in both MobileNavBar and DesktopHeader navigation"
    - "MagazineSkeleton shows during loading, then editorial renders with GSAP animations"
    - "Generate My Edition CTA appears at bottom on scroll and links to /magazine/personal"
    - "Theme colors (#050505 bg, #eafd67 accent) are visible on the magazine page"
  artifacts:
    - path: "packages/web/app/magazine/page.tsx"
      provides: "Daily editorial page (server component)"
      min_lines: 15
    - path: "packages/web/app/magazine/layout.tsx"
      provides: "Magazine layout with dark theme wrapper"
    - path: "packages/web/lib/components/magazine/GenerateMyEdition.tsx"
      provides: "Scroll-triggered bottom CTA"
    - path: "packages/web/lib/components/MobileNavBar.tsx"
      provides: "Updated with Magazine tab"
      contains: "magazine"
    - path: "packages/web/lib/design-system/desktop-header.tsx"
      provides: "Updated with Magazine nav link"
      contains: "magazine"
  key_links:
    - from: "packages/web/app/magazine/page.tsx"
      to: "packages/web/lib/stores/magazineStore.ts"
      via: "Client component loads daily issue from store"
      pattern: "useMagazineStore|loadDailyIssue"
    - from: "packages/web/app/magazine/page.tsx"
      to: "packages/web/lib/components/magazine/MagazineRenderer.tsx"
      via: "Renders issue via MagazineRenderer"
      pattern: "MagazineRenderer"
    - from: "packages/web/lib/components/MobileNavBar.tsx"
      to: "/magazine"
      via: "NavItem with magazine href"
      pattern: "magazine"
---

<objective>
Create the /magazine route (SCR-MAG-01 Daily Editorial) and add Magazine navigation to both mobile NavBar and desktop header.

Purpose: This is the primary entry point for the magazine feature. Users navigate here from the NavBar to see the AI-generated daily editorial, rendered by MagazineRenderer with GSAP animations.

Output: Working /magazine page with mock data, skeleton loading, GSAP animations, and navigation integration.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/m7-01-magazine-frontend-mock/m7-01-CONTEXT.md
@.planning/phases/m7-01-magazine-frontend-mock/m7-01-01-SUMMARY.md
@.planning/phases/m7-01-magazine-frontend-mock/m7-01-02-SUMMARY.md
@packages/web/lib/components/MobileNavBar.tsx
@packages/web/lib/design-system/desktop-header.tsx
@packages/web/lib/components/ConditionalNav.tsx
@specs/screens/magazine/SCR-MAG-01-daily-editorial.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Daily Editorial Page + Layout</name>
  <files>
    packages/web/app/magazine/page.tsx
    packages/web/app/magazine/layout.tsx
    packages/web/lib/components/magazine/GenerateMyEdition.tsx
  </files>
  <action>
    **1. `packages/web/app/magazine/layout.tsx`:**
    - Simple layout wrapper that applies magazine-specific styles
    - Wrap children in a div with `bg-mag-bg min-h-screen text-mag-text`
    - This ensures the dark theme applies to all magazine sub-routes (/magazine, /magazine/personal)
    - Export metadata with title "Magazine | Decoded"

    **2. `packages/web/app/magazine/page.tsx`:**
    - Server component that renders a client wrapper
    - Create an inline "use client" component `DailyEditorialClient` (or separate file if cleaner):
      - On mount: call `useMagazineStore().loadDailyIssue()`
      - Read `currentIssue` and `isLoading` from store
      - While loading: render `<MagazineSkeleton />`
      - On error: render error card with retry button (styled with mag-accent)
      - When loaded: render `<MagazineRenderer issue={currentIssue} />`
      - After renderer: render `<GenerateMyEdition />`

    **3. `packages/web/lib/components/magazine/GenerateMyEdition.tsx`:**
    - "use client" component
    - Scroll-triggered CTA at the bottom of the editorial page
    - Use GSAP ScrollTrigger: trigger when element enters viewport at 90% scroll
    - Animation: fade-up (opacity 0->1, y 20->0, duration 0.5s)
    - UI: Dark card with accent border, text "Generate My Edition", arrow icon
    - On click: `router.push('/magazine/personal')`
    - Clean up GSAP context on unmount
    - Styled with `bg-mag-bg border border-mag-accent text-mag-accent`
  </action>
  <verify>
    1. `npx tsc --noEmit --project packages/web/tsconfig.json` passes
    2. Files exist: `packages/web/app/magazine/page.tsx`, `packages/web/app/magazine/layout.tsx`
    3. Page imports MagazineRenderer and MagazineSkeleton
    4. GenerateMyEdition uses ScrollTrigger
  </verify>
  <done>
    - /magazine route renders daily editorial from mock data
    - Loading state shows MagazineSkeleton
    - MagazineRenderer displays all components with GSAP animations
    - GenerateMyEdition CTA appears at bottom with scroll-triggered reveal
    - Magazine layout applies dark theme to all sub-routes
  </done>
</task>

<task type="auto">
  <name>Task 2: NavBar + DesktopHeader Magazine Tab</name>
  <files>
    packages/web/lib/components/MobileNavBar.tsx
    packages/web/lib/design-system/desktop-header.tsx
  </files>
  <action>
    **1. Update `MobileNavBar.tsx`:**
    - Add Magazine to the navItems array. Use `BookOpen` icon from lucide-react (import it).
    - Insert between Search and Request (Upload):
      ```typescript
      { id: "magazine", href: "/magazine", icon: BookOpen, label: "Magazine" },
      ```
    - This makes 5 nav items: Home, Search, Magazine, Upload, Profile
    - Active state: `pathname.startsWith('/magazine')` for magazine routes (covers /magazine and /magazine/personal)

    **2. Update `desktop-header.tsx`:**
    - Add Magazine to NAV_ITEMS array:
      ```typescript
      { href: "/magazine", label: "Magazine" },
      ```
    - Insert after Explore, before Upload
    - Active state is already handled by the existing pathname matching logic

    Do NOT change any other nav items or styling. Only add the Magazine entry.
  </action>
  <verify>
    1. `npx tsc --noEmit --project packages/web/tsconfig.json` passes
    2. Grep for "magazine" in MobileNavBar.tsx confirms nav item added
    3. Grep for "magazine" in desktop-header.tsx confirms nav item added
    4. `yarn build` passes (or at least `yarn lint`)
  </verify>
  <done>
    - Magazine tab appears in MobileNavBar (5th item with BookOpen icon)
    - Magazine nav link appears in DesktopHeader
    - Active state highlights correctly on /magazine and /magazine/personal routes
    - No other nav items modified
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project packages/web/tsconfig.json` — zero errors
2. `yarn dev` — /magazine route loads without errors
3. MobileNavBar shows 5 items including Magazine
4. DesktopHeader shows Magazine link
5. Daily editorial renders with GSAP animations
6. GenerateMyEdition CTA visible at bottom
</verification>

<success_criteria>
- Navigating to /magazine shows daily editorial with mock data
- MagazineSkeleton appears briefly during loading
- GSAP animations play on component entry
- Magazine tab is accessible from both mobile and desktop navigation
- GenerateMyEdition CTA links to /magazine/personal
- Dark theme (#050505 bg) applied to entire magazine route
</success_criteria>

<output>
After completion, create `.planning/phases/m7-01-magazine-frontend-mock/m7-01-03-SUMMARY.md`
</output>
