---
phase: v4-03-flow-documents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/flows/FLW-01-discovery.md
  - specs/flows/FLW-02-detail.md
  - specs/flows/FLW-03-creation.md
  - specs/flows/FLW-04-user.md
  - specs/flows/FLW-05-vton.md

autonomous: true

must_haves:
  truths:
    - "FLW-01 documents the Home -> Search/Filter -> Feed -> Detail navigation contract with route paths and triggers"
    - "FLW-02 documents the Post/Image Detail -> Spot -> Item -> Shopping journey with interaction contracts"
    - "FLW-03 documents the Upload -> AI Detect -> Spot Creation -> Solution Input journey step-by-step"
    - "FLW-04 documents the Login -> Profile -> Activity -> Earnings journey with auth state transitions"
    - "FLW-05 exists as a DRAFT file with VTON fitting flow outline and clear DRAFT status warning"
  artifacts:
    - path: "specs/flows/FLW-01-discovery.md"
      provides: "Content discovery navigation contract"
      contains: "Mermaid diagram + transition table"
    - path: "specs/flows/FLW-02-detail.md"
      provides: "Detail view interaction contract"
      contains: "Mermaid diagram + transition table"
    - path: "specs/flows/FLW-03-creation.md"
      provides: "Content creation step-by-step flow"
      contains: "Mermaid diagram + transition table"
    - path: "specs/flows/FLW-04-user.md"
      provides: "User system auth flow"
      contains: "Mermaid diagram + transition table"
    - path: "specs/flows/FLW-05-vton.md"
      provides: "VTON fitting flow draft"
      contains: "DRAFT status banner"
  key_links:
    - from: "specs/flows/FLW-01-discovery.md"
      to: "specs/flows/FLW-02-detail.md"
      via: "Detail entry point cross-reference"
    - from: "specs/flows/FLW-03-creation.md"
      to: "specs/_shared/api-contracts.md"
      via: "API endpoint references (upload, analyze, spots, solutions)"
    - from: "specs/flows/FLW-04-user.md"
      to: "specs/_shared/store-map.md"
      via: "authStore state transition references"
---

<objective>
Write all 5 flow documents (FLW-01 through FLW-05) for the decoded-app user journeys.

Purpose: Establish navigation contracts between screens so that subsequent screen specs (v4-04 through v4-07) can reference flow files for cross-screen context. Flows document WHAT screen transitions happen and WHEN, not HOW each screen works.

Output: 5 markdown files in `specs/flows/` — 4 finalized flows + 1 DRAFT flow
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v4-03-flow-documents/v4-03-CONTEXT.md

# Shared foundation files (reference for cross-linking)
@specs/_shared/component-registry.md
@specs/_shared/api-contracts.md
@specs/_shared/store-map.md
@specs/_shared/data-models.md

# Templates
@specs/shared/templates/flow-spec-template.md

# Prior phase context
@.planning/phases/v4-01-archive-foundation/v4-01-02-SUMMARY.md
@.planning/phases/v4-02-shared-foundation/v4-02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write FLW-01 (Discovery) and FLW-02 (Detail) flow documents</name>
  <files>specs/flows/FLW-01-discovery.md, specs/flows/FLW-02-detail.md</files>
  <action>
Create `specs/flows/` directory if it does not exist.

**FLW-01-discovery.md** — Content Discovery Flow (Home -> Search/Filter -> Feed -> Detail)

Use the flow-spec-template as structural reference. Apply these decisions from CONTEXT.md:
- Format: Mermaid flowchart/stateDiagram at top for visual overview, then transition table for details
- 200-line limit
- Include actual Next.js route paths for each screen
- Responsibility boundary: document WHAT/WHEN (navigation triggers, screen transitions), NOT HOW (UI details, component rendering)
- Reference `_shared/` files by relative path when mentioning stores or APIs

Screens to cover with their routes:
- Home (`/`) — entry point, hero carousel, trending, best sections
- Search (`/search`) — full-screen overlay, searchStore
- Explore (`/explore`) — category grid, filterStore
- Feed (`/feed`) — social timeline
- Image Grid (`/images`) — image discovery with infinite scroll
- Exit points to Detail flow (FLW-02): click on post/image -> `/images/[id]` or `/posts/[id]`

Transition table columns: From (screen + route) | Trigger | To (screen + route) | Store Changes | Data Fetched

Include cross-reference to FLW-02 as the detail entry point.

**FLW-02-detail.md** — Content Detail Flow (Post/Image Detail -> Spot -> Item -> Shopping)

Screens to cover with their routes:
- Image Detail (`/images/[id]`) — lightbox, hero, hotspot overlay
- Post Detail (`/posts/[id]`) — post metadata, content
- Spot selection (within detail view) — Hotspot click, transitionStore FLIP
- Item/Solution view (within detail view) — SpotDetail panel, solution list
- Shopping exit — external link to shop (affiliate)

This flow is more interaction-based (within a single page) than navigation-based. Use the Mermaid diagram to show the interaction states within the detail page, and the transition table for state changes.

Include cross-reference back to FLW-01 (entry from discovery) and forward to FLW-03 (if user creates content from detail context).
  </action>
  <verify>
- `specs/flows/FLW-01-discovery.md` exists with Mermaid diagram and transition table
- `specs/flows/FLW-02-detail.md` exists with Mermaid diagram and transition table
- Both files are under 200 lines each
- Both files include actual route paths (/, /search, /explore, /feed, /images, /images/[id], /posts/[id])
- Both files reference _shared/ files where relevant
- Cross-references between FLW-01 and FLW-02 are present
  </verify>
  <done>FLW-01 documents the discovery journey from Home through Search/Feed to Detail entry. FLW-02 documents the detail view interaction journey from post/image through spots to shopping. Both use Mermaid + table format with verified route paths.</done>
</task>

<task type="auto">
  <name>Task 2: Write FLW-03 (Creation), FLW-04 (User), and FLW-05 (VTON DRAFT) flow documents</name>
  <files>specs/flows/FLW-03-creation.md, specs/flows/FLW-04-user.md, specs/flows/FLW-05-vton.md</files>
  <action>
**FLW-03-creation.md** — Content Creation Flow (Upload -> AI Detect -> Spot Creation -> Solution Input)

Same format conventions as Task 1 (Mermaid + table, 200-line limit, actual routes, what/when only).

Screens to cover with their routes:
- Upload (`/request/upload`) — DropZone, image selection, requestStore initial state
- AI Detection (`/request/detect`) — AI analysis results, detected items display
- Spot editing (within detect view) — add/remove/adjust spots on image
- Solution input (within detect view or separate step) — link items to products, affiliate URLs

Key API endpoints to reference (from api-contracts.md):
- `POST /api/v1/posts/upload` — image upload
- `POST /api/v1/posts/analyze` — AI analysis
- `POST /api/v1/posts/[postId]/spots` — create spots
- `POST /api/v1/spots/[spotId]/solutions` — add solutions
- `POST /api/v1/posts/with-solution` — combined creation

requestStore state transitions are critical here — document the step progression (upload -> detecting -> editing -> submitting -> complete).

**FLW-04-user.md** — User Authentication Flow (Login -> Profile -> Activity -> Earnings)

Screens to cover with their routes:
- Login (`/login`) — OAuth providers (Kakao, Google, Apple), guest mode
- Profile (`/profile`) — user info, activity tabs, badges, stats
- Activity (within profile) — user history, contributions
- Earnings (within profile or future route) — click tracking, settlements

Key state: authStore transitions (unauthenticated -> loading -> authenticated / guest). Document:
- OAuth redirect flow (login -> provider -> callback -> redirect)
- Guest mode entry and limitations
- Auth-conditional navigation (what changes when logged in vs guest)
- Profile data loading (users/me endpoint)

Reference `_shared/store-map.md` authStore section for state shape.

**FLW-05-vton.md** — VTON Fitting Flow (DRAFT)

This is a DRAFT document. Requirements from ROADMAP and CONTEXT:
- Must have `> STATUS: DRAFT — not approved for implementation` banner at file top
- VTON stages from requirements: Pick&Drop -> Chic Blur -> Blueprint -> Magic Flip -> Morphing Loop
- No codebase references needed (this is a future feature with no current implementation)
- Keep shorter than the other flows (~100-150 lines max) — this is an outline, not a contract
- Mark all sections as provisional/draft
- Include a note that this flow connects to FLW-02 (detail view) as an entry point (user picks items from detail to try on)
  </action>
  <verify>
- `specs/flows/FLW-03-creation.md` exists with Mermaid diagram and transition table
- `specs/flows/FLW-04-user.md` exists with Mermaid diagram and transition table
- `specs/flows/FLW-05-vton.md` exists with DRAFT status banner at top
- FLW-03 and FLW-04 are under 200 lines each
- FLW-05 is under 150 lines
- FLW-03 references upload/analyze/spots/solutions API endpoints
- FLW-04 references authStore state transitions
- FLW-05 has clear DRAFT status and mentions connection to FLW-02
  </verify>
  <done>FLW-03 documents the creation journey from upload through AI detection to solution submission. FLW-04 documents the auth flow from login through profile and activity. FLW-05 exists as a DRAFT VTON flow outline with explicit DRAFT status warning and provisional content.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify:

1. Directory check: `ls specs/flows/` shows exactly 5 files (FLW-01 through FLW-05)
2. Line count: `wc -l specs/flows/FLW-0*.md` — all FLW-01~04 under 200 lines, FLW-05 under 150 lines
3. Route paths: grep for actual routes (/, /search, /explore, /feed, /images, /images/[id], /posts/[id], /request/upload, /request/detect, /login, /profile) across all flow files
4. Mermaid presence: each file contains a mermaid code block
5. DRAFT banner: FLW-05 starts with `> STATUS: DRAFT`
6. Cross-references: FLW-01 mentions FLW-02, FLW-02 mentions FLW-01, FLW-05 mentions FLW-02
7. _shared/ references: at least FLW-03 references api-contracts.md, FLW-04 references store-map.md
</verification>

<success_criteria>
- 5 flow files exist in specs/flows/ with correct naming (FLW-01 through FLW-05)
- All 4 main flows use Mermaid + transition table format within 200-line limit
- Every screen reference includes its actual Next.js route path
- Flows document WHAT/WHEN navigation contracts, not HOW screens work (screen spec territory)
- FLW-05 has explicit DRAFT status and is clearly marked as pending approval
- Cross-references between flows exist where journeys connect
- _shared/ files are referenced where relevant (stores, APIs)
</success_criteria>

<output>
After completion, create `.planning/phases/v4-03-flow-documents/v4-03-01-SUMMARY.md`
</output>
