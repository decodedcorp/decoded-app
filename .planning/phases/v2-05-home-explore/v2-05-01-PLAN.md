---
phase: v2-05-home-explore-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/main/SectionHeader.tsx
  - packages/web/lib/components/main/HomePageSections.tsx
  - packages/web/lib/components/main/HomeAnimatedContent.tsx
  - packages/web/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User visiting home page sees all sections with decoded.pen styling and spacing"
    - "User on mobile sees single-column home layout with responsive typography"
    - "User on desktop sees multi-column layout for section grids (4-6 columns)"
    - "Section headers use decoded.pen typography (h2: 24px semibold)"
    - "Section spacing follows decoded.pen spec (py-10 md:py-16, consistent padding)"
    - "All existing data fetching and animations continue to work"
  artifacts:
    - path: "packages/web/lib/components/main/SectionHeader.tsx"
      provides: "Updated SectionHeader with decoded.pen styling"
      exports: ["SectionHeader"]
    - path: "packages/web/lib/components/main/HomePageSections.tsx"
      provides: "Refactored section components with consistent styling"
      exports: ["HeroSection", "DecodedPickSection", "ArtistSpotlightSection", "WhatsNewSection", "DiscoverItemsSection", "BestItemSection", "WeeklyBestSection", "TrendingNowSection"]
  key_links:
    - from: "packages/web/app/page.tsx"
      to: "HomeAnimatedContent"
      via: "client component import"
      pattern: "import.*HomeAnimatedContent"
    - from: "packages/web/lib/components/main/HomeAnimatedContent.tsx"
      to: "section components"
      via: "composition"
      pattern: "import.*Section"
---

<objective>
Implement Home page mobile and desktop layouts with decoded.pen design system styling.

Purpose: Apply decoded.pen design tokens and spacing to all Home page sections while preserving existing functionality (data fetching, animations, server components pattern).

Output:
- Updated `SectionHeader.tsx` with decoded.pen typography and spacing
- Refactored section components with consistent padding and grid layouts
- Updated `HomeAnimatedContent.tsx` with proper section spacing
- Updated `app/page.tsx` with layout wrapper for header padding
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/phases/v2-05-home-explore-pages/v2-05-CONTEXT.md

Reference files:
@packages/web/lib/design-system/tokens.ts (spacing, typography)
@packages/web/lib/design-system/typography.tsx (Heading, Text components)
@packages/web/lib/components/main/SectionHeader.tsx (current implementation)
@packages/web/lib/components/main/HomeAnimatedContent.tsx (current implementation)
@packages/web/lib/components/main/DiscoverSection.tsx (section pattern)
@packages/web/lib/components/main/BestSection.tsx (section pattern)
@packages/web/app/page.tsx (server component entry)
@docs/design-system/decoded.pen (design source)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SectionHeader with decoded.pen styling</name>
  <files>packages/web/lib/components/main/SectionHeader.tsx</files>
  <action>
Refactor SectionHeader to use design-system typography and decoded.pen specifications:

1. **Import design-system typography:**
   ```typescript
   import { Heading, Text } from "@/lib/design-system";
   ```

2. **Update typography:**
   - Title: Use `<Heading variant="h2">` (maps to text-2xl font-semibold)
   - Subtitle: Use `<Text variant="small" textColor="muted">`
   - VIEW MORE link: Use `<Text variant="small" textColor="muted">` with hover:text-foreground

3. **Adjust spacing:**
   - Keep flex layout with items-end justify-between
   - Margin bottom: mb-6 (as current, matches decoded.pen)
   - Subtitle margin: mt-1

4. **Preserve motion animation:**
   - Keep motion.div wrapper with whileInView animation
   - Maintain current viewport and transition settings

5. **Props remain unchanged:**
   - title: string
   - subtitle?: string
   - viewMoreLink?: string
   - viewMoreText?: string (default: "VIEW MORE")

Do NOT change the animation behavior or props interface.
  </action>
  <verify>
- File compiles without TypeScript errors: `cd packages/web && npx tsc --noEmit`
- SectionHeader still exports correctly from main/index.ts
  </verify>
  <done>
SectionHeader uses design-system Heading and Text components with decoded.pen styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize section padding and max-width</name>
  <files>packages/web/lib/components/main/DiscoverSection.tsx, packages/web/lib/components/main/BestSection.tsx, packages/web/lib/components/main/WhatsNewSection.tsx, packages/web/lib/components/main/DecodedPickSection.tsx, packages/web/lib/components/main/ArtistSpotlightSection.tsx, packages/web/lib/components/main/TrendingSection.tsx</files>
  <action>
Apply consistent decoded.pen section styling across all Home page sections:

1. **Standard section wrapper classes (apply to all sections):**
   ```typescript
   <section className="py-10 md:py-16 px-4 md:px-6 lg:px-8">
     <div className="max-w-7xl mx-auto">
       {/* section content */}
     </div>
   </section>
   ```

2. **Background alternation pattern:**
   - HeroSection: no background (uses image/gradient)
   - DecodedPickSection: bg-card
   - ArtistSpotlightSection: bg-background
   - WhatsNewSection: bg-card
   - DiscoverItemsSection: bg-background
   - DiscoverProductsSection: bg-card
   - BestItemSection: bg-background
   - WeeklyBestSection: bg-card
   - TrendingNowSection: bg-background

3. **Grid column updates (responsive):**
   - 2 columns on mobile (grid-cols-2)
   - 3 columns on tablet (md:grid-cols-3)
   - 4-6 columns on desktop depending on section:
     - Item grids: lg:grid-cols-6
     - Style cards: lg:grid-cols-4
     - Weekly best: md:grid-cols-4 (already 4 items per page)

4. **Gap standardization:**
   - Card grids: gap-4 (16px)
   - Horizontal scrolls: gap-4

Do NOT change any data handling, props, or animation logic.
  </action>
  <verify>
- All section files compile: `cd packages/web && npx tsc --noEmit`
- Run dev server and visually verify sections render correctly
  </verify>
  <done>
All Home page sections have consistent padding, max-width, and responsive grids matching decoded.pen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update HomeAnimatedContent section spacing</name>
  <files>packages/web/lib/components/main/HomeAnimatedContent.tsx</files>
  <action>
Update the main container and section wrapper spacing:

1. **Remove space-y-0 from main:**
   - Sections handle their own vertical padding (py-10 md:py-16)
   - Main should just be a container: `<main>`

2. **Ensure motion.div wrappers don't add extra spacing:**
   - motion.div should have no margin/padding classes
   - viewport margin can stay as is for scroll trigger

3. **Section order remains the same:**
   1. HeroSection
   2. DecodedPickSection
   3. ArtistSpotlightSection
   4. WhatsNewSection
   5. DiscoverItemsSection
   6. DiscoverProductsSection
   7. BestItemSection
   8. WeeklyBestSection
   9. TrendingNowSection

4. **Verify all sections receive their props correctly:**
   - No changes to props passing
   - No changes to data transformations

Do NOT modify the animation variants or viewport settings.
  </action>
  <verify>
- TypeScript compiles: `cd packages/web && npx tsc --noEmit`
- Sections flow naturally with alternating backgrounds
  </verify>
  <done>
HomeAnimatedContent provides clean section spacing without conflicting margins.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update app/page.tsx layout wrapper</name>
  <files>packages/web/app/page.tsx</files>
  <action>
Ensure the Home page has proper layout structure for header spacing:

1. **Current structure check:**
   - Uses Header component (MobileHeader)
   - Uses HomeAnimatedContent
   - Uses MainFooter

2. **Apply header padding compensation:**
   ```typescript
   <div className="min-h-screen bg-background pt-14 pb-14 md:pt-16 md:pb-0">
   ```
   - Mobile: pt-14 (56px for mobile header) + pb-14 (for MobileNavBar)
   - Desktop: pt-16 (64px for desktop header) + no bottom padding

3. **Ensure DesktopHeader and MobileHeader are both rendered:**
   - Import from design-system: `import { DesktopHeader, MobileHeader } from "@/lib/design-system";`
   - Or keep using the existing Header component which handles responsive visibility

4. **Footer positioning:**
   - MainFooter should be at the bottom naturally due to content flow
   - No fixed positioning needed on home page

Current implementation may already have some of this - only change what's needed to match decoded.pen specs.
  </action>
  <verify>
- TypeScript compiles: `cd packages/web && npx tsc --noEmit`
- Home page renders with proper header spacing on both mobile and desktop
  </verify>
  <done>
Home page layout has proper header and nav bar padding compensation.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd packages/web && npx tsc --noEmit` passes
2. `cd packages/web && npm run lint` passes (or only shows pre-existing warnings)
3. Home page displays all sections with consistent decoded.pen styling
4. Mobile layout shows single/two-column grids
5. Desktop layout shows multi-column grids (4-6 columns)
6. All existing data fetching and animations still work
</verification>

<success_criteria>
- SectionHeader uses design-system typography components
- All sections have consistent padding (py-10 md:py-16, px-4 md:px-6 lg:px-8)
- All sections have max-w-7xl mx-auto container
- Responsive grid columns work correctly on all breakpoints
- Alternating background colors create visual section separation
- No breaking changes to data flow or animations
</success_criteria>

<output>
After completion, create `.planning/phases/v2-05-home-explore-pages/v2-05-01-SUMMARY.md`
</output>
