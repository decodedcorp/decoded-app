---
phase: v2-02-core-interactive-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/input.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "User can type into input fields with visible placeholder text"
    - "User can see left/right icons in input fields"
    - "User can see labels above input fields when provided"
    - "User can see error messages below input fields when validation fails"
    - "User can use search input with clear button functionality"
  artifacts:
    - path: "packages/web/lib/design-system/input.tsx"
      provides: "Input and SearchInput components"
      exports: ["Input", "inputVariants", "SearchInput"]
    - path: "packages/web/lib/design-system/index.ts"
      provides: "Barrel export for Input components"
      exports: ["Input", "SearchInput"]
  key_links:
    - from: "packages/web/lib/design-system/input.tsx"
      to: "tokens.ts"
      via: "Tailwind classes using design tokens"
      pattern: "border-input|bg-background|text-destructive"
---

<objective>
Create Input and SearchInput components in the design-system folder with support for icons, labels, error states, and search functionality.

Purpose: Input components are essential for forms, search, and user data entry. Following the design system pattern established in v2-01, these components use cva for variants and integrate with design tokens.

Output: Input.tsx with Input and SearchInput components covering INP-01, INP-02, INP-03 requirements.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/phases/v2-02-core-interactive-components/v2-02-CONTEXT.md
@.planning/phases/v2-01-design-system-foundation/v2-01-02-SUMMARY.md
@packages/web/lib/design-system/typography.tsx
@packages/web/lib/design-system/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base Input component with icons and label</name>
  <files>packages/web/lib/design-system/input.tsx</files>
  <action>
Create `packages/web/lib/design-system/input.tsx` with:

1. Use cva for inputVariants with:
   - Base styles: `flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50`
   - Error variant: `border-destructive focus-visible:ring-destructive`
   - Search variant: `rounded-full pl-10 pr-10`

2. InputProps interface extending React.InputHTMLAttributes:
   ```tsx
   interface InputProps extends React.InputHTMLAttributes<HTMLInputElement>,
     VariantProps<typeof inputVariants> {
     leftIcon?: React.ReactNode;
     rightIcon?: React.ReactNode;
     label?: string;
     helperText?: string;
     error?: string;
   }
   ```

3. Input component with forwardRef:
   - Wrapper div containing optional label, input container, helper/error text
   - Icon containers positioned absolutely within input container
   - When leftIcon present: add `pl-10` padding
   - When rightIcon present: add `pr-10` padding
   - Error state shows error message in text-destructive color
   - Label uses `text-sm font-medium mb-1.5` styling

4. Export Input, inputVariants, and InputProps type
  </action>
  <verify>
1. File exists at packages/web/lib/design-system/input.tsx
2. `cd packages/web && npx tsc --noEmit` passes
3. Input accepts leftIcon, rightIcon, label, error props
  </verify>
  <done>
Input component created with icon slots, label, and error state support
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SearchInput component</name>
  <files>packages/web/lib/design-system/input.tsx</files>
  <action>
Add SearchInput component to the same file:

1. SearchInput wraps Input with search-specific defaults:
   ```tsx
   interface SearchInputProps extends Omit<InputProps, 'leftIcon' | 'variant'> {
     onClear?: () => void;
   }
   ```

2. SearchInput implementation:
   - Always shows Search icon (Lucide) as leftIcon
   - Shows X (close) icon as rightIcon when value is non-empty
   - X icon is clickable: clears input and calls onClear
   - Uses `variant="search"` from inputVariants (rounded-full)
   - Placeholder defaults to "Search..." if not provided

3. Import Search and X icons from lucide-react

4. Clear button styling:
   - Cursor pointer on X icon
   - Hover state: opacity change
   - onClick handler clears value

Example usage:
```tsx
<SearchInput
  value={query}
  onChange={(e) => setQuery(e.target.value)}
  onClear={() => setQuery('')}
/>
```
  </action>
  <verify>
1. `cd packages/web && npx tsc --noEmit` passes
2. SearchInput exports alongside Input
3. SearchInput has Search icon left, X icon right when value present
  </verify>
  <done>
SearchInput component created with clear button functionality
  </done>
</task>

<task type="auto">
  <name>Task 3: Update barrel export and verify build</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
1. Update packages/web/lib/design-system/index.ts to export Input components:
   ```tsx
   // Input Components
   export { Input, inputVariants, SearchInput } from './input';
   export type { InputProps, SearchInputProps } from './input';
   ```

2. Run full verification:
   - `cd packages/web && npx tsc --noEmit` - type check
   - `cd packages/web && yarn build` - production build

3. Verify exports are accessible:
   ```tsx
   import { Input, SearchInput } from '@/lib/design-system';
   ```
  </action>
  <verify>
1. Barrel export includes Input, inputVariants, SearchInput
2. TypeScript compilation passes
3. Build succeeds with no errors
  </verify>
  <done>
Input components exported from design-system and build passes
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd packages/web && npx tsc --noEmit` passes
2. `cd packages/web && yarn build` succeeds
3. Input exports: Input, inputVariants, InputProps
4. SearchInput exports: SearchInput, SearchInputProps
5. Barrel export in index.ts includes all input exports
</verification>

<success_criteria>
- Input component supports leftIcon, rightIcon, label, helperText, error props
- Input has error variant with red border
- SearchInput shows search icon left, clear button right
- SearchInput clear button works when clicked
- All TypeScript types properly exported
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/v2-02-core-interactive-components/v2-02-02-SUMMARY.md`
</output>
