---
phase: v2-02-core-interactive-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/ui/button.tsx
autonomous: true

must_haves:
  truths:
    - "User can click buttons with all 5 style variants (default/Primary, secondary, outline, ghost, destructive)"
    - "User can click buttons in 5 size variants (sm, default, lg, icon, icon-sm, icon-lg)"
    - "User sees loading spinner when button isLoading=true"
    - "User cannot interact with disabled or loading buttons"
    - "Button hover, active, focus states work correctly"
  artifacts:
    - path: "packages/web/lib/components/ui/button.tsx"
      provides: "Extended Button component with icon sizes and loading state"
      exports: ["Button", "buttonVariants"]
  key_links:
    - from: "packages/web/lib/components/ui/button.tsx"
      to: "design-system tokens"
      via: "Tailwind utility classes using CSS variables"
      pattern: "bg-primary|text-primary-foreground"
---

<objective>
Extend the existing shadcn/ui Button component with additional size variants for icon-only buttons and a loading state.

Purpose: Buttons are the primary interactive element. ActionButtons (like/save/share) will use Button with icon size variants, eliminating the need for a separate ActionButton component.

Output: Enhanced Button component that covers BTN-01 through BTN-09 requirements.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/phases/v2-02-core-interactive-components/v2-02-CONTEXT.md
@.planning/phases/v2-01-design-system-foundation/v2-01-01-SUMMARY.md
@packages/web/lib/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icon-sm and icon-lg size variants</name>
  <files>packages/web/lib/components/ui/button.tsx</files>
  <action>
Add two new size variants to buttonVariants:
- `icon-sm`: `h-8 w-8` for small icon buttons (32px)
- `icon-lg`: `h-12 w-12` for large icon buttons (48px)

Keep existing sizes unchanged:
- `default`: h-10 px-4 py-2
- `sm`: h-9 rounded-md px-3
- `lg`: h-11 rounded-md px-8
- `icon`: h-10 w-10

Ensure all icon sizes include `rounded-md` for consistency.
  </action>
  <verify>
1. Run `npx tsc --noEmit` in packages/web - no type errors
2. Verify buttonVariants includes all 6 size options
  </verify>
  <done>
Button supports 6 size variants: sm, default, lg, icon, icon-sm, icon-lg
  </done>
</task>

<task type="auto">
  <name>Task 2: Add isLoading prop with spinner</name>
  <files>packages/web/lib/components/ui/button.tsx</files>
  <action>
1. Extend ButtonProps interface with `isLoading?: boolean` prop
2. When isLoading=true:
   - Show a spinner icon (use Lucide `Loader2` with `animate-spin`)
   - Disable pointer events (button becomes non-interactive)
   - Keep button text visible but dimmed (opacity-70)
3. Add conditional rendering in Button component:
   ```tsx
   {isLoading && <Loader2 className="animate-spin" />}
   ```
4. When isLoading, add `pointer-events-none opacity-70` to button classes
5. Import Loader2 from lucide-react at top of file
  </action>
  <verify>
1. Run `npx tsc --noEmit` in packages/web - no type errors
2. Inspect Button component renders Loader2 when isLoading=true
3. Check isLoading prop exists in ButtonProps type
  </verify>
  <done>
Button shows spinner and disables interaction when isLoading=true
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all button states work correctly</name>
  <files>packages/web/lib/components/ui/button.tsx</files>
  <action>
Verify the implementation covers all requirements:

**Style variants (BTN-01 to BTN-05):**
- default (Primary): bg-primary text-primary-foreground
- secondary: bg-secondary text-secondary-foreground
- outline: border border-input bg-background
- ghost: hover:bg-accent
- destructive: bg-destructive text-destructive-foreground

**Size variants (BTN-06, BTN-07):**
- sm (Small): h-9
- default: h-10
- lg (Large): h-11

**Icon variants (BTN-08, BTN-09):**
- icon: h-10 w-10 (default icon)
- icon-sm: h-8 w-8 (small icon)
- icon-lg: h-12 w-12 (large icon)

Run TypeScript check to ensure exports are correct:
```bash
cd packages/web && npx tsc --noEmit
```

Test that `yarn build` succeeds with the changes.
  </action>
  <verify>
1. `yarn build` completes without errors
2. All button variants accessible via buttonVariants
3. TypeScript types correctly infer all variant options
  </verify>
  <done>
All 5 style variants and 6 size variants work with proper TypeScript types
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd packages/web && npx tsc --noEmit` passes
2. `cd packages/web && yarn build` succeeds
3. Button exports: Button, buttonVariants
4. ButtonProps includes: variant, size, isLoading, asChild
5. Size variants include: sm, default, lg, icon, icon-sm, icon-lg
</verification>

<success_criteria>
- All 5 style variants work (default, secondary, outline, ghost, destructive)
- All 6 size variants work (sm, default, lg, icon, icon-sm, icon-lg)
- isLoading prop shows spinner and disables interaction
- TypeScript compilation passes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/v2-02-core-interactive-components/v2-02-01-SUMMARY.md`
</output>
