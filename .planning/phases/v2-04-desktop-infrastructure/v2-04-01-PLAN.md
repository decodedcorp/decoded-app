---
phase: v2-04-desktop-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/desktop-header.tsx
  - packages/web/lib/design-system/mobile-header.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "User on desktop (md+) sees sticky header with logo, navigation links, and user area"
    - "User on mobile (<md) sees simplified header with logo and search icon"
    - "Navigation items (Home, Discover, Create) are visible as text links on desktop"
    - "Header stays fixed at top when scrolling"
    - "Logged-in user sees avatar and notification icon; logged-out user sees Login button"
  artifacts:
    - path: "packages/web/lib/design-system/desktop-header.tsx"
      provides: "DesktopHeader component with navigation, search, user area"
      exports: ["DesktopHeader", "DesktopHeaderProps"]
    - path: "packages/web/lib/design-system/mobile-header.tsx"
      provides: "MobileHeader component with logo and search"
      exports: ["MobileHeader", "MobileHeaderProps"]
  key_links:
    - from: "packages/web/lib/design-system/desktop-header.tsx"
      to: "useAuthStore"
      via: "Zustand store hook"
      pattern: "useAuthStore"
    - from: "packages/web/lib/design-system/index.ts"
      to: "desktop-header.tsx, mobile-header.tsx"
      via: "barrel export"
      pattern: "export.*DesktopHeader|MobileHeader"
---

<objective>
Create DesktopHeader and MobileHeader design-system components with decoded.pen styling.

Purpose: Provide responsive header navigation for desktop and mobile breakpoints, replacing the current Sidebar-based desktop navigation with a top header approach per CONTEXT.md decisions.

Output:
- `packages/web/lib/design-system/desktop-header.tsx` - Desktop header with nav links, search, user area
- `packages/web/lib/design-system/mobile-header.tsx` - Mobile header with logo and search icon
- Updated barrel export in `index.ts`
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/phases/v2-04-desktop-infrastructure/v2-04-CONTEXT.md

Reference files:
@packages/web/lib/design-system/card.tsx (cva pattern)
@packages/web/lib/design-system/tokens.ts (zIndex.header, colors)
@packages/web/lib/components/Header.tsx (current mobile header - to be replaced)
@packages/web/lib/components/DecodedLogo.tsx (reuse logo component)
@specs/shared/components/CMN-01-header.md (spec reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DesktopHeader component</name>
  <files>packages/web/lib/design-system/desktop-header.tsx</files>
  <action>
Create DesktopHeader component following design-system patterns:

1. **Layout Structure:**
   - Sticky header with `position: fixed`, `top: 0`, full width
   - z-index: 30 (from tokens.zIndex.header)
   - Height: 64px (--header-height-desktop from spec)
   - Background: bg-background/95 with backdrop-blur
   - Border-bottom for subtle separation

2. **Left Section:**
   - DecodedLogo component (import from @/lib/components/DecodedLogo)
   - Link to home (/)
   - Use same logo config as current Header.tsx

3. **Center Section (Navigation):**
   - Horizontal nav items: Home, Discover, Create
   - Use Next.js Link components
   - Active state: text-foreground + font-medium
   - Inactive state: text-muted-foreground + hover:text-foreground
   - Use usePathname() to detect active route

4. **Right Section:**
   - Search icon button (Search from lucide-react) - onClick prop for expansion
   - Conditional rendering based on auth state (use useAuthStore):
     - Logged out: Login button (Button component with variant="outline")
     - Logged in: Notification bell icon + Avatar with dropdown trigger
   - Avatar can use a simple placeholder initially (32x32 circle)

5. **Styling:**
   - Use cva for variants (default, transparent for hero pages)
   - Use cn() utility for className merging
   - Apply spacing.4 (16px) for horizontal padding
   - Hidden on mobile: className includes "hidden md:flex"

6. **Props Interface:**
   - variant?: 'default' | 'transparent'
   - onSearchClick?: () => void
   - className?: string

Do NOT implement search dropdown, notification panel, or user menu dropdown - these are out of scope. Just provide onClick handlers and visual placeholders.
  </action>
  <verify>
- File exists at packages/web/lib/design-system/desktop-header.tsx
- TypeScript compiles without errors: `cd packages/web && npx tsc --noEmit`
- Component exports DesktopHeader and DesktopHeaderProps
  </verify>
  <done>
DesktopHeader renders with logo, navigation links, search icon, and conditional user area (login button vs avatar) based on auth state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MobileHeader component</name>
  <files>packages/web/lib/design-system/mobile-header.tsx</files>
  <action>
Create MobileHeader component for mobile breakpoint:

1. **Layout Structure:**
   - Sticky header with `position: fixed`, `top: 0`, full width
   - z-index: 30
   - Height: 56px (--header-height-mobile from spec)
   - Background: bg-background/80 with backdrop-blur-md
   - Only visible on mobile: "md:hidden"

2. **Left Section:**
   - DecodedLogo component (same as current Header.tsx)
   - Link to home

3. **Right Section:**
   - Search icon button (Search from lucide-react)
   - Optional filter icon button (SlidersHorizontal from lucide-react)
   - Both should accept onClick handlers as props

4. **Props Interface:**
   - onSearchClick?: () => void
   - onFilterClick?: () => void
   - showFilter?: boolean (default true)
   - className?: string

This is essentially a refactored version of the current Header.tsx from lib/components, but placed in design-system with cleaner props interface.
  </action>
  <verify>
- File exists at packages/web/lib/design-system/mobile-header.tsx
- TypeScript compiles without errors
- Component exports MobileHeader and MobileHeaderProps
  </verify>
  <done>
MobileHeader renders with logo and search/filter icons, visible only on mobile (<768px).
  </done>
</task>

<task type="auto">
  <name>Task 3: Export header components from design-system index</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
Add exports for DesktopHeader and MobileHeader to the design-system barrel file:

```typescript
// Header Components
export { DesktopHeader } from './desktop-header';
export type { DesktopHeaderProps } from './desktop-header';

export { MobileHeader } from './mobile-header';
export type { MobileHeaderProps } from './mobile-header';
```

Add these after the existing Profile Header Card exports.
  </action>
  <verify>
- Run: `cd packages/web && npx tsc --noEmit` - should compile successfully
- Exports are available: DesktopHeader, MobileHeader, DesktopHeaderProps, MobileHeaderProps
  </verify>
  <done>
Header components are exported from design-system barrel file and can be imported via `@/lib/design-system`.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd packages/web && npx tsc --noEmit` passes
2. `cd packages/web && npm run lint` passes (or only shows pre-existing warnings)
3. Both DesktopHeader and MobileHeader can be imported from `@/lib/design-system`
</verification>

<success_criteria>
- DesktopHeader component exists with navigation links, search, and user area
- MobileHeader component exists with logo and search/filter icons
- Both components use design-system patterns (cva, cn, tokens)
- Components are properly exported from design-system index
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/v2-04-desktop-infrastructure/v2-04-01-SUMMARY.md`
</output>
