---
phase: v2-09-documentation-polish
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - docs/qa-screenshots/README.md
  - docs/qa-screenshots/*.png
  - packages/web/playwright.config.ts
  - packages/web/tests/visual-qa.spec.ts
autonomous: false

must_haves:
  truths:
    - "주요 페이지의 스크린샷이 모든 breakpoint에서 캡처되어 있다"
    - "decoded.pen과 비교하여 시각적 차이가 식별되고 기록되어 있다"
    - "발견된 시각적 이슈가 수정되었거나 문서화되어 있다"
  artifacts:
    - path: "docs/qa-screenshots/README.md"
      provides: "QA screenshot index and findings"
      contains: "Visual QA"
    - path: "packages/web/tests/visual-qa.spec.ts"
      provides: "Playwright screenshot automation"
      contains: "screenshot"
  key_links:
    - from: "docs/qa-screenshots/README.md"
      to: "docs/design-system/decoded.pen"
      via: "design reference"
      pattern: "decoded.pen"
---

<objective>
Playwright로 주요 페이지의 반응형 스크린샷을 캡처하고, decoded.pen과 비교하여 시각적 차이를 식별/수정한다.

Purpose: v2.0 디자인 시스템 구현이 decoded.pen 디자인과 pixel-perfect하게 일치하는지 검증
Output: QA 스크린샷, 시각적 차이 리포트, 필요한 CSS/레이아웃 수정
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/phases/v2-09-documentation-polish/v2-09-CONTEXT.md

# Design reference
@docs/design-system/decoded.pen

# Key pages to test
@packages/web/app/page.tsx
@packages/web/app/explore/page.tsx
@packages/web/app/feed/page.tsx
@packages/web/app/profile/page.tsx
@packages/web/app/search/page.tsx
@packages/web/app/images/[id]/page.tsx
@packages/web/app/request/upload/page.tsx
@packages/web/app/login/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright Visual QA 테스트 설정</name>
  <files>
    packages/web/playwright.config.ts
    packages/web/tests/visual-qa.spec.ts
  </files>
  <action>
Playwright로 반응형 스크린샷 자동 캡처 테스트 설정:

1. **playwright.config.ts** 확인/업데이트:
   - 이미 설정되어 있으면 projects에 viewport 설정 확인
   - 없으면 기본 설정으로 진행
   - webServer가 `yarn dev`를 실행하도록 설정

2. **tests/visual-qa.spec.ts** 생성:
   ```typescript
   import { test } from '@playwright/test';

   const VIEWPORTS = [
     { name: 'mobile', width: 375, height: 812 },
     { name: 'tablet', width: 768, height: 1024 },
     { name: 'desktop', width: 1280, height: 800 },
     { name: 'desktop-lg', width: 1440, height: 900 },
   ];

   const PAGES = [
     { name: 'home', path: '/' },
     { name: 'explore', path: '/explore' },
     { name: 'feed', path: '/feed' },
     { name: 'search', path: '/search?q=dress' },
     { name: 'profile', path: '/profile' },
     { name: 'login', path: '/login' },
     { name: 'request-upload', path: '/request/upload' },
   ];

   for (const viewport of VIEWPORTS) {
     test.describe(`${viewport.name} (${viewport.width}x${viewport.height})`, () => {
       test.use({ viewport: { width: viewport.width, height: viewport.height } });

       for (const page of PAGES) {
         test(`${page.name}`, async ({ page: p }) => {
           await p.goto(page.path);
           await p.waitForLoadState('networkidle');
           // Wait for animations to settle
           await p.waitForTimeout(500);
           await p.screenshot({
             path: `../../docs/qa-screenshots/${viewport.name}-${page.name}.png`,
             fullPage: true,
           });
         });
       }
     });
   }
   ```

3. **package.json 스크립트 확인**:
   - `yarn test:e2e` 또는 `yarn playwright test` 명령어 존재 확인
   - 없으면 추가: `"test:visual": "playwright test tests/visual-qa.spec.ts"`

Note: 테스트 실행은 Task 2에서 수행
  </action>
  <verify>
    - packages/web/tests/visual-qa.spec.ts 파일 존재
    - grep "screenshot" packages/web/tests/visual-qa.spec.ts
    - grep "VIEWPORTS" packages/web/tests/visual-qa.spec.ts
  </verify>
  <done>
    Playwright 테스트가 6개 breakpoint에서 7개 페이지 스크린샷을 캡처하도록 설정됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 스크린샷 캡처 및 QA 문서 생성</name>
  <files>
    docs/qa-screenshots/README.md
    docs/qa-screenshots/*.png
  </files>
  <action>
1. **docs/qa-screenshots/ 폴더 생성**

2. **Playwright 테스트 실행**:
   ```bash
   cd packages/web
   yarn playwright test tests/visual-qa.spec.ts --reporter=list
   ```

   실패 시 개별 페이지 수동 스크린샷 (fallback):
   ```bash
   yarn playwright screenshot http://localhost:3000/ docs/qa-screenshots/desktop-home.png --viewport-size=1280,800
   ```

3. **docs/qa-screenshots/README.md** 생성:
   ```markdown
   # Visual QA Screenshots

   **Generated:** 2026-02-05
   **Reference:** docs/design-system/decoded.pen

   ## Breakpoints Tested

   | Name | Width | Height |
   |------|-------|--------|
   | Mobile | 375px | 812px |
   | Tablet | 768px | 1024px |
   | Desktop | 1280px | 800px |
   | Desktop LG | 1440px | 900px |

   ## Pages Captured

   | Page | Mobile | Tablet | Desktop | Desktop LG |
   |------|--------|--------|---------|------------|
   | Home | [link] | [link] | [link] | [link] |
   | Explore | ... | ... | ... | ... |
   | Feed | ... | ... | ... | ... |
   | Search | ... | ... | ... | ... |
   | Profile | ... | ... | ... | ... |
   | Login | ... | ... | ... | ... |
   | Request Upload | ... | ... | ... | ... |

   ## Findings

   ### [Page Name]
   - **Issue**: [설명]
   - **Severity**: [Critical/Major/Minor]
   - **Status**: [Fixed/Documented/Deferred]
   - **decoded.pen Reference**: [섹션]

   ## Regeneration

   ```bash
   cd packages/web
   yarn playwright test tests/visual-qa.spec.ts
   ```
   ```

4. 스크린샷 파일 naming: `{viewport}-{page}.png`
   - mobile-home.png, tablet-home.png, desktop-home.png, desktop-lg-home.png
   - mobile-explore.png, tablet-explore.png, ...
  </action>
  <verify>
    - docs/qa-screenshots/ 폴더 존재
    - docs/qa-screenshots/README.md 파일 존재
    - ls docs/qa-screenshots/*.png | wc -l (28개 이상 - 4 viewports x 7 pages)
  </verify>
  <done>
    모든 주요 페이지의 스크린샷이 4개 breakpoint에서 캡처되고 문서화됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    4개 breakpoint(mobile 375px, tablet 768px, desktop 1280px, desktop-lg 1440px)에서
    7개 페이지(home, explore, feed, search, profile, login, request-upload)의
    스크린샷을 캡처했습니다.
  </what-built>
  <how-to-verify>
    1. docs/qa-screenshots/ 폴더의 스크린샷 확인
    2. decoded.pen (docs/design-system/decoded.pen)을 열어 디자인 원본 확인
    3. 각 스크린샷과 decoded.pen 비교:
       - Mobile: 375px 레이아웃 확인
       - Desktop: 1280px+ 레이아웃 확인
    4. 발견된 시각적 차이점 기록:
       - 간격(spacing) 차이
       - 타이포그래피(font-size, line-height) 차이
       - 색상(color) 차이
       - 정렬(alignment) 차이
       - 애니메이션 이슈 (있다면)
    5. 수정이 필요한 이슈 목록 작성
  </how-to-verify>
  <resume-signal>
    다음 중 하나로 응답:
    - "approved" - 모든 페이지가 decoded.pen과 일치
    - 이슈 목록 제공 - 수정이 필요한 시각적 차이점 설명
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: 시각적 이슈 수정</name>
  <files>
    (checkpoint 피드백에 따라 결정)
  </files>
  <action>
사용자가 제공한 시각적 이슈 목록을 기반으로 수정 작업 수행:

1. **이슈 분석**:
   - 각 이슈의 원인 파일 식별
   - decoded.pen 참조하여 올바른 값 확인

2. **수정 우선순위**:
   - Critical: 레이아웃 깨짐, 기능 영향
   - Major: 명확한 시각적 차이
   - Minor: 미세한 spacing/color 차이

3. **수정 패턴**:
   - Spacing: Tailwind 클래스 조정 (p-4 → p-3, gap-4 → gap-3)
   - Typography: text-* 클래스 변경, font-* 조정
   - Color: bg-*, text-* 클래스, CSS variable 확인
   - Alignment: flex, justify-*, items-* 조정

4. **수정 후 검증**:
   - 해당 페이지 브라우저에서 확인
   - 모바일/데스크톱 모두 확인
   - 수정된 스크린샷 재캡처 (optional)

5. **README.md 업데이트**:
   - Findings 섹션에 수정 내역 기록
   - Status를 "Fixed"로 변경

Note: 사용자가 "approved"로 응답하면 이 task는 skip
  </action>
  <verify>
    - 보고된 모든 이슈가 수정되었거나 README.md에 문서화됨
    - docs/qa-screenshots/README.md의 Findings 섹션 업데이트됨
  </verify>
  <done>
    모든 시각적 이슈가 수정되었거나 deferred로 문서화됨
  </done>
</task>

</tasks>

<verification>
전체 검증:
```bash
# 테스트 파일 확인
ls packages/web/tests/visual-qa.spec.ts

# 스크린샷 폴더 확인
ls -la docs/qa-screenshots/
ls docs/qa-screenshots/*.png | wc -l

# README 확인
cat docs/qa-screenshots/README.md | head -30
```
</verification>

<success_criteria>
1. packages/web/tests/visual-qa.spec.ts가 4 breakpoint x 7 pages 스크린샷 캡처
2. docs/qa-screenshots/에 28개 이상의 PNG 파일 존재
3. docs/qa-screenshots/README.md에 QA 결과 문서화
4. 사용자가 보고한 시각적 이슈가 수정되었거나 문서화됨
5. 모든 페이지가 decoded.pen과 일치하거나 차이점이 기록됨
</success_criteria>

<output>
After completion, create `.planning/phases/v2-09-documentation-polish/v2-09-03-SUMMARY.md`
</output>
