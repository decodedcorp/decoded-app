---
phase: v2-01-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on: ["v2-01-01"]
files_modified:
  - packages/web/tailwind.config.ts
  - packages/web/app/globals.css
autonomous: true

must_haves:
  truths:
    - "Tailwind config extends with custom design tokens from tokens.ts"
    - "Typography utility classes work: text-hero, text-h1, text-h2, text-h3, text-h4"
    - "Custom spacing scale available in Tailwind classes"
  artifacts:
    - path: "packages/web/tailwind.config.ts"
      provides: "Extended Tailwind config with design system tokens"
      contains: "typography.*.hero|h1|h2|h3|h4"
    - path: "packages/web/app/globals.css"
      provides: "Updated CSS variables and utility classes for typography"
      contains: "--text-hero|--text-h1"
  key_links:
    - from: "packages/web/tailwind.config.ts"
      to: "packages/web/lib/design-system/tokens.ts"
      via: "imports tokens for Tailwind extension"
      pattern: "import.*from.*design-system"
---

<objective>
Configure Tailwind CSS with custom design system tokens for seamless utility class usage.

Purpose: Enable developers to use design tokens via Tailwind utilities (e.g., text-hero, text-h1).
Output: Extended Tailwind config with typography, spacing, and color tokens.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/phases/v2-01-design-system-foundation/v2-01-01-SUMMARY.md
@packages/web/tailwind.config.ts
@packages/web/app/globals.css
@packages/web/lib/design-system/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tailwind config with typography tokens</name>
  <files>packages/web/tailwind.config.ts</files>
  <action>
Update tailwind.config.ts to extend typography with decoded.pen font sizes.

Current config already has some typography variables. Extend to include all heading levels with proper responsive behavior:

```typescript
// In theme.extend.fontSize, add/update:
fontSize: {
  // Existing variables reference
  hero: 'var(--text-hero)',
  'heading-xl': 'var(--text-heading-xl)',
  'heading-lg': 'var(--text-heading-lg)',
  'heading-md': 'var(--text-heading-md)',
  'heading-sm': 'var(--text-heading-sm)',

  // NEW: Semantic heading sizes with line-height tuples
  'h1': ['3rem', { lineHeight: '1.15', fontWeight: '600' }],      // 48px
  'h2': ['2.25rem', { lineHeight: '1.2', fontWeight: '600' }],    // 36px
  'h3': ['1.75rem', { lineHeight: '1.25', fontWeight: '600' }],   // 28px
  'h4': ['1.5rem', { lineHeight: '1.3', fontWeight: '600' }],     // 24px
  'body-lg': ['1.125rem', { lineHeight: '1.75' }],                // 18px
  'body': ['1rem', { lineHeight: '1.5' }],                        // 16px
  'body-sm': ['0.875rem', { lineHeight: '1.5' }],                 // 14px
  'caption': ['0.75rem', { lineHeight: '1.4' }],                  // 12px
},
```

This allows usage like:
- `text-hero` for hero titles
- `text-h1` through `text-h4` for headings
- `text-body`, `text-body-sm`, `text-caption` for body text

Note: Keep existing CSS variable references for backward compatibility.
  </action>
  <verify>
    - Tailwind compiles successfully: `cd packages/web && npx tailwindcss --help`
    - Classes like text-h1, text-h2 are recognized
  </verify>
  <done>
    - Tailwind config extended with semantic heading sizes
    - Line-height and font-weight included in size definitions
    - Backward compatible with existing CSS variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Add typography CSS variables to globals.css</name>
  <files>packages/web/app/globals.css</files>
  <action>
Update globals.css to add semantic typography CSS variables that match decoded.pen specs.

In the :root section, add or update:

```css
/* Typography - Heading Sizes (from decoded.pen) */
--text-h1: 3rem;           /* 48px */
--text-h2: 2.25rem;        /* 36px */
--text-h3: 1.75rem;        /* 28px */
--text-h4: 1.5rem;         /* 24px */

/* Typography - Body Sizes */
--text-body-lg: 1.125rem;  /* 18px */
--text-body: 1rem;         /* 16px */
--text-body-sm: 0.875rem;  /* 14px */
--text-caption: 0.75rem;   /* 12px */

/* Typography - Line Heights */
--leading-hero: 1.1;
--leading-h1: 1.15;
--leading-h2: 1.2;
--leading-h3: 1.25;
--leading-h4: 1.3;
--leading-body: 1.5;
--leading-caption: 1.4;

/* Typography - Font Weights */
--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;
```

These variables complement the existing --text-hero and --text-heading-* variables.
  </action>
  <verify>
    - CSS variables are accessible: check via browser dev tools
    - No syntax errors in globals.css
  </verify>
  <done>
    - Typography CSS variables added for all text sizes
    - Line height and font weight variables defined
    - Compatible with existing variable system
  </done>
</task>

<task type="auto">
  <name>Task 3: Add typography utility layer classes</name>
  <files>packages/web/app/globals.css</files>
  <action>
Add @layer utilities section with semantic typography classes for easy application:

```css
@layer utilities {
  /* Heading typography presets */
  .typography-hero {
    @apply font-serif text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-bold leading-[1.1] tracking-tight;
  }

  .typography-h1 {
    @apply font-serif text-4xl md:text-5xl lg:text-6xl font-semibold leading-[1.15] tracking-tight;
  }

  .typography-h2 {
    @apply font-serif text-3xl md:text-4xl lg:text-5xl font-semibold leading-[1.2] tracking-tight;
  }

  .typography-h3 {
    @apply font-serif text-2xl md:text-3xl font-semibold leading-[1.25];
  }

  .typography-h4 {
    @apply font-serif text-xl md:text-2xl font-semibold leading-[1.3];
  }

  /* Body typography presets */
  .typography-body {
    @apply font-sans text-sm md:text-base leading-relaxed;
  }

  .typography-body-lg {
    @apply font-sans text-base md:text-lg leading-relaxed;
  }

  .typography-small {
    @apply font-sans text-sm leading-normal;
  }

  .typography-caption {
    @apply font-sans text-xs leading-snug text-muted-foreground;
  }

  .typography-overline {
    @apply font-sans text-xs uppercase tracking-widest font-medium;
  }

  .typography-label {
    @apply font-sans text-sm font-medium;
  }
}
```

These utility classes provide a quick way to apply typography without using components.
  </action>
  <verify>
    - Classes can be applied: `<h1 className="typography-hero">Title</h1>`
    - Styles render correctly with responsive sizing
  </verify>
  <done>
    - Typography utility classes added to globals.css
    - All variants from decoded.pen covered
    - Responsive sizing built into utilities
  </done>
</task>

</tasks>

<verification>
1. Run dev server: `cd packages/web && yarn dev`
2. Create test page with all typography classes
3. Verify responsive sizing at different breakpoints
4. Check font families: Playfair Display for headings, Inter for body
</verification>

<success_criteria>
- Tailwind config extended with typography tokens
- CSS variables defined for all typography sizes
- Utility classes available: typography-hero, typography-h1, etc.
- All text styles match decoded.pen specifications
- Responsive sizing works correctly at all breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/v2-01-design-system-foundation/v2-01-03-SUMMARY.md`
</output>
