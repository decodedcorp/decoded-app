---
phase: v2-01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/tokens.ts
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "Design tokens file exports all color, spacing, and typography values from decoded.pen"
    - "Token values match the decoded.pen specification exactly"
    - "Tokens can be imported and used in any component or page"
  artifacts:
    - path: "packages/web/lib/design-system/tokens.ts"
      provides: "Design tokens object with colors, spacing, typography, breakpoints"
      exports: ["colors", "spacing", "typography", "breakpoints", "shadows"]
    - path: "packages/web/lib/design-system/index.ts"
      provides: "Barrel export for design system"
      exports: ["*"]
  key_links:
    - from: "packages/web/lib/design-system/tokens.ts"
      to: "decoded.pen typography section"
      via: "font sizes, weights, line heights from design"
      pattern: "fontSize.*64|48|36|28|24"
---

<objective>
Extract and codify design tokens from decoded.pen into a TypeScript module.

Purpose: Create a single source of truth for design values that can be used across the app and for Tailwind configuration.
Output: Design tokens file with colors, spacing, typography scales, and breakpoints.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@docs/design-system/decoded.pen (typography section lines 800-880)
@docs/design-system/colors.md
@docs/design-system/spacing.md
@docs/design-system/typography.md
@packages/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design tokens file with typography, colors, and spacing</name>
  <files>
    packages/web/lib/design-system/tokens.ts
    packages/web/lib/design-system/index.ts
  </files>
  <action>
Create `packages/web/lib/design-system/` directory and `tokens.ts` file.

Extract from decoded.pen typography section:
- Hero: Playfair Display, 64px, 700 weight
- H1: Playfair Display, 48px, 600 weight
- H2: Playfair Display, 36px, 600 weight
- H3: Playfair Display, 28px, 600 weight
- H4: Playfair Display, 24px, 600 weight
- Body: default text style
- Small: secondary text style
- Caption: fine print style

Structure the tokens as:
```typescript
export const typography = {
  fonts: {
    serif: ['Playfair Display', 'Georgia', 'serif'],
    sans: ['Inter', 'system-ui', 'sans-serif'],
    mono: ['JetBrains Mono', 'Consolas', 'monospace'],
  },
  sizes: {
    hero: { fontSize: '64px', lineHeight: '1.1', fontWeight: '700', fontFamily: 'serif' },
    h1: { fontSize: '48px', lineHeight: '1.15', fontWeight: '600', fontFamily: 'serif' },
    h2: { fontSize: '36px', lineHeight: '1.2', fontWeight: '600', fontFamily: 'serif' },
    h3: { fontSize: '28px', lineHeight: '1.25', fontWeight: '600', fontFamily: 'serif' },
    h4: { fontSize: '24px', lineHeight: '1.3', fontWeight: '600', fontFamily: 'serif' },
    body: { fontSize: '16px', lineHeight: '1.5', fontWeight: '400', fontFamily: 'sans' },
    small: { fontSize: '14px', lineHeight: '1.5', fontWeight: '400', fontFamily: 'sans' },
    caption: { fontSize: '12px', lineHeight: '1.4', fontWeight: '400', fontFamily: 'sans' },
  },
} as const;

export const colors = {
  // Reference semantic colors from globals.css CSS variables
  primary: 'var(--primary)',
  secondary: 'var(--secondary)',
  // ... etc
} as const;

export const spacing = {
  // Based on 4px base unit from docs/design-system/spacing.md
  0: '0px',
  0.5: '2px',
  1: '4px',
  2: '8px',
  // ... following the scale
} as const;

export const breakpoints = {
  sm: '640px',
  md: '768px',
  lg: '1024px',
  xl: '1280px',
  '2xl': '1536px',
} as const;

export const shadows = {
  // From globals.css shadow values
} as const;
```

Also create `index.ts` as barrel export.
  </action>
  <verify>
    - File exists at packages/web/lib/design-system/tokens.ts
    - TypeScript compilation succeeds (no type errors)
    - Exports can be imported: `import { typography } from '@/lib/design-system'`
  </verify>
  <done>
    - Design tokens file exports typography, colors, spacing, breakpoints, shadows
    - All values match decoded.pen and existing docs specifications
    - File is properly typed with `as const` for type inference
  </done>
</task>

<task type="auto">
  <name>Task 2: Add responsive typography scales</name>
  <files>packages/web/lib/design-system/tokens.ts</files>
  <action>
Extend the typography tokens to include responsive variants based on docs/design-system/typography.md responsive sizes table:

```typescript
export const responsiveTypography = {
  pageTitle: {
    mobile: 'text-2xl',   // 24px
    tablet: 'text-3xl',   // 30px
    desktop: 'text-4xl',  // 36px
  },
  sectionTitle: {
    mobile: 'text-xl',    // 20px
    tablet: 'text-2xl',   // 24px
    desktop: 'text-2xl',  // 24px
  },
  cardTitle: {
    mobile: 'text-base',  // 16px
    tablet: 'text-lg',    // 18px
    desktop: 'text-lg',   // 18px
  },
  body: {
    mobile: 'text-sm',    // 14px
    tablet: 'text-base',  // 16px
    desktop: 'text-base', // 16px
  },
  caption: {
    mobile: 'text-xs',    // 12px
    tablet: 'text-xs',    // 12px
    desktop: 'text-sm',   // 14px
  },
} as const;
```

Add helper type for TypeScript consumers.
  </action>
  <verify>
    - responsiveTypography export exists
    - Types are properly inferred for all responsive variants
  </verify>
  <done>
    - Responsive typography scales defined for mobile/tablet/desktop
    - Values match docs/design-system/typography.md responsive sizes table
  </done>
</task>

</tasks>

<verification>
1. Run TypeScript check: `cd packages/web && npx tsc --noEmit`
2. Verify import works: Create temp test file importing from @/lib/design-system
3. Check all exports are present: typography, colors, spacing, breakpoints, shadows, responsiveTypography
</verification>

<success_criteria>
- Design tokens file created with all required exports
- TypeScript types properly inferred with `as const`
- Values match decoded.pen and documentation specifications
- File can be imported from any component
</success_criteria>

<output>
After completion, create `.planning/phases/v2-01-design-system-foundation/v2-01-01-SUMMARY.md`
</output>
