---
phase: v2-07-search-image-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/search/SearchOverlay.tsx
  - packages/web/lib/components/search/SearchInput.tsx
  - packages/web/lib/components/search/SearchTabs.tsx
  - packages/web/lib/components/search/RecentSearches.tsx
  - packages/web/lib/components/search/index.ts
  - packages/web/app/search/SearchPageClient.tsx
autonomous: true

must_haves:
  truths:
    - "User can tap search to open full-screen overlay"
    - "User sees recent searches before typing (max 5-10 items)"
    - "User can clear individual recent searches or all"
    - "User sees live results with debounced keystroke updates"
    - "Search tabs show result counts (All, People, Media, Items)"
    - "Active tab has sliding underline indicator"
  artifacts:
    - path: "packages/web/lib/components/search/SearchOverlay.tsx"
      provides: "Full-screen search overlay with slide-in animation"
      min_lines: 100
    - path: "packages/web/lib/components/search/RecentSearches.tsx"
      provides: "Recent searches list with clear functionality"
      min_lines: 50
  key_links:
    - from: "packages/web/app/search/SearchPageClient.tsx"
      to: "SearchOverlay"
      via: "Import and render SearchOverlay"
      pattern: "SearchOverlay"
    - from: "SearchTabs.tsx"
      to: "motion.div"
      via: "Sliding underline indicator"
      pattern: "layoutId.*underline"
---

<objective>
Implement Search page with full-screen overlay UX and decoded.pen styling

Purpose: Users can discover content through an intuitive full-screen search experience with recent searches, live results, and tabbed filtering per decoded.pen design specifications.

Output:
- SearchOverlay component with slide-in animation (from top)
- RecentSearches component with individual/bulk clear
- Updated SearchInput with decoded.pen styling (361px width, 48px height, rounded-12)
- Updated SearchTabs with sliding underline indicator and result counts
- Search page integration with overlay mode
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/phases/v2-07-search-image-detail/v2-07-CONTEXT.md

# Existing search components
@packages/web/lib/components/search/SearchInput.tsx
@packages/web/lib/components/search/SearchTabs.tsx
@packages/web/lib/components/search/index.ts
@packages/web/app/search/SearchPageClient.tsx

# Design patterns from v2-06
@.planning/phases/v2-06-feed-profile/v2-06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchOverlay component with slide-in animation</name>
  <files>packages/web/lib/components/search/SearchOverlay.tsx</files>
  <action>
Create full-screen search overlay component per CONTEXT.md decisions:

1. Layout structure:
   - Fixed position covering entire viewport (inset-0)
   - z-50 for proper stacking above other content
   - bg-background for solid backdrop

2. Animation (Framer Motion):
   - Slide in from top: initial={{ y: '-100%' }} animate={{ y: 0 }} exit={{ y: '-100%' }}
   - Duration 0.3s with ease-out timing
   - Use AnimatePresence for enter/exit transitions

3. Header section:
   - Back button (ArrowLeft icon) on left, triggers onClose
   - SearchInput in center, auto-focused when overlay opens
   - Both items centered vertically with gap-4

4. Close behaviors:
   - Back button click
   - Tap outside search content area (click on backdrop)
   - Escape key press

5. Content area:
   - Shows RecentSearches when query is empty
   - Shows SearchTabs + SearchResults when query has value
   - Proper padding: px-4 py-6, max-w-4xl mx-auto for content width

Props interface:
```typescript
interface SearchOverlayProps {
  isOpen: boolean;
  onClose: () => void;
  initialQuery?: string;
}
```

Use Framer Motion's AnimatePresence pattern similar to v2-06-03 tab transitions.
  </action>
  <verify>
File exists at packages/web/lib/components/search/SearchOverlay.tsx
Component exports SearchOverlay with AnimatePresence animation
Verify motion.div has y transform animation
  </verify>
  <done>
SearchOverlay component renders full-screen with slide-in animation
Back button and escape key close the overlay
Content area shows recent searches or results based on query state
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RecentSearches component with clear functionality</name>
  <files>packages/web/lib/components/search/RecentSearches.tsx</files>
  <action>
Create recent searches display component per CONTEXT.md decisions:

1. Layout:
   - Section header: "Recent Searches" with "Clear all" button on right
   - List of recent search items (max 5-10, use 8 as default)
   - Each item shows search term + X button to remove

2. Data source:
   - Use existing useRecentSearchesStore from @decoded/shared
   - Store already has: searches[], addSearch(), removeSearch(), clearAll()

3. Item styling:
   - Hover state for clickable items (bg-accent/50)
   - X button appears on hover or always visible on mobile
   - Search icon (Clock or History icon) left of text
   - Text truncated if too long

4. Interactions:
   - Click on item: trigger search with that term (call onSelect prop)
   - Click X on item: remove from recent (call removeSearch)
   - Click "Clear all": clear all recent searches (call clearAll)

5. Empty state:
   - Show subtle message when no recent searches: "No recent searches"

Props interface:
```typescript
interface RecentSearchesProps {
  onSelect: (query: string) => void;
  maxItems?: number;
}
```

Style with existing design tokens (text-muted-foreground, bg-accent, etc.).
  </action>
  <verify>
File exists at packages/web/lib/components/search/RecentSearches.tsx
Component imports useRecentSearchesStore
Verify clear functionality exists (individual and bulk)
  </verify>
  <done>
RecentSearches shows list from store with max limit
Individual X buttons remove single items
"Clear all" button clears entire list
Clicking item triggers onSelect callback
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SearchTabs with sliding underline indicator</name>
  <files>packages/web/lib/components/search/SearchTabs.tsx</files>
  <action>
Enhance SearchTabs component per CONTEXT.md decisions:

1. Replace static underline with animated sliding indicator:
   - Use Framer Motion's layoutId for shared layout animation
   - Underline slides horizontally between tabs
   - Use motion.span with layoutId="search-tab-underline"
   - 2px height, primary color per decoded.pen spec

2. Update tab styling:
   - Border-bottom 1px #3D3D3D (use border-border)
   - Tabs: All, People, Media, Items (already correct)
   - Show result counts in parentheses: "Items (24)"
   - Active tab text: text-foreground
   - Inactive tab text: text-muted-foreground with hover:text-foreground

3. Animation configuration:
   - transition={{ type: "spring", stiffness: 500, damping: 30 }}
   - Or use simple tween: transition={{ duration: 0.2 }}

4. Keep existing functionality:
   - activeTab from store
   - setActiveTab on click
   - facets prop for counts
   - All existing ARIA attributes

Code pattern for sliding underline:
```tsx
{isActive && (
  <motion.span
    layoutId="search-tab-underline"
    className="absolute bottom-0 left-0 right-0 h-0.5 bg-primary"
    transition={{ type: "spring", stiffness: 500, damping: 30 }}
  />
)}
```
  </action>
  <verify>
SearchTabs imports motion from framer-motion
Verify layoutId="search-tab-underline" exists on active indicator
Tab counts display in format "Label (count)"
  </verify>
  <done>
Tab underline slides horizontally when switching tabs
Result counts shown on all tabs with available data
Animation feels smooth and responsive (spring physics)
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Component structure check:
   - `ls packages/web/lib/components/search/` shows SearchOverlay.tsx, RecentSearches.tsx
   - SearchTabs.tsx has motion import and layoutId

2. Visual verification (manual):
   - Open search overlay: full screen with slide-in animation
   - Recent searches shown when no query
   - Tab underline slides between tabs
   - Result counts appear on tabs

3. Lint check:
   - `cd packages/web && yarn lint` passes without errors
</verification>

<success_criteria>
- SearchOverlay opens full-screen with slide-in from top animation
- RecentSearches displays stored queries with clear functionality
- SearchTabs has animated sliding underline indicator
- Tab result counts display in "Label (count)" format
- All animations are smooth (0.2-0.3s duration)
- ESLint passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/v2-07-search-image-detail/v2-07-01-SUMMARY.md`
</output>
