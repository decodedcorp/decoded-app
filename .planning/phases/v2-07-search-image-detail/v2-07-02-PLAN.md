---
phase: v2-07-search-image-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/detail/HeroSection.tsx
  - packages/web/lib/components/detail/ImageDetailPage.tsx
  - packages/web/lib/components/detail/ImageDetailContent.tsx
  - packages/web/lib/components/detail/Lightbox.tsx
autonomous: true

must_haves:
  truths:
    - "User sees hero image with parallax effect on scroll"
    - "User sees hero title fade out as they scroll past"
    - "User can tap hero image to open fullscreen lightbox"
    - "Action buttons (close, share, report) stay fixed at top while scrolling"
    - "Desktop layout uses same vertical structure but wider container"
    - "Mobile hero has max 426px height per decoded.pen spec"
  artifacts:
    - path: "packages/web/lib/components/detail/HeroSection.tsx"
      provides: "Hero section with parallax and title fade animations"
      contains: "ScrollTrigger"
    - path: "packages/web/lib/components/detail/Lightbox.tsx"
      provides: "Fullscreen image lightbox with zoom capability"
      min_lines: 80
  key_links:
    - from: "HeroSection.tsx"
      to: "gsap/ScrollTrigger"
      via: "Parallax scroll animation"
      pattern: "ScrollTrigger"
    - from: "ImageDetailPage.tsx"
      to: "Lightbox"
      via: "Hero tap opens lightbox"
      pattern: "setShowLightbox"
---

<objective>
Implement Image Detail page with decoded.pen design and GSAP scroll animations

Purpose: Users experience an immersive image detail view with parallax hero, fading title, fixed action buttons, and fullscreen lightbox - matching decoded.pen design specifications.

Output:
- Updated HeroSection with decoded.pen styling (426px mobile height, gradient overlay, Playfair Display 64px title)
- Enhanced scroll animations (hero parallax, title fade-out)
- New Lightbox component for fullscreen zoomable image view
- Fixed action buttons that persist while scrolling
- Responsive layout (same vertical structure, wider on desktop)
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/phases/v2-07-search-image-detail/v2-07-CONTEXT.md

# Existing detail components
@packages/web/lib/components/detail/HeroSection.tsx
@packages/web/lib/components/detail/ImageDetailPage.tsx
@packages/web/lib/components/detail/ImageDetailContent.tsx

# Design system patterns
@packages/web/lib/tokens/typography.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HeroSection with decoded.pen styling and enhanced scroll animations</name>
  <files>packages/web/lib/components/detail/HeroSection.tsx</files>
  <action>
Update HeroSection per decoded.pen spec and CONTEXT.md decisions:

1. Mobile height constraint:
   - Change from `h-screen` to responsive height
   - Mobile: `h-[426px]` (fixed per decoded.pen)
   - Desktop: `h-[60vh] max-h-[600px]` for balanced proportions
   - Keep `min-h-[250px]` for modal mode

2. Typography update (decoded.pen spec):
   - Title: font-serif (Playfair Display), font-bold
   - Size: text-5xl md:text-6xl lg:text-7xl (approx 64px at lg)
   - Color: text-white
   - Position: bottom of hero with gradient overlay

3. Gradient overlay update:
   - Keep existing: `bg-gradient-to-t from-black/80 via-black/40 to-transparent`
   - This matches decoded.pen style

4. Enhanced scroll animations (GSAP ScrollTrigger):
   - Hero image parallax: image moves slower than scroll (ratio ~0.3)
     ```typescript
     gsap.to(imageRef.current, {
       y: 100,
       ease: "none",
       scrollTrigger: {
         trigger: heroRef.current,
         start: "top top",
         end: "bottom top",
         scrub: true,
       },
     });
     ```
   - Title fade-out: opacity 1 → 0 as user scrolls past
     ```typescript
     gsap.to(titleRef.current, {
       opacity: 0,
       y: -50,
       ease: "none",
       scrollTrigger: {
         trigger: heroRef.current,
         start: "top top",
         end: "30% top",
         scrub: true,
       },
     });
     ```

5. Add click handler for lightbox:
   - onClick prop to parent for opening lightbox
   - cursor-pointer on image
   - Add aria-label="View fullscreen"

6. Keep Ken Burns entrance animation (already exists).

Update class logic:
```tsx
className={`relative w-full overflow-hidden ${
  isModal
    ? "h-[45vh] min-h-[250px]"
    : "h-[426px] md:h-[60vh] md:max-h-[600px]"
}`}
```
  </action>
  <verify>
HeroSection has mobile height 426px (h-[426px])
ScrollTrigger creates both parallax and title fade animations
onClick handler exists for lightbox trigger
  </verify>
  <done>
Hero displays at 426px on mobile, 60vh (max 600px) on desktop
Parallax effect moves image slower than scroll
Title fades out as user scrolls past hero section
Click on hero image triggers lightbox callback
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Lightbox component for fullscreen image view</name>
  <files>packages/web/lib/components/detail/Lightbox.tsx</files>
  <action>
Create fullscreen image lightbox component per CONTEXT.md decisions:

1. Layout:
   - Fixed position, full viewport (inset-0)
   - z-[60] to stack above everything including header
   - bg-black for dark backdrop

2. Image display:
   - Centered with object-contain
   - Allow pinch-zoom on touch devices (touch-action: pinch-zoom)
   - Double-tap to zoom toggle (optional enhancement)

3. Controls:
   - Close button (X) top-right, white icon on semi-transparent bg
   - Use same style as action buttons: rounded-full bg-white/10 backdrop-blur
   - Close on backdrop click (outside image area)
   - Close on Escape key press

4. Animation (Framer Motion):
   - Fade in/out: initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
   - Image scale: initial={{ scale: 0.9 }} animate={{ scale: 1 }}
   - Duration 0.3s

5. Zoom functionality (Claude's discretion):
   - Simple approach: CSS transform scale on double-tap/click
   - useState for isZoomed
   - When zoomed, allow pan with transform translate
   - Or use a simple image zoom library if available in deps

Props interface:
```typescript
interface LightboxProps {
  isOpen: boolean;
  onClose: () => void;
  imageUrl: string;
  alt?: string;
}
```

Keep implementation simple - basic zoom toggle is sufficient per CONTEXT.md "Claude's discretion" note.
  </action>
  <verify>
File exists at packages/web/lib/components/detail/Lightbox.tsx
Component has AnimatePresence animation
Close button and backdrop click both trigger onClose
Escape key listener closes lightbox
  </verify>
  <done>
Lightbox opens fullscreen with fade animation
Image displays centered with object-contain
Close via X button, backdrop click, or Escape key
Basic zoom functionality on tap/click
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ImageDetailPage with fixed action buttons and lightbox integration</name>
  <files>packages/web/lib/components/detail/ImageDetailPage.tsx</files>
  <action>
Update ImageDetailPage per CONTEXT.md decisions:

1. Add lightbox state:
   ```typescript
   const [showLightbox, setShowLightbox] = useState(false);
   ```

2. Pass onClick to HeroSection (via ImageDetailContent):
   - Add prop for onHeroClick to ImageDetailContent
   - Wire it up to setShowLightbox(true)

3. Action buttons already fixed at top (existing code is correct):
   - Keep: `fixed right-4 top-4 z-50`
   - Buttons: ReportErrorButton, Share, Close
   - Current styling is good: `bg-background/80 backdrop-blur-sm`

4. Update action button styling to match decoded.pen:
   - Size: h-10 w-10 (already correct)
   - Add subtle hover effect: hover:bg-background/90 (already exists)
   - Consider adding transition-transform hover:scale-105

5. Render Lightbox:
   ```tsx
   <Lightbox
     isOpen={showLightbox}
     onClose={() => setShowLightbox(false)}
     imageUrl={image.image_url || ''}
     alt={`Image ${image.id}`}
   />
   ```

6. Import Lightbox component:
   ```typescript
   import { Lightbox } from "./Lightbox";
   ```

7. Update exports in detail/index.ts if it exists.
  </action>
  <verify>
ImageDetailPage imports Lightbox component
useState for showLightbox exists
Lightbox rendered conditionally based on showLightbox state
Action buttons have fixed positioning (already correct)
  </verify>
  <done>
Tapping hero image opens fullscreen lightbox
Lightbox closes via X, backdrop click, or Escape
Action buttons (close, share, report) stay fixed at top during scroll
All interactions work on both mobile and desktop
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. File structure check:
   - `ls packages/web/lib/components/detail/` shows Lightbox.tsx
   - HeroSection.tsx has updated height classes

2. Animation verification (grep):
   - `grep -n "ScrollTrigger" packages/web/lib/components/detail/HeroSection.tsx`
   - `grep -n "showLightbox" packages/web/lib/components/detail/ImageDetailPage.tsx`

3. Visual verification (manual):
   - Hero height is 426px on mobile viewport
   - Scrolling shows parallax effect and title fade
   - Tapping hero opens lightbox
   - Action buttons stay fixed while scrolling

4. Lint check:
   - `cd packages/web && yarn lint` passes
</verification>

<success_criteria>
- HeroSection displays at 426px mobile / 60vh desktop height
- Parallax scroll effect on hero image (moves slower than scroll)
- Title fades out as user scrolls past hero
- Lightbox opens on hero tap with fullscreen image
- Lightbox closes via X, backdrop, or Escape key
- Action buttons remain fixed at top during scroll
- ESLint passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/v2-07-search-image-detail/v2-07-02-SUMMARY.md`
</output>
