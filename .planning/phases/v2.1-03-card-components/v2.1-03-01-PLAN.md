---
phase: v2.1-03-card-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/artist-card.tsx
  - packages/web/lib/design-system/stat-card.tsx
  - packages/web/lib/design-system/index.ts
  - packages/web/lib/utils/format.ts
autonomous: true

must_haves:
  truths:
    - "User can view artist profiles in card format with avatar and post count"
    - "User sees statistics displayed in visually consistent stat cards"
    - "Large numbers display in abbreviated format (1K, 1M)"
    - "Stat cards show trend indicators with arrows"
  artifacts:
    - path: "packages/web/lib/design-system/artist-card.tsx"
      provides: "ArtistCard component with avatar, name, post count"
      exports: ["ArtistCard", "ArtistCardSkeleton", "ArtistCardProps"]
    - path: "packages/web/lib/design-system/stat-card.tsx"
      provides: "StatCard component with value, label, trend indicator"
      exports: ["StatCard", "StatCardSkeleton", "StatCardProps"]
    - path: "packages/web/lib/utils/format.ts"
      provides: "formatStatValue utility for number abbreviation"
      exports: ["formatStatValue"]
  key_links:
    - from: "packages/web/lib/design-system/artist-card.tsx"
      to: "next/image"
      via: "Next.js Image for avatar"
      pattern: "import.*Image.*from.*next/image"
    - from: "packages/web/lib/design-system/artist-card.tsx"
      to: "next/link"
      via: "Link for profile navigation"
      pattern: "import.*Link.*from.*next/link"
    - from: "packages/web/lib/design-system/stat-card.tsx"
      to: "packages/web/lib/utils/format.ts"
      via: "formatStatValue import"
      pattern: "import.*formatStatValue.*from"
---

<objective>
Create ArtistCard and StatCard components for the decoded.pen design system.

Purpose: Enable consistent display of artist profiles in card grids and statistics in dashboard-style layouts. These cards form the foundation for profile sections, artist grids, and analytics displays.

Output:
- ArtistCard component with avatar, name, post count, click-to-profile navigation
- StatCard component with value, label, trend indicator (arrow + percentage)
- formatStatValue utility for number abbreviation (1K, 1M)
- Skeleton components for loading states
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.1-03-card-components/v2.1-03-CONTEXT.md
@.planning/phases/v2.1-03-card-components/v2.1-03-RESEARCH.md

# Reference existing design system patterns
@packages/web/lib/design-system/card.tsx
@packages/web/lib/design-system/product-card.tsx
@packages/web/lib/design-system/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formatStatValue utility</name>
  <files>packages/web/lib/utils/format.ts</files>
  <action>
Create or extend format.ts with formatStatValue function for number abbreviation:

```typescript
/**
 * Format large numbers with abbreviations
 * 1,234 -> "1.2K", 1,234,567 -> "1.2M"
 * Uses Intl.NumberFormat for proper locale handling
 */
export function formatStatValue(value: number): string {
  if (value >= 1_000_000) {
    return new Intl.NumberFormat("en", {
      notation: "compact",
      maximumFractionDigits: 1,
    }).format(value);
  }
  if (value >= 1_000) {
    return new Intl.NumberFormat("en", {
      notation: "compact",
      maximumFractionDigits: 1,
    }).format(value);
  }
  return value.toLocaleString();
}
```

If format.ts exists, add the function. If not, create the file with proper JSDoc documentation.
  </action>
  <verify>
TypeScript compilation passes: `cd packages/web && npx tsc --noEmit`
  </verify>
  <done>
formatStatValue function exists and correctly abbreviates: 1234 -> "1.2K", 1234567 -> "1.2M", 999 -> "999"
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ArtistCard component</name>
  <files>packages/web/lib/design-system/artist-card.tsx</files>
  <action>
Create artist-card.tsx following the established CVA pattern from product-card.tsx:

Structure:
- Avatar image using Next.js Image with fallback (initials or placeholder)
- Artist name (line-clamp-1)
- Post count display
- Click navigates to /profile/[id]

Props interface:
```typescript
interface ArtistCardProps {
  id: string;
  name: string;
  avatarUrl?: string;
  postCount: number;
  className?: string;
}
```

Implementation notes:
- Use forwardRef pattern (consistent with existing components)
- Avatar: 64px circle with Image component, sizes prop for optimization
- Fallback: Show first letter of name in centered circle with bg-muted
- Layout: Flex column, centered, min-width 120px
- Wrap in Next.js Link to /profile/{id}
- Hover state: subtle scale or shadow transition

Include ArtistCardSkeleton:
- Circular placeholder for avatar
- Text placeholders for name and count
- animate-pulse for shimmer effect
  </action>
  <verify>
TypeScript compilation passes: `cd packages/web && npx tsc --noEmit`
  </verify>
  <done>
ArtistCard component renders avatar, name, post count and links to profile page. Skeleton variant provides loading state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create StatCard component and update exports</name>
  <files>packages/web/lib/design-system/stat-card.tsx, packages/web/lib/design-system/index.ts</files>
  <action>
Create stat-card.tsx following CVA pattern:

Props interface:
```typescript
interface StatCardProps {
  label: string;
  value: number;
  trend?: {
    direction: "up" | "down" | "neutral";
    value: number; // percentage
  };
  onClick?: () => void;
  className?: string;
}
```

Implementation:
- Import formatStatValue from utils/format
- Card layout: bg-card, rounded-xl, p-4 md:p-6, border border-border
- Value display: text-2xl md:text-3xl font-bold, use formatStatValue
- Label: text-xs md:text-sm text-muted-foreground
- Trend indicator (if provided):
  - Use Lucide ArrowUp/ArrowDown icons (or TrendingUp/TrendingDown)
  - Green for up, red for down, gray for neutral
  - Format: Arrow icon + "+15%" or "-5%"
- If onClick provided: cursor-pointer, hover:bg-accent/50 transition
- Accessible: role="button" if clickable, aria-label

Include StatCardSkeleton for loading state.

Update index.ts to export:
- ArtistCard, ArtistCardSkeleton, ArtistCardProps
- StatCard, StatCardSkeleton, StatCardProps
  </action>
  <verify>
1. TypeScript compilation passes: `cd packages/web && npx tsc --noEmit`
2. Imports work: Create a temp test file or check barrel export resolves
  </verify>
  <done>
StatCard displays formatted value with optional trend indicator. Both ArtistCard and StatCard are exported from design-system barrel.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd packages/web && npx tsc --noEmit` passes
2. All components exported from @/lib/design-system
3. Number formatting: 1234 displays as "1.2K", 1234567 as "1.2M"
4. ArtistCard links to profile, shows avatar with fallback
5. StatCard shows trend arrow when trend prop provided
</verification>

<success_criteria>
- ArtistCard component with avatar, name, post count, profile link navigation
- StatCard component with formatted value, label, optional trend indicator
- formatStatValue utility correctly abbreviates large numbers
- Skeleton components for both cards
- All components follow CVA + forwardRef pattern
- Exported from design-system barrel import
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-03-card-components/v2.1-03-01-SUMMARY.md`
</output>
