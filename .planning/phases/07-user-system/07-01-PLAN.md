---
phase: v4-07-screen-specs-user-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/screens/user/SCR-USER-01-login.md
  - specs/screens/user/SCR-USER-02-profile.md
  - specs/screens/user/SCR-USER-03-earnings.md
autonomous: true

must_haves:
  truths:
    - "SCR-USER-01 documents the login page route (/login) with Google OAuth only (LoginCard renders only Google button), DomeGallery animated background, and authStore state transition (unauthenticated -> authenticated)"
    - "SCR-USER-01 includes guest login flow (isGuest=true, router.push '/'), error display (red block), and post-login redirect hardcoded to '/'"
    - "SCR-USER-01 documents that no 'already authenticated' redirect exists — authenticated users can revisit /login"
    - "SCR-USER-02 documents the profile page route (/profile) with real data (useMe, useUserStats) and mock data (badges, rankings, follow stats, all 4 activity tab contents) clearly separated"
    - "SCR-USER-02 includes ProfileEditModal as fully implemented, auth pattern as API-error-driven (401 -> error component, not login redirect), and always own-profile only (no /profile/[userId])"
    - "SCR-USER-02 documents all 4 activity tabs (PostsGrid, SpotsList, SolutionsList, SavedGrid) with MOCK data status marked"
    - "SCR-USER-03 documents earnings as NOT-IMPL — no route, no component, no API — with StatsCards.tsx alert('수익 페이지는 아직 구현되지 않았습니다.') as the only reference point"
    - "All file paths in all specs are verified against actual filesystem"
    - "Auth-conditional rendering patterns documented: middleware protects /admin/:path* only; header uses user ? authenticated : Login link; profile uses API-error-driven auth (401)"
  artifacts:
    - path: "specs/screens/user/SCR-USER-01-login.md"
      provides: "Login screen spec with Google OAuth flow, authStore state transitions, DomeGallery background"
      contains: "Component Map"
    - path: "specs/screens/user/SCR-USER-02-profile.md"
      provides: "Profile screen spec with activity tabs, badges, stats, real vs mock data inventory"
      contains: "Activity Tabs"
    - path: "specs/screens/user/SCR-USER-03-earnings.md"
      provides: "Earnings stub spec documenting NOT-IMPL status with total_points data model reference"
      contains: "NOT-IMPL"
  key_links:
    - from: "SCR-USER-01"
      to: "SCR-USER-02"
      via: "cross-reference: post-login redirect to '/' then header links to /profile"
    - from: "SCR-USER-02"
      to: "SCR-USER-03"
      via: "cross-reference: StatsCards Points tap triggers alert (earnings NOT-IMPL)"
    - from: "All SCR-USER-*"
      to: "specs/flows/FLW-04-user.md"
      via: "flow context cross-reference for auth state transitions"
    - from: "All SCR-USER-*"
      to: "specs/_shared/store-map.md"
      via: "authStore field definitions"
---

<objective>
Write screen specs SCR-USER-01 (Login), SCR-USER-02 (Profile), and SCR-USER-03 (Earnings) for the User System bundle, with authStore state shape and auth-conditional rendering patterns verified from actual code.

Purpose: Enable AI agents to modify user system screens with correct file paths, understand the authStore state machine (selectIsAuthenticated vs selectIsLoggedIn), and know which features are real vs mock vs NOT-IMPL.
Output: Three spec files in `specs/screens/user/` following v4.0 screen spec template with EARS requirements, mobile+desktop wireframes, authStore state tables, and real/mock data inventory.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Templates and shared foundation
@specs/shared/templates/screen-spec-template.md
@specs/_shared/component-registry.md
@specs/_shared/store-map.md
@specs/_shared/data-models.md
@specs/_shared/api-contracts.md

# Flow context (FLW-04 is the PRIMARY flow for User System)
@specs/flows/FLW-04-user.md

# Phase context (user decisions from discuss-phase)
@.planning/phases/07-user-system/07-CONTEXT.md

# ============================================================
# Login route and components:
# ============================================================
@packages/web/app/login/page.tsx
@packages/web/app/login/layout.tsx
@packages/web/app/login/LoginBackground.tsx
@packages/web/lib/components/auth/LoginCard.tsx
@packages/web/lib/components/auth/OAuthButton.tsx
@packages/web/lib/components/auth/AuthProvider.tsx

# ============================================================
# Profile route and components:
# ============================================================
@packages/web/app/profile/page.tsx
@packages/web/app/profile/ProfileClient.tsx
@packages/web/lib/components/profile/ProfileHeader.tsx
@packages/web/lib/components/profile/ProfileBio.tsx
@packages/web/lib/components/profile/FollowStats.tsx
@packages/web/lib/components/profile/StatsCards.tsx
@packages/web/lib/components/profile/BadgeGrid.tsx
@packages/web/lib/components/profile/BadgeModal.tsx
@packages/web/lib/components/profile/RankingList.tsx
@packages/web/lib/components/profile/ActivityTabs.tsx
@packages/web/lib/components/profile/ActivityContent.tsx
@packages/web/lib/components/profile/PostsGrid.tsx
@packages/web/lib/components/profile/SpotsList.tsx
@packages/web/lib/components/profile/SolutionsList.tsx
@packages/web/lib/components/profile/SavedGrid.tsx
@packages/web/lib/components/profile/ProfileEditModal.tsx
@packages/web/lib/components/profile/ProfileDesktopLayout.tsx

# ============================================================
# State, hooks, API:
# ============================================================
@packages/web/lib/stores/authStore.ts
@packages/web/lib/stores/profileStore.ts
@packages/web/lib/hooks/useProfile.ts

# ============================================================
# Design system components used:
# ============================================================
@packages/web/lib/design-system/profile-header-card.tsx
@packages/web/lib/design-system/stat-card.tsx
@packages/web/lib/design-system/badge.tsx
@packages/web/lib/design-system/ranking-item.tsx
@packages/web/lib/design-system/leader-item.tsx
@packages/web/lib/design-system/tabs.tsx
@packages/web/lib/design-system/oauth-button.tsx
@packages/web/lib/design-system/guest-button.tsx
@packages/web/lib/design-system/login-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCR-USER-01-login.md</name>
  <files>specs/screens/user/SCR-USER-01-login.md</files>
  <action>
Create directory `specs/screens/user/` if it doesn't exist.

Write `SCR-USER-01-login.md` following screen-spec-template format with these specifics:

**PRE-VERIFIED FACTS (from actual code — do NOT re-investigate, use directly):**
- Route: `/login` — page at `packages/web/app/login/page.tsx`
- Layout: `packages/web/app/login/layout.tsx` — centered card layout with full-bleed background
- LoginCard renders Google OAuth ONLY — no Kakao or Apple buttons in current UI
- DomeGallery animated background (Three.js dome with rotating images)
- LoginBackground wraps DomeGallery at `packages/web/app/login/LoginBackground.tsx`
- authStore state shape: user|null, isAdmin, isGuest, isLoading, isInitialized, loadingProvider, error
- Two key selectors: selectIsAuthenticated (guest+logged-in) vs selectIsLoggedIn (logged-in only)
- Google OAuth: signInWithOAuth({provider:'google'}) → Supabase redirects → callback → authStore.setUser
- Guest login: guestLogin() → isGuest=true → router.push("/")
- Post-login redirect: hardcoded to "/" (not configurable, no returnUrl)
- Error display: red block with error message text
- No "already authenticated" redirect — users can visit /login even when logged in
- Middleware only protects /admin/:path* — /login has NO auth middleware

**Structure:**
1. **Header** — ID: SCR-USER-01, Route: `/login`, Status: implemented
2. **Purpose** — User authenticates via Google OAuth or continues as guest
3. **Component Map** — Table with verified paths:
   - Page: LoginPage — `packages/web/app/login/page.tsx`
   - Layout: LoginLayout — `packages/web/app/login/layout.tsx`
   - LoginBackground — `packages/web/app/login/LoginBackground.tsx` — Three.js DomeGallery
   - LoginCard — `packages/web/lib/components/auth/LoginCard.tsx` — card container
   - DS OAuthButton — `packages/web/lib/design-system/oauth-button.tsx` — Google button
   - DS GuestButton — `packages/web/lib/design-system/guest-button.tsx` — guest entry
   - DS LoginCard — `packages/web/lib/design-system/login-card.tsx` — card UI shell
   - AuthProvider — `packages/web/lib/components/auth/AuthProvider.tsx` — session listener
   - Verify ALL paths with `ls`

4. **Layout — Mobile** — ASCII wireframe:
   - Full-bleed DomeGallery background (Three.js animated dome)
   - Centered LoginCard: Logo + "decoded" text + Google OAuth button + Guest button + error block (if error)
   - Keep wireframe concise (8-12 lines)

5. **Layout — Desktop** — Delta from mobile:
   - Same centered card, larger viewport for DomeGallery
   - Card max-width constraint

6. **authStore State Transitions** — State table:
   | State | authStore Fields | UI |
   - initial: isInitialized=false, isLoading=true → loading state
   - unauthenticated: user=null, isGuest=false → show login card
   - authenticating: loadingProvider='google' → OAuthButton loading spinner
   - authenticated: user=User, isGuest=false → redirect to "/"
   - guest: user=null, isGuest=true → redirect to "/"
   - error: error=string → red error block below buttons

   **Key Selectors (must include as dedicated table):**
   | Selector | Formula | Use |
   - selectIsAuthenticated: !!user || isGuest → true for both logged-in AND guest users (used for "can browse" checks)
   - selectIsLoggedIn: !!user → true ONLY for real OAuth users (used for features requiring real account)
   - selectIsGuest: isGuest → true only for guest mode

7. **Google OAuth Flow** — Ordered steps:
   1. User taps Google button → OAuthButton onClick
   2. authStore.setLoadingProvider('google')
   3. supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo } })
   4. Browser redirects to Google consent → callback URL
   5. AuthProvider.onAuthStateChange detects SIGNED_IN → authStore.setUser(user)
   6. LoginCard checks user → router.push("/")

8. **Guest Login Flow** — Ordered steps:
   1. User taps Guest button → GuestButton onClick
   2. authStore.guestLogin() → isGuest=true
   3. router.push("/")

9. **Error Cases** — Table:
   - OAuth popup blocked → error message display
   - OAuth provider error → authStore.error set → red block
   - Network failure → error message

10. **Requirements** — EARS format:
   - "When user taps Google OAuth button, the system shall initiate Supabase OAuth flow with Google provider" — IMPL
   - "When OAuth completes successfully, the system shall set authStore user and redirect to '/'" — IMPL
   - "When user taps Guest button, the system shall set isGuest=true and redirect to '/'" — IMPL
   - "When OAuth fails, the system shall display error message in red block below buttons" — IMPL
   - "When authenticated user visits /login, the system does NOT redirect away — no auth guard on /login" — IMPL (by omission)
   - "Kakao and Apple OAuth buttons" — NOT-IMPL (code exists in OAuthButton but LoginCard only renders Google)

11. **Auth-Conditional Pattern** — Document the cross-cutting pattern:
   - Middleware: only /admin/:path* protected
   - Header: user ? authenticated menu : Login link
   - /login page: no auth check at all

**Cross-references:**
- `See: SCR-USER-02 — Profile (authenticated user destination)`
- `See: FLW-04 — User Flow (auth state transitions)`

**Line budget:** Target 160 lines. Max 200.
  </action>
  <verify>
1. `wc -l specs/screens/user/SCR-USER-01-login.md` — must be <=200 lines
2. Grep for `packages/web/` paths; verify each with `ls` — zero mismatches
3. File contains authStore state transition table with selectIsAuthenticated vs selectIsLoggedIn
4. File contains Google OAuth flow steps (not Kakao/Apple)
5. File contains guest login flow
6. File contains EARS requirements with IMPL / NOT-IMPL tags
7. File contains mobile + desktop wireframe sections
  </verify>
  <done>
SCR-USER-01-login.md exists with <=200 lines, all paths verified, authStore state transitions documented, Google OAuth and guest flows detailed, Kakao/Apple marked NOT-IMPL, and cross-references to USER-02 and FLW-04 included.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SCR-USER-02-profile.md and SCR-USER-03-earnings.md</name>
  <files>
    specs/screens/user/SCR-USER-02-profile.md
    specs/screens/user/SCR-USER-03-earnings.md
  </files>
  <action>
Write both specs sequentially.

## SCR-USER-02-profile.md

**PRE-VERIFIED FACTS (from actual code):**
- Route: `/profile` — page at `packages/web/app/profile/page.tsx` (SSR) + ProfileClient
- ProfileClient at `packages/web/app/profile/ProfileClient.tsx` — main client component
- Always own-profile only — no /profile/[userId] route exists
- Auth pattern: API-error-driven (useMe/useUserStats return 401 → error component, NOT login redirect)
- Real data: user info via useMe(), stats via useUserStats()
- Mock data: badges (MOCK_BADGES), rankings (MOCK_RANKINGS), follow stats (hardcoded numbers), all 4 activity tab contents (PostsGrid/SpotsList/SolutionsList/SavedGrid use MOCK_* arrays)
- ProfileEditModal: fully implemented — name, bio, avatar upload
- ProfileDesktopLayout: side-by-side layout for md+ breakpoint
- 4 Activity tabs: Posts, Spots, Solutions, Saved
- profileStore: selectedTab, isEditModalOpen

**Structure:**
1. **Header** — ID: SCR-USER-02, Route: `/profile`, Status: partially implemented (mix of real + mock)
2. **Purpose** — User views own profile with activity history, badges, rankings, and stats
3. **Component Map** — Table with verified paths (mark each as REAL or MOCK data):
   - Page: ProfilePage — `packages/web/app/profile/page.tsx`
   - ProfileClient — `packages/web/app/profile/ProfileClient.tsx` — orchestrator
   - ProfileHeader — `packages/web/lib/components/profile/ProfileHeader.tsx` — avatar, name, bio — REAL (useMe)
   - ProfileBio — `packages/web/lib/components/profile/ProfileBio.tsx` — bio text — REAL
   - FollowStats — `packages/web/lib/components/profile/FollowStats.tsx` — followers/following counts — MOCK
   - StatsCards — `packages/web/lib/components/profile/StatsCards.tsx` — Points/Posts/Spots/Solutions — REAL (useUserStats)
   - BadgeGrid — `packages/web/lib/components/profile/BadgeGrid.tsx` — badge collection — MOCK
   - BadgeModal — `packages/web/lib/components/profile/BadgeModal.tsx` — badge detail popup — MOCK
   - RankingList — `packages/web/lib/components/profile/RankingList.tsx` — leaderboard — MOCK
   - ActivityTabs — `packages/web/lib/components/profile/ActivityTabs.tsx` — tab bar — REAL (UI)
   - ActivityContent — `packages/web/lib/components/profile/ActivityContent.tsx` — tab content router — REAL (UI)
   - PostsGrid — `packages/web/lib/components/profile/PostsGrid.tsx` — posts tab — MOCK
   - SpotsList — `packages/web/lib/components/profile/SpotsList.tsx` — spots tab — MOCK
   - SolutionsList — `packages/web/lib/components/profile/SolutionsList.tsx` — solutions tab — MOCK
   - SavedGrid — `packages/web/lib/components/profile/SavedGrid.tsx` — saved tab — MOCK
   - ProfileEditModal — `packages/web/lib/components/profile/ProfileEditModal.tsx` — edit form — REAL
   - ProfileDesktopLayout — `packages/web/lib/components/profile/ProfileDesktopLayout.tsx` — desktop layout
   - DS ProfileHeaderCard — `packages/web/lib/design-system/profile-header-card.tsx`
   - DS StatCard — `packages/web/lib/design-system/stat-card.tsx`
   - DS Badge — `packages/web/lib/design-system/badge.tsx`
   - DS RankingItem — `packages/web/lib/design-system/ranking-item.tsx`
   - DS Tabs — `packages/web/lib/design-system/tabs.tsx`
   - Verify ALL paths with `ls`

4. **Layout — Mobile** — ASCII wireframe:
   - ProfileHeader (avatar + name + bio + edit button)
   - FollowStats (followers | following | posts)
   - StatsCards (4-card grid: Points/Posts/Spots/Solutions)
   - BadgeGrid (horizontal scroll badges)
   - RankingList (top 3 + list)
   - ActivityTabs (Posts | Spots | Solutions | Saved)
   - ActivityContent (selected tab content)
   - Keep wireframe concise (10-14 lines)

5. **Layout — Desktop** — Delta:
   - ProfileDesktopLayout: left sidebar (header + stats + badges) + right main (rankings + activity tabs)

6. **Real vs Mock Data Inventory** — Table:
   | Section | Data Source | Status |
   - Profile info (name, bio, avatar): useMe() → GET /api/v1/users/me — REAL
   - Stats (points, posts, spots, solutions): useUserStats() → GET /api/v1/users/me/stats — REAL
   - Profile edit (name, bio, avatar): ProfileEditModal → PATCH /api/v1/users/me — REAL
   - Follow stats (followers, following): MOCK (hardcoded numbers) — MOCK
   - Badges: MOCK_BADGES array — MOCK
   - Rankings: MOCK_RANKINGS array — MOCK
   - Activity > Posts: MOCK_POSTS array — MOCK
   - Activity > Spots: MOCK_SPOTS array — MOCK
   - Activity > Solutions: MOCK_SOLUTIONS array — MOCK
   - Activity > Saved: MOCK_SAVED array — MOCK

7. **profileStore Fields** — Table:
   | Field | Type | Purpose |
   - badgeModalMode: BadgeModalMode — badge modal state ("closed" | "detail" | "list")
   - selectedBadge: Badge | null — currently selected badge for modal

   **Local component state (in ProfileClient.tsx, NOT profileStore):**
   - activeTab: ActivityTab (useState) — active activity tab
   - isEditModalOpen: boolean (useState) — edit modal visibility

8. **Auth Pattern** — Document:
   - No middleware protection on /profile
   - Auth is API-driven: useMe() returns 401 → error component renders (not redirect to /login)
   - No selectIsAuthenticated check in component — purely server-response-driven

9. **ProfileEditModal** — Fields:
   - Display name (text input)
   - Bio (textarea)
   - Avatar (image upload with preview)
   - Save → PATCH /api/v1/users/me

10. **Requirements** — EARS format:
   - "When authenticated user visits /profile, the system shall fetch user info and stats from API" — IMPL
   - "When API returns 401, the system shall show error component (not redirect to login)" — IMPL
   - "When user taps Edit button, the system shall open ProfileEditModal" — IMPL
   - "When user saves profile edits, the system shall PATCH /api/v1/users/me and refresh data" — IMPL
   - "When user taps activity tab, the system shall switch visible content" — IMPL
   - "When user taps Points stat card, the system shall show alert('수익 페이지는 아직 구현되지 않았습니다.')" — IMPL
   - "Badge detail, ranking calculations, follow counts, and activity tab data are all mock" — MOCK
   - "Other-user profile (/profile/[userId])" — NOT-IMPL

**Cross-references:**
- `See: SCR-USER-01 — Login (authentication entry)`
- `See: SCR-USER-03 — Earnings (linked from Points stat card)`
- `See: FLW-04 — User Flow (profile access patterns)`

**Line budget:** Target 180 lines. Max 200.

---

## SCR-USER-03-earnings.md

**PRE-VERIFIED FACTS (from actual code):**
- NO route exists for earnings — no /earnings, /profile/earnings, or similar
- NO component exists for earnings dashboard
- NO API endpoint for earnings data
- The only reference: StatsCards.tsx Points card onClick → alert("수익 페이지는 아직 구현되지 않았습니다.")
- total_points exists in UserStats data model (returned by GET /api/v1/users/me/stats)
- This is a STUB SPEC documenting the NOT-IMPL status
- NOTE: ROADMAP success criterion says "클릭 추적, 정산 플로우, 기간별 데이터 표시가 기록되어 있다" — this predates codebase verification. The spec reflects actual implementation state per CONTEXT.md (Claude's discretion to document based on code). Add a note in the spec header: "ROADMAP criterion describes intended scope; spec documents actual implementation state (NOT-IMPL)."

**Structure:**
1. **Header** — ID: SCR-USER-03, Route: N/A (not implemented), Status: NOT-IMPL
2. **Purpose** — Earnings dashboard for affiliate click tracking, settlement, and period-based revenue display
3. **Implementation Status** — Clear NOT-IMPL banner:
   - No route
   - No component
   - No API
   - Only reference: StatsCards Points → alert()
4. **Existing Data Points** — What DOES exist:
   - total_points in UserStats (real API data)
   - StatsCards renders Points count from useUserStats
   - Points card tap → alert with Korean message
5. **Planned Scope (from requirements)** — Brief outline of USER-03 requirement:
   - Click tracking
   - Settlement flow
   - Period-based data display
   - Note: These are requirement descriptions, NOT implemented features
6. **Entry Point** — How user would reach earnings:
   - Profile > StatsCards > Points tap (currently shows alert)
7. **Requirements** — EARS format:
   - "When user taps Points stat card, the system shall show alert('수익 페이지는 아직 구현되지 않았습니다.')" — IMPL (alert only)
   - "Earnings dashboard with click tracking, settlement flow, period-based revenue" — NOT-IMPL
   - "Earnings API endpoint" — NOT-IMPL

**Cross-references:**
- `See: SCR-USER-02 — Profile (StatsCards Points tap triggers earnings alert)`
- `See: FLW-04 — User Flow (earnings placeholder in user journey)`

**Line budget:** Target 50 lines. Max 80. (Minimal spec for NOT-IMPL screen)
  </action>
  <verify>
1. `wc -l specs/screens/user/SCR-USER-02-profile.md` — must be <=200 lines
2. `wc -l specs/screens/user/SCR-USER-03-earnings.md` — must be <=80 lines
3. SCR-USER-02 contains Real vs Mock Data Inventory table
4. SCR-USER-02 contains ProfileEditModal fields (fully implemented)
5. SCR-USER-02 documents auth pattern as API-error-driven (401 → error component)
6. SCR-USER-02 documents all 4 activity tabs with MOCK status
7. SCR-USER-03 has clear NOT-IMPL banner at top
8. SCR-USER-03 documents StatsCards Points alert as only existing reference
9. Grep for `packages/web/` paths in SCR-USER-02; verify each with `ls` — zero mismatches
10. Both specs have EARS requirements with IMPL / MOCK / NOT-IMPL tags
  </verify>
  <done>
SCR-USER-02-profile.md and SCR-USER-03-earnings.md both exist with correct line counts, real/mock data clearly inventoried, auth-conditional pattern documented, ProfileEditModal fields detailed, activity tabs with MOCK status marked, and SCR-USER-03 correctly documented as NOT-IMPL stub.
  </done>
</task>

</tasks>

<verification>
1. All three files exist in `specs/screens/user/`
2. Combined line count <=480 (200 + 200 + 80)
3. Zero stale file paths — every `packages/web/` reference verified against filesystem
4. authStore state transitions documented in SCR-USER-01 (selectIsAuthenticated vs selectIsLoggedIn)
5. Google OAuth flow steps documented, Kakao/Apple marked NOT-IMPL
6. Guest login flow documented in SCR-USER-01
7. Real vs Mock data inventory present in SCR-USER-02 with clear REAL/MOCK tags
8. ProfileEditModal documented as fully implemented in SCR-USER-02
9. All 4 activity tabs documented with MOCK status in SCR-USER-02
10. Auth-conditional rendering pattern documented (middleware only /admin, header conditional, profile API-driven)
11. SCR-USER-03 clearly marked as NOT-IMPL with StatsCards alert as only reference
12. All three specs cross-reference each other and FLW-04
13. EARS requirements present with IMPL / MOCK / NOT-IMPL tags
14. All specs have mobile + desktop wireframe/layout sections (except SCR-USER-03 which is stub)
</verification>

<success_criteria>
- SCR-USER-01 covers login page with Google OAuth only, guest login, authStore state transitions, DomeGallery background, and error handling
- SCR-USER-02 covers profile page with clear real/mock data separation, ProfileEditModal (real), 4 activity tabs (mock), badges/rankings (mock), and API-error-driven auth pattern
- SCR-USER-03 is a concise NOT-IMPL stub documenting that only total_points data and StatsCards alert exist
- Auth-conditional rendering patterns are documented across specs (middleware scope, header conditional, API-driven auth)
- An AI agent reading these specs can identify which files to modify, understands which data is real vs mock, and knows the auth pattern for user system screens
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-system/07-01-SUMMARY.md`
</output>
