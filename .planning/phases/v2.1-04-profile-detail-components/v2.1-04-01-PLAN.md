---
phase: v2.1-04-profile-detail-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/design-system/badge.tsx
  - packages/web/lib/design-system/leader-item.tsx
  - packages/web/lib/design-system/index.ts
autonomous: true

must_haves:
  truths:
    - "User sees earned badges displayed with full color and details"
    - "User sees locked badges as dark silhouettes with ? icon"
    - "User sees top 3 podium positions with gold/silver/bronze medals"
    - "User sees self highlighted in leaderboard (background color)"
  artifacts:
    - path: "packages/web/lib/design-system/badge.tsx"
      provides: "Badge component with earned/locked states"
      exports: ["Badge", "BadgeSkeleton", "badgeVariants"]
    - path: "packages/web/lib/design-system/leader-item.tsx"
      provides: "LeaderItem component with podium and highlight variants"
      exports: ["LeaderItem", "LeaderItemSkeleton"]
  key_links:
    - from: "packages/web/lib/design-system/badge.tsx"
      to: "Card base component"
      via: "extends Card for consistent styling"
      pattern: "import.*Card.*from"
    - from: "packages/web/lib/design-system/index.ts"
      to: "Badge, LeaderItem exports"
      via: "barrel export"
      pattern: "export.*Badge|LeaderItem"
---

<objective>
Create Badge and LeaderItem components for profile gamification display.

Purpose: Enable gamification UI on profile pages with earned/locked badges and leaderboard rankings
Output: Badge.tsx, LeaderItem.tsx components with variants, exported from design-system barrel
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.1.md
@.planning/STATE.md
@.planning/phases/v2.1-04-profile-detail-components/v2.1-04-CONTEXT.md

# Existing patterns to follow
@packages/web/lib/design-system/artist-card.tsx
@packages/web/lib/design-system/stat-card.tsx
@packages/web/lib/design-system/tokens.ts
@packages/web/lib/design-system/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Badge component</name>
  <files>packages/web/lib/design-system/badge.tsx</files>
  <action>
Create Badge component following existing design-system patterns (see artist-card.tsx, stat-card.tsx).

**Component structure:**
- Use CVA for variants: `earned` (full color), `locked` (dark silhouette + ?)
- Props: `id`, `name`, `iconUrl` (badge icon image), `description`, `earnedAt` (optional date), `variant` (earned/locked), `onClick` (for modal trigger)

**Earned state:**
- Full color badge icon display
- Badge name below icon
- Click triggers onClick handler (modal will show details)
- Subtle hover effect (scale, shadow)

**Locked state:**
- Dark silhouette effect using CSS filter (grayscale + brightness down)
- Overlay with "?" text centered
- Name shows as "???" or hidden
- Still clickable (to show "how to earn" info)

**Layout:**
- Fixed size for grid display (e.g., 80x80 icon area + text below)
- Use Card base for consistent styling
- Responsive: 3 columns mobile, 4 columns desktop (grid parent decides this, component is just the item)

**Include BadgeSkeleton:**
- Loading state matching Badge structure
- Shimmer animation on icon area and text

Follow forwardRef pattern from existing components.
  </action>
  <verify>
- TypeScript compiles without errors: `yarn tsc --noEmit`
- Component exports properly defined
- Both variants render correctly
  </verify>
  <done>
- Badge component with earned/locked variants
- BadgeSkeleton for loading state
- Follows design-system conventions (forwardRef, CVA, Card base)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LeaderItem component</name>
  <files>packages/web/lib/design-system/leader-item.tsx</files>
  <action>
Create LeaderItem component for leaderboard display.

**Component structure:**
- Props: `rank` (number), `avatarUrl`, `nickname`, `score`, `rankChange` (optional: { direction: 'up'|'down'|'neutral', value: number }), `isCurrentUser` (boolean), `onClick` (for profile preview modal)

**Podium positions (rank 1-3):**
- Medal icon: Gold (rank 1), Silver (rank 2), Bronze (rank 3)
- Use Lucide Medal or Trophy icon with appropriate colors
- Background color highlight: subtle gold/silver/bronze tint

**Medal colors:**
- Gold: `bg-yellow-500/10` with `text-yellow-500` icon
- Silver: `bg-gray-400/10` with `text-gray-400` icon
- Bronze: `bg-orange-600/10` with `text-orange-600` icon

**Current user highlight:**
- If `isCurrentUser=true`: Add primary color left border + subtle background tint
- Works in addition to podium styling

**Layout:**
- Horizontal row: Rank # (or medal) | Avatar | Nickname | Score | Rank change
- Avatar: 40x40 rounded-full with fallback (first letter)
- Rank change: Use RankChange sub-component (or inline) showing arrow + value
  - Up: primary color, up arrow
  - Down: secondary/destructive color, down arrow
  - Neutral: muted, dash

**Include LeaderItemSkeleton:**
- Loading state matching LeaderItem structure

Follow existing patterns from artist-card.tsx for avatar handling.
  </action>
  <verify>
- TypeScript compiles without errors: `yarn tsc --noEmit`
- Podium variants show correct medal colors
- Current user highlight displays correctly
  </verify>
  <done>
- LeaderItem component with podium and highlight variants
- LeaderItemSkeleton for loading state
- Rank change indicator inline (will be extracted to standalone in Plan 02)
  </done>
</task>

<task type="auto">
  <name>Task 3: Export from design-system barrel</name>
  <files>packages/web/lib/design-system/index.ts</files>
  <action>
Add exports for Badge and LeaderItem to design-system barrel file.

Add to index.ts:

```typescript
// Badge Component
export { Badge, BadgeSkeleton, badgeVariants } from "./badge";
export type { BadgeProps, BadgeSkeletonProps } from "./badge";

// LeaderItem Component
export { LeaderItem, LeaderItemSkeleton } from "./leader-item";
export type { LeaderItemProps, LeaderItemSkeletonProps } from "./leader-item";
```

Place after existing card component exports, maintaining alphabetical or logical grouping.
  </action>
  <verify>
- TypeScript compiles without errors: `yarn tsc --noEmit`
- Imports work: `import { Badge, LeaderItem } from "@/lib/design-system"`
  </verify>
  <done>
- Badge and LeaderItem exported from design-system barrel
- Type exports included
  </done>
</task>

</tasks>

<verification>
1. `yarn tsc --noEmit` passes
2. All components follow existing design-system patterns
3. Badge shows earned/locked states correctly
4. LeaderItem shows podium medals and current user highlight
5. Skeletons provide loading states
</verification>

<success_criteria>
- Badge component renders with earned (full color) and locked (silhouette + ?) states
- LeaderItem shows gold/silver/bronze medals for ranks 1-3
- Current user is highlighted with border and background
- All components exported from design-system barrel
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-04-profile-detail-components/v2.1-04-01-SUMMARY.md`
</output>
