---
phase: m7-03-the-decoded-studio
plan: 04
type: execute
wave: 4
depends_on: ["m7-03-01", "m7-03-02", "m7-03-03"]
files_modified:
  - packages/web/lib/components/collection/IssueDetailPanel.tsx
  - packages/web/lib/components/collection/EmptyStudio.tsx
  - packages/web/lib/components/collection/studio/StudioLoader.tsx
  - packages/web/lib/components/collection/studio/StudioHUD.tsx
  - packages/web/lib/components/collection/CollectionClient.tsx
  - packages/web/lib/components/collection/index.ts
autonomous: false

must_haves:
  truths:
    - "Focusing a book shows IssueDetailPanel with title, date, tags, and action buttons"
    - "Empty collection shows EmptyStudio with holographic wireframe CTA"
    - "StudioLoader shows neon progress bar during asset loading"
    - "StudioHUD shows header with title and issue count"
    - "WebGL fallback renders existing BookshelfView with banner"
    - "Open Magazine navigates to /magazine/issue/[id]"
  artifacts:
    - path: "packages/web/lib/components/collection/IssueDetailPanel.tsx"
      provides: "2D HTML overlay panel with issue metadata and action buttons"
      contains: "IssueDetailPanel"
    - path: "packages/web/lib/components/collection/EmptyStudio.tsx"
      provides: "Empty room with holographic CTA"
      contains: "EmptyStudio"
    - path: "packages/web/lib/components/collection/studio/StudioLoader.tsx"
      provides: "Suspense fallback with neon loading bar"
      contains: "StudioLoader"
    - path: "packages/web/lib/components/collection/studio/StudioHUD.tsx"
      provides: "2D HTML overlay header"
      contains: "StudioHUD"
  key_links:
    - from: "IssueDetailPanel.tsx"
      to: "studioStore.ts"
      via: "reads focusedIssueId and cameraState"
      pattern: "focusedIssueId"
    - from: "IssueDetailPanel.tsx"
      to: "magazineStore.ts"
      via: "reads collectionIssues to find focused issue data"
      pattern: "collectionIssues"
    - from: "CollectionClient.tsx"
      to: "EmptyStudio.tsx"
      via: "renders when collectionIssues.length === 0"
      pattern: "EmptyStudio"
    - from: "CollectionClient.tsx"
      to: "BookshelfView.tsx"
      via: "renders when WebGL not supported"
      pattern: "BookshelfView"
---

<objective>
Create the IssueDetailPanel (2D overlay with metadata and actions), EmptyStudio (holographic CTA), StudioLoader (neon progress bar), StudioHUD (header overlay), and finalize the CollectionClient orchestration with WebGL fallback and barrel exports.

Purpose: Complete the user-facing UI layer -- users can view issue details, take actions, and see proper loading/empty/fallback states.
Output: Full collection page experience with all UI overlays, states, and navigation working.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-RESEARCH.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-01-SUMMARY.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-02-SUMMARY.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-03-SUMMARY.md

@packages/web/lib/stores/studioStore.ts
@packages/web/lib/stores/magazineStore.ts
@packages/web/lib/components/collection/CollectionClient.tsx
@packages/web/lib/components/collection/BookshelfView.tsx
@packages/web/lib/components/collection/EmptyBookshelf.tsx
@packages/web/lib/components/collection/index.ts

<interfaces>
<!-- Key types from prior plans -->

From studioStore.ts:
```typescript
type CameraState = "entry" | "browse" | "focused" | "exit";
interface StudioState {
  cameraState: CameraState;
  focusedIssueId: string | null;
  entryComplete: boolean;
  qualityLevel: "high" | "medium" | "low";
  focusIssue: (id: string) => void;
  unfocus: () => void;
  setCameraState: (s: CameraState) => void;
}
```

From magazineStore.ts:
```typescript
interface MagazineState {
  collectionIssues: MagazineIssue[];
  activeIssueId: string | null;
  loadCollection: () => Promise<void>;
}
```

From MagazineIssue type:
```typescript
export interface MagazineIssue {
  id: string;
  issue_number: number;
  title: string;
  subtitle?: string;
  cover_image_url: string;
  layout_json: LayoutJSON;
  theme_palette: ThemePalette;
  generated_at: string;
  theme_keywords?: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IssueDetailPanel + EmptyStudio + StudioLoader + StudioHUD</name>
  <files>
    packages/web/lib/components/collection/IssueDetailPanel.tsx
    packages/web/lib/components/collection/EmptyStudio.tsx
    packages/web/lib/components/collection/studio/StudioLoader.tsx
    packages/web/lib/components/collection/studio/StudioHUD.tsx
  </files>
  <action>
1. **IssueDetailPanel.tsx** ("use client"):
   - Reads `focusedIssueId` and `cameraState` from studioStore
   - Reads `collectionIssues` from magazineStore to find the focused issue
   - Only visible when `cameraState === "focused"` and focusedIssueId is set
   - Entry animation: Motion `animate` with translateY 100%->0, opacity 0->1 (0.3s)
   - Exit: AnimatePresence for unmount animation (translateY 0->100%, 0.2s)
   - Layout:
     - Mobile: fixed bottom, full width, bg-black/60 backdrop-blur-md, rounded-t-2xl, px-6 py-5
     - Desktop: fixed right-4 bottom-4, w-80, rounded-2xl
   - Content:
     - Volume label: "Vol.{issue_number}" in #eafd67, text-xs font-bold tracking-widest uppercase
     - Title: issue.title, text-xl font-bold text-white
     - Date: formatted from generated_at as "MMMM D, YYYY" (use Intl.DateTimeFormat)
     - Tags: issue.theme_keywords as pills (#eafd67 border, text-xs)
   - Action buttons:
     - "Open Magazine" -- primary, bg-[#eafd67] text-black font-bold. onClick: navigate to /magazine/issue/[issue.id]
     - "Share" -- secondary, border border-white/20. onClick: placeholder for now (no share sheet in this plan -- keep it simple)
     - "Remove" -- ghost, text-red-400/60. onClick: placeholder (no delete API yet)

2. **EmptyStudio.tsx** ("use client"):
   - Renders when collectionIssues.length === 0 AND WebGL is supported
   - This is a 2D overlay on top of the 3D empty room (StudioScene still renders with no books)
   - Centered content:
     - Wireframe book icon or simple SVG outline pulsing with #eafd67
     - "Your studio is waiting" -- text-lg text-white/60
     - "Generate your first issue" -- text-sm text-white/40
     - [Generate First Issue] button -- bg-[#eafd67] text-black, navigates to /magazine/personal
   - Dark overlay with subtle vignette feel

3. **StudioLoader.tsx** ("use client"):
   - Used as Suspense fallback and initial loading state
   - Dark void background (#050505)
   - Centered neon progress bar:
     - Thin bar (h-0.5), bg-[#eafd67] with CSS animation (indeterminate shimmer)
     - Width oscillates via keyframe animation
   - "Loading your studio..." text below bar, text-sm text-white/40, tracking-wider
   - Export as named export

4. **StudioHUD.tsx** ("use client"):
   - 2D HTML overlay rendered OUTSIDE the Canvas (sibling div)
   - Fixed top, z-50, full width
   - Content:
     - Left: back button (ArrowLeft icon, onClick: studioStore.setCameraState("exit"))
     - Center: "The Decoded Studio" -- text-sm font-bold tracking-wider text-white uppercase
     - Right: issue count badge (same style as existing, bg-[#eafd67] text-black)
   - Semi-transparent: bg-transparent (no backdrop, just floating text)
   - Reads collectionIssues.length from magazineStore
   - Visible only after entryComplete from studioStore
   - Entry animation: opacity 0->1, translateY -20->0, 0.5s delay after entry
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app/packages/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>IssueDetailPanel shows metadata and actions on focus, EmptyStudio shows holographic CTA, StudioLoader shows neon progress, StudioHUD shows header overlay</done>
</task>

<task type="auto">
  <name>Task 2: Wire CollectionClient orchestration + barrel exports + fallback</name>
  <files>
    packages/web/lib/components/collection/CollectionClient.tsx
    packages/web/lib/components/collection/index.ts
  </files>
  <action>
1. **Update CollectionClient.tsx** -- final orchestration:
   - Dynamic import StudioScene with ssr: false, loading: () => <StudioLoader />
   - WebGL detection flow (from Plan 01, ensure complete):
     - State: supportsWebGL (null | boolean)
     - useEffect: check webgl2 context, set state
     - null -> StudioLoader
     - false -> BookshelfView fallback + subtle banner div ("3D studio requires a modern browser. Showing classic view.")
     - true + empty collection -> StudioScene + EmptyStudio overlay
     - true + has issues -> StudioScene (books rendered by MagazineRack inside)
   - Render StudioHUD outside Canvas (as sibling, only when WebGL)
   - Render IssueDetailPanel outside Canvas (as sibling, only when WebGL)
   - Load collection data via magazineStore.loadCollection() on mount

2. **Update index.ts** -- add new exports:
   - Export IssueDetailPanel, EmptyStudio
   - Keep existing exports (BookshelfView etc. for fallback)

3. **Fallback banner styling:**
   - Position: fixed top-0 left-0 right-0, z-50
   - Content: "3D studio requires a modern browser. Showing classic view."
   - Style: text-xs text-center py-1 bg-mag-accent/10 text-mag-accent/60
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app/packages/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>CollectionClient orchestrates WebGL check, 3D scene, 2D overlays, and fallback. All components wired and exported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete 3D Studio experience</name>
  <files>packages/web/lib/components/collection/</files>
  <action>
Human verifies the complete 3D Decoded Studio experience end-to-end.

Steps:
1. Run `yarn dev` in packages/web
2. Visit http://localhost:3000/collection
3. Verify: entry camera dolly animation plays (dark corridor to room reveal)
4. Verify: neon #eafd67 lights with bloom glow visible on walls
5. Verify: reflective floor shows neon reflections
6. Verify: magazine books float with gentle bobbing, arranged in arc
7. Verify: spines show "Vol.01", "Vol.02" etc. in neon text
8. Verify: moving mouse causes subtle camera parallax
9. Verify: hovering a book scales it up and makes spine glow brighter
10. Verify: clicking a book zooms camera and opens the cover
11. Verify: IssueDetailPanel slides up with issue title, date, tags, buttons
12. Verify: pressing Escape or clicking empty space returns to browse
13. Verify: "The Decoded Studio" header and issue count visible in HUD
14. Verify: "Open Magazine" button navigates to magazine issue page
15. Test on mobile viewport: verify simplified rendering, no parallax
  </action>
  <verify>Human approval</verify>
  <done>User confirms the complete 3D studio experience works as designed</done>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` -- no type errors
2. Full visual walkthrough of all states: loading -> entry -> browse -> hover -> focus -> unfocus -> exit
3. Empty state renders holographic CTA
4. WebGL fallback shows classic bookshelf with banner
</verification>

<success_criteria>
- IssueDetailPanel shows volume, title, date, tags, action buttons on book focus
- EmptyStudio renders wireframe book CTA when no issues
- StudioLoader shows neon progress bar during loading
- StudioHUD shows title + count after entry completes
- WebGL fallback gracefully shows BookshelfView with informational banner
- "Open Magazine" navigates to /magazine/issue/[id]
- All components exported from barrel index
- Human verifies complete visual experience
</success_criteria>

<output>
After completion, create `.planning/phases/m7-03-the-decoded-studio/m7-03-04-SUMMARY.md`
</output>
