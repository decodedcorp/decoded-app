---
phase: m7-03-the-decoded-studio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/stores/studioStore.ts
  - packages/web/lib/components/collection/studio/SplineStudio.tsx
  - packages/web/lib/components/collection/studio/useSplineRuntime.ts
  - packages/web/lib/components/collection/StudioLoader.tsx
  - packages/web/lib/components/collection/studio/TextureSwapSpike.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "studioStore tracks camera state machine (loading/entry/browse/focused/exit) and focusedIssueId"
    - "SplineStudio component loads a .splinecode scene via dynamic import with ssr:false"
    - "useSplineRuntime hook captures Application ref and exposes findObject, triggerEvent, setVar, transitionCamera"
    - "StudioLoader renders neon #eafd67 progress bar on dark background"
    - "Texture swap spike confirms whether material.layers image.data or setVariable approach works"
  artifacts:
    - path: "packages/web/lib/stores/studioStore.ts"
      provides: "Camera state machine + focused issue state"
      exports: ["useStudioStore"]
    - path: "packages/web/lib/components/collection/studio/SplineStudio.tsx"
      provides: "Spline scene wrapper component"
      exports: ["SplineStudio"]
    - path: "packages/web/lib/components/collection/studio/useSplineRuntime.ts"
      provides: "Spline Application ref + control methods"
      exports: ["useSplineRuntime"]
    - path: "packages/web/lib/components/collection/StudioLoader.tsx"
      provides: "Loading screen for Spline scene"
      exports: ["StudioLoader"]
  key_links:
    - from: "SplineStudio.tsx"
      to: "useSplineRuntime.ts"
      via: "onLoad callback stores Application ref"
      pattern: "useSplineRuntime.*onLoad"
    - from: "SplineStudio.tsx"
      to: "studioStore.ts"
      via: "sets cameraState on load completion"
      pattern: "setCameraState"
---

<objective>
Install Spline packages, create the studioStore state machine, build the SplineStudio wrapper with useSplineRuntime hook, create StudioLoader, and run a texture swap spike test to de-risk the highest uncertainty item.

Purpose: Establish the Spline Pro foundation and validate the critical texture swap approach before building data bridges.
Output: studioStore, SplineStudio, useSplineRuntime, StudioLoader, spike test results.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-RESEARCH.md

@packages/web/lib/stores/magazineStore.ts
@packages/web/lib/components/magazine/types.ts
@packages/web/lib/components/collection/CollectionClient.tsx

<interfaces>
<!-- Existing types the executor needs -->

From packages/web/lib/components/magazine/types.ts:
```typescript
export interface MagazineIssue {
  id: string;
  issue_number: number;
  title: string;
  subtitle?: string;
  cover_image_url: string;
  layout_json: LayoutJSON;
  theme_palette: ThemePalette;
  generated_at: string;
  theme_keywords?: string[];
}
```

From packages/web/lib/stores/magazineStore.ts:
```typescript
// Key fields used by collection:
collectionIssues: MagazineIssue[];
activeIssueId: string | null;
loadCollection: () => Promise<void>;
setActiveIssueId: (id: string | null) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Spline packages + studioStore + useSplineRuntime + StudioLoader</name>
  <files>
    packages/web/package.json
    packages/web/lib/stores/studioStore.ts
    packages/web/lib/components/collection/studio/useSplineRuntime.ts
    packages/web/lib/components/collection/StudioLoader.tsx
  </files>
  <action>
1. Install packages:
   ```bash
   cd packages/web && yarn add @splinetool/react-spline @splinetool/runtime
   ```

2. Create `packages/web/lib/stores/studioStore.ts`:
   - Zustand store with state machine:
     ```typescript
     type CameraState = "loading" | "entry" | "browse" | "focused" | "exit";
     ```
   - Fields: `cameraState` (default "loading"), `focusedIssueId: string | null`, `entryComplete: boolean`, `splineLoaded: boolean`
   - Actions: `setCameraState(s)`, `focusIssue(id)` (sets focusedIssueId + cameraState="focused"), `unfocus()` (clears focusedIssueId + cameraState="browse"), `setSplineLoaded(b)`
   - Follow exact pattern from RESEARCH.md studioStore example but add `splineLoaded` field

3. Create `packages/web/lib/components/collection/studio/useSplineRuntime.ts`:
   - Hook that stores `Application` ref via `useRef`
   - `onLoad(spline: Application)` callback to capture ref
   - `findObject(name: string)` -> SPEObject | undefined
   - `triggerEvent(event: string, name: string)` -> emitEvent
   - `setVar(name: string, value: string | number | boolean)` -> setVariable
   - `transitionCamera(stateName: string, durationMs?: number)` -> finds "MainCamera", calls transition with EASE_IN_OUT (easing: 4)
   - Import types: `import type { Application, SPEObject } from "@splinetool/runtime";`
   - Return `{ onLoad, splineRef, findObject, triggerEvent, setVar, transitionCamera }`

4. Create `packages/web/lib/components/collection/StudioLoader.tsx`:
   - "use client" component
   - Full-screen dark (#050505) background
   - Centered #eafd67 indeterminate progress bar (CSS animation, shimmer left-to-right)
   - "Loading your studio..." text below bar in text-mag-text/60
   - Accept `visible` prop; when false, fade out with opacity transition (0.3s) then unmount
   - Use Tailwind only, no external animation libs
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>studioStore exports useStudioStore with camera state machine. useSplineRuntime exports typed hook with Application ref + control methods. StudioLoader renders neon loading screen. All type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: SplineStudio wrapper + Texture swap spike test</name>
  <files>
    packages/web/lib/components/collection/studio/SplineStudio.tsx
    packages/web/lib/components/collection/studio/TextureSwapSpike.tsx
    packages/web/public/spline/.gitkeep
  </files>
  <action>
1. Create `packages/web/public/spline/.gitkeep` to establish the directory for future .splinecode files.

2. Create `packages/web/lib/components/collection/studio/SplineStudio.tsx`:
   - "use client" component
   - Import `Spline` from `@splinetool/react-spline`
   - Use `useSplineRuntime()` hook for onLoad + control methods
   - Use `useStudioStore()` for camera state + focusIssue
   - Use `useMagazineStore()` for collectionIssues
   - Scene URL: `/spline/decoded-studio.splinecode` (placeholder, will be replaced when Spline scene is designed)
   - `onLoad` handler:
     a. Call runtime.onLoad(spline)
     b. Set splineLoaded(true)
     c. Set initial variables via setVar: issue_count, show_empty_state
     d. After 2500ms setTimeout, setCameraState("browse") — entry animation timeout
   - `onSplineMouseDown` handler:
     a. Check if target name starts with "Magazine_"
     b. Parse index from name
     c. Map to collectionIssues[index]
     d. If found: focusIssue(issue.id), compute camera focus position per RESEARCH.md Pitfall 6 pattern
   - `onSplineMouseHover` handler:
     a. Set cursor pointer if Magazine_ target, default otherwise
   - Render: `<div className="fixed inset-0 bg-[#050505]">` wrapping `<Spline>` with scene prop + event handlers
   - NOTE: This component will be dynamically imported by CollectionClient in Plan 02. Do NOT add ssr:false here — that's the consumer's job.

3. Create `packages/web/lib/components/collection/studio/TextureSwapSpike.tsx`:
   - "use client" spike/test component (will be deleted after validation)
   - Purpose: validate whether dynamic texture swap works via `material.layers` API
   - Import Spline, useRef for Application
   - On load:
     a. Find object "Magazine_0" (or first named book)
     b. Log `obj.material` and `obj.material?.layers` to console
     c. Attempt to find a layer with `.image` property
     d. If found, try setting `layer.image = { data: "/mock/magazine-covers/issue-01.jpg", name: "test" }`
     e. Call `spline.requestRender()`
     f. Log success/failure
   - Render: simple div with Spline embed + console output display (pre element showing results)
   - Add a comment: `// SPIKE TEST — delete after validating texture swap approach`
   - This does NOT need a route; it's a component that can be temporarily mounted in /lab or tested manually
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>SplineStudio wraps Spline component with onLoad/event handlers and integrates studioStore + magazineStore. TextureSwapSpike component exists for manual validation of material.layers texture approach. public/spline/ directory created for scene files.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with zero errors
2. studioStore state transitions work: loading -> entry -> browse -> focused -> browse
3. SplineStudio component references correct Spline event handler props
4. StudioLoader renders visually (can verify by temporarily mounting in /collection)
</verification>

<success_criteria>
- Spline packages installed and importable
- studioStore camera state machine complete with all transitions
- useSplineRuntime hook provides typed control methods
- SplineStudio component wired to both stores and Spline events
- StudioLoader shows neon progress bar
- TextureSwapSpike ready for manual validation
- All files type-check clean
</success_criteria>

<output>
After completion, create `.planning/phases/m7-03-the-decoded-studio/m7-03-01-SUMMARY.md`
</output>
