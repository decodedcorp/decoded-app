---
phase: m7-03-the-decoded-studio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/collection/studio/StudioScene.tsx
  - packages/web/lib/components/collection/studio/StudioRoom.tsx
  - packages/web/lib/components/collection/studio/StudioLighting.tsx
  - packages/web/lib/components/collection/studio/CameraRig.tsx
  - packages/web/lib/stores/studioStore.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "R3F Canvas renders without SSR crash on /collection route"
    - "Dark gallery room with reflective floor and neon lighting is visible"
    - "Entry camera animation plays on mount (dolly from corridor to isometric view)"
    - "studioStore manages camera state transitions (entry -> browse)"
  artifacts:
    - path: "packages/web/lib/components/collection/studio/StudioScene.tsx"
      provides: "R3F Canvas root with Suspense"
      contains: "<Canvas"
    - path: "packages/web/lib/components/collection/studio/StudioRoom.tsx"
      provides: "Floor Reflector + wall geometry"
      contains: "Reflector"
    - path: "packages/web/lib/components/collection/studio/StudioLighting.tsx"
      provides: "Neon #eafd67 emissive lights + ambient"
      contains: "eafd67"
    - path: "packages/web/lib/components/collection/studio/CameraRig.tsx"
      provides: "GSAP entry camera animation"
      contains: "useGSAP"
    - path: "packages/web/lib/stores/studioStore.ts"
      provides: "Camera state machine store"
      exports: ["useStudioStore"]
  key_links:
    - from: "StudioScene.tsx"
      to: "StudioRoom, StudioLighting, CameraRig"
      via: "JSX children inside Canvas"
      pattern: "<StudioRoom|<StudioLighting|<CameraRig"
    - from: "CameraRig.tsx"
      to: "studioStore.ts"
      via: "setCameraState on animation complete"
      pattern: "setCameraState.*browse"
---

<objective>
Install R3F v8 ecosystem, create the StudioScene Canvas root, StudioRoom (floor Reflector + walls), StudioLighting (neon #eafd67 emissive strips), CameraRig (GSAP entry dolly animation), and studioStore (camera state machine).

Purpose: Establish the 3D foundation -- a visible, lit gallery room with entry animation that all subsequent plans build upon.
Output: Running 3D scene at /collection with dark room, reflective floor, neon lights, and camera entry animation.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-RESEARCH.md

@packages/web/lib/components/collection/CollectionClient.tsx
@packages/web/lib/components/collection/index.ts
@packages/web/lib/stores/magazineStore.ts
@packages/web/lib/components/magazine/types.ts

<interfaces>
<!-- Existing types the executor needs -->

From packages/web/lib/components/magazine/types.ts:
```typescript
export interface MagazineIssue {
  id: string;
  issue_number: number;
  title: string;
  subtitle?: string;
  cover_image_url: string;
  layout_json: LayoutJSON;
  theme_palette: ThemePalette;
  generated_at: string;
  theme_keywords?: string[];
}
```

From packages/web/lib/stores/magazineStore.ts:
```typescript
// Existing store -- studioStore is NEW, separate file
export const useMagazineStore = create<MagazineState>(...);
// Uses: collectionIssues, activeIssueId, setActiveIssueId, loadCollection
```

From m7-03-RESEARCH.md (studioStore pattern):
```typescript
type CameraState = "entry" | "browse" | "focused" | "exit";
type QualityLevel = "high" | "medium" | "low";

interface StudioState {
  cameraState: CameraState;
  focusedIssueId: string | null;
  entryComplete: boolean;
  qualityLevel: QualityLevel;
  setCameraState: (s: CameraState) => void;
  focusIssue: (id: string) => void;
  unfocus: () => void;
  setQualityLevel: (q: QualityLevel) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install R3F v8 + create studioStore + StudioScene Canvas</name>
  <files>
    packages/web/package.json
    packages/web/lib/stores/studioStore.ts
    packages/web/lib/components/collection/studio/StudioScene.tsx
  </files>
  <action>
1. Install R3F v8 ecosystem (MUST use these exact versions for React 18 compatibility -- v9 requires React 19):
   ```
   cd packages/web && yarn add @react-three/fiber@^8.18.0 @react-three/drei@^9.122.0 @react-three/postprocessing@^2.19.1
   ```
   Also install leva as dev dep: `yarn add -D leva@^0.10`

2. Create `packages/web/lib/stores/studioStore.ts`:
   - Zustand store following the pattern from RESEARCH.md Pattern 5
   - CameraState: "entry" | "browse" | "focused" | "exit"
   - QualityLevel: "high" | "medium" | "low"
   - Fields: cameraState, focusedIssueId, entryComplete, qualityLevel
   - Actions: setCameraState (sets entryComplete=true when leaving entry), focusIssue, unfocus, setQualityLevel
   - Default qualityLevel: "medium" on mobile (<768px), "high" on desktop

3. Create `packages/web/lib/components/collection/studio/StudioScene.tsx`:
   - "use client" component
   - R3F `<Canvas>` with camera at (0, 1.5, -8), fov 50
   - GL settings: antialias true, alpha false, powerPreference "high-performance"
   - dpr: [1, 2]
   - Wrap all 3D children in `<Suspense fallback={null}>`
   - Include `<Preload all />` from drei
   - Render StudioRoom, StudioLighting, CameraRig as children (placeholder comments for now if components not yet created -- but they will be created in Task 2)
   - Export as named export `StudioScene`
   - Canvas wrapped in `<div className="fixed inset-0 bg-[#050505]">`
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app && cat packages/web/lib/stores/studioStore.ts | head -5 && cat packages/web/lib/components/collection/studio/StudioScene.tsx | head -5 && node -e "const pkg = require('./packages/web/package.json'); console.log('r3f:', pkg.dependencies['@react-three/fiber'], 'drei:', pkg.dependencies['@react-three/drei'], 'pp:', pkg.dependencies['@react-three/postprocessing'])"</automated>
  </verify>
  <done>R3F v8 packages installed, studioStore created with camera state machine, StudioScene Canvas root created</done>
</task>

<task type="auto">
  <name>Task 2: StudioRoom + StudioLighting + CameraRig entry animation</name>
  <files>
    packages/web/lib/components/collection/studio/StudioRoom.tsx
    packages/web/lib/components/collection/studio/StudioLighting.tsx
    packages/web/lib/components/collection/studio/CameraRig.tsx
    packages/web/lib/components/collection/CollectionClient.tsx
  </files>
  <action>
1. Create `StudioRoom.tsx`:
   - Floor: Drei `<Reflector>` component, 8x6 size, rotation [-PI/2, 0, 0], position [0,0,0]
   - Reflector props: resolution based on qualityLevel from studioStore (high=1024, medium=512, low=256), mirror 0.5, mixBlur 8, mixStrength 1, blur [300, 100]
   - Floor material: color #050505, metalness 0.5, roughness 0.8
   - Back wall: BoxGeometry or PlaneGeometry at z=-3, 8m wide, 4m tall, matte dark material (#0a0a0a)
   - Side walls: Two planes at x=-4 and x=4, 6m deep, 4m tall
   - No ceiling (open top for lighting)

2. Create `StudioLighting.tsx`:
   - Low ambient light: intensity 0.15, color warm white
   - 2-4 neon strip lights along upper walls: BoxGeometry (narrow, long), position along wall tops
   - Neon material: color #eafd67, emissive #eafd67, emissiveIntensity 3.0, toneMapped false (this is critical for Bloom to pick up)
   - One spot light from above pointing down for subtle fill

3. Create `CameraRig.tsx`:
   - Use `useThree()` to get camera reference
   - Use `useGSAP` from `@gsap/react` for entry animation lifecycle
   - Entry animation GSAP timeline:
     - Camera starts at (0, 1.5, -8) -- corridor behind
     - Dolly to (0, 3.0, 6) -- elevated isometric view
     - Duration 2.0s, ease "power2.inOut"
     - lookAt target: (0, 0.5, 0) -- updated on each frame via onUpdate
   - On timeline complete: call `studioStore.setCameraState("browse")`
   - Skip handler: onClick/onTouchStart on a transparent overlay -- if cameraState is "entry", kill timeline and set camera to final position immediately
   - IMPORTANT: Do NOT add useFrame parallax in this plan. That comes in Plan 03. Only entry animation here.

4. Update `CollectionClient.tsx`:
   - Import StudioScene via `next/dynamic` with `{ ssr: false }` (CRITICAL: Canvas crashes on SSR)
   - Add WebGL2 detection in useEffect (create canvas, check webgl2 context)
   - Three states: loading (null webgl check) -> StudioLoader placeholder, no WebGL -> BookshelfView fallback, WebGL -> StudioScene
   - Keep existing BookshelfView as the fallback path
   - Remove existing header from CollectionClient -- HUD will be handled in Plan 04
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app/packages/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>StudioRoom renders dark gallery with Reflector floor, StudioLighting shows neon #eafd67 strips, CameraRig plays entry dolly animation, CollectionClient dynamically imports StudioScene with SSR disabled and WebGL fallback</done>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` -- no type errors
2. `yarn dev` -- /collection route loads without SSR crash
3. Visual: dark room with reflective floor and neon #eafd67 glow strips visible
4. Camera dolly animation plays from corridor to isometric view on page load
</verification>

<success_criteria>
- R3F v8 ecosystem installed (fiber ^8, drei ^9, postprocessing ^2)
- StudioScene Canvas renders without SSR errors
- Dark room with Reflector floor and neon lighting visible
- Entry camera animation completes and studioStore transitions to "browse"
- WebGL fallback renders existing BookshelfView when WebGL2 unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/m7-03-the-decoded-studio/m7-03-01-SUMMARY.md`
</output>
