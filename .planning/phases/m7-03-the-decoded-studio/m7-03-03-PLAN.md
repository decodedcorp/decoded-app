---
phase: m7-03-the-decoded-studio
plan: 03
type: execute
wave: 3
depends_on: ["m7-03-01", "m7-03-02"]
files_modified:
  - packages/web/lib/components/collection/studio/CameraRig.tsx
  - packages/web/lib/components/collection/studio/MagazineBook.tsx
  - packages/web/lib/components/collection/studio/StudioEffects.tsx
  - packages/web/lib/components/collection/studio/StudioScene.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Mouse movement causes subtle camera parallax in browse state"
    - "Hovering a book scales it up and increases spine glow"
    - "Clicking a book zooms camera to it and opens the cover"
    - "Clicking away or pressing Escape returns to browse"
    - "Bloom postprocessing makes neon lights glow without bleeding to all materials"
    - "Performance adapts automatically when FPS drops"
  artifacts:
    - path: "packages/web/lib/components/collection/studio/CameraRig.tsx"
      provides: "Mouse parallax + click-to-focus + unfocus + exit camera transitions"
      contains: "useFrame"
    - path: "packages/web/lib/components/collection/studio/StudioEffects.tsx"
      provides: "EffectComposer with Bloom + Vignette"
      contains: "luminanceThreshold"
  key_links:
    - from: "CameraRig.tsx"
      to: "studioStore.ts"
      via: "reads cameraState, calls focusIssue/unfocus"
      pattern: "cameraState.*browse"
    - from: "MagazineBook.tsx"
      to: "CameraRig.tsx"
      via: "onClick triggers focusIssue in store, CameraRig reacts"
      pattern: "focusIssue"
    - from: "StudioEffects.tsx"
      to: "studioStore.ts"
      via: "reads qualityLevel to conditionally render effects"
      pattern: "qualityLevel"
---

<objective>
Add interactive camera behaviors (mouse parallax, click-to-focus zoom, unfocus, exit), magazine hover glow effects, Bloom/Vignette postprocessing, and performance adaptation via PerformanceMonitor.

Purpose: Make the 3D studio interactive -- users can explore the room and focus on individual magazines.
Output: Fully interactive camera rig with hover/click interactions, selective bloom glow, and adaptive performance.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-RESEARCH.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-01-SUMMARY.md
@.planning/phases/m7-03-the-decoded-studio/m7-03-02-SUMMARY.md

@packages/web/lib/stores/studioStore.ts
@packages/web/lib/components/collection/studio/CameraRig.tsx
@packages/web/lib/components/collection/studio/MagazineBook.tsx
@packages/web/lib/components/collection/studio/StudioScene.tsx

<interfaces>
<!-- From Plan 01/02 outputs -->

From studioStore.ts:
```typescript
type CameraState = "entry" | "browse" | "focused" | "exit";
interface StudioState {
  cameraState: CameraState;
  focusedIssueId: string | null;
  entryComplete: boolean;
  qualityLevel: "high" | "medium" | "low";
  setCameraState: (s: CameraState) => void;
  focusIssue: (id: string) => void;   // sets focusedIssueId + cameraState="focused"
  unfocus: () => void;                 // clears focusedIssueId + cameraState="browse"
  setQualityLevel: (q: QualityLevel) => void;
}
```

From MagazineBook.tsx (Plan 02):
```typescript
interface MagazineBookProps {
  issue: MagazineIssue;
  position: [number, number, number];
  rotation?: [number, number, number];
  isOpen?: boolean;
  opacity?: number;
  onPointerOver?: () => void;
  onPointerOut?: () => void;
  onClick?: () => void;
}
```

From m7-03-RESEARCH.md (Pattern 4 - Bloom):
```typescript
<Bloom luminanceThreshold={0.6} luminanceSmoothing={0.025} intensity={1.5} />
// Only materials with emissiveIntensity > threshold glow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraRig interactions (parallax, focus zoom, unfocus, exit)</name>
  <files>
    packages/web/lib/components/collection/studio/CameraRig.tsx
    packages/web/lib/components/collection/studio/MagazineBook.tsx
    packages/web/lib/components/collection/studio/MagazineRack.tsx
  </files>
  <action>
1. **Update CameraRig.tsx** -- add interactive camera behaviors:

   **Mouse parallax (browse state only):**
   - Add `useFrame` callback that reads `pointer` from R3F state
   - CRITICAL: Gate behind `studioStore.cameraState === "browse"` to avoid GSAP race condition (Pitfall 5 from RESEARCH)
   - Target camera.position.x = browsePos.x + pointer.x * 0.3
   - Target camera.position.y = browsePos.y + pointer.y * 0.15
   - Lerp with factor 0.05 for smooth damping
   - Always call camera.lookAt(roomCenter) where roomCenter = (0, 0.5, 0)

   **Click-to-focus zoom:**
   - Export a function or use studioStore subscription to react to focusIssue calls
   - When cameraState transitions to "focused":
     - Store current browse camera position for return
     - Calculate focus position: focusedBook.position + Vector3(0, 0.3, 1.2) (1.2m in front, slightly above)
     - GSAP tween camera.position to focus position (0.6s, power2.inOut)
     - GSAP tween lookAt target to book position (0.6s)
     - Use studioStore.subscribe or useEffect on focusedIssueId
   - Need a ref map: Map<issueId, Vector3> of book world positions (populated by MagazineRack)

   **Unfocus (back to browse):**
   - When cameraState transitions to "browse" from "focused":
     - GSAP tween camera back to stored browse position (0.5s, power2.inOut)

   **Exit camera:**
   - When cameraState transitions to "exit":
     - Reverse entry path: camera retreats to (0, 1.5, -8) over 1.5s
     - After complete: trigger router.back() or navigate away

   **Escape key handler:**
   - Add useEffect with keydown listener for "Escape"
   - If cameraState === "focused": call studioStore.unfocus()

   **Click-away handler:**
   - On Canvas/scene `onPointerMissed` prop (R3F): if cameraState === "focused", call unfocus()

2. **Update MagazineBook.tsx** -- add hover glow effect:
   - onPointerOver: GSAP tween scale to 1.08 (0.2s), increase spine emissiveIntensity from 2.0 to 4.0, set document.body.style.cursor = "pointer"
   - onPointerOut: reverse scale to 1.0, emissiveIntensity back to 2.0, cursor = "auto"
   - Use refs for the group and spine text material to animate
   - onClick: call studioStore.focusIssue(issue.id)

3. **Update MagazineRack.tsx** -- register book positions:
   - After layout calculation, store each book's world position in a ref Map
   - Pass position data to CameraRig (via studioStore or a shared ref)
   - Add `bookPositions` to studioStore or use a React context
   - Connect onClick/onPointerOver/onPointerOut handlers to each MagazineBook
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app/packages/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Mouse parallax works in browse state, clicking a book zooms camera and opens cover, Escape/click-away returns to browse, hover scales book and increases glow</done>
</task>

<task type="auto">
  <name>Task 2: StudioEffects (Bloom + Vignette) + PerformanceMonitor</name>
  <files>
    packages/web/lib/components/collection/studio/StudioEffects.tsx
    packages/web/lib/components/collection/studio/StudioScene.tsx
  </files>
  <action>
1. Create `StudioEffects.tsx`:
   - Import EffectComposer, Bloom, Vignette from `@react-three/postprocessing`
   - Read qualityLevel from studioStore
   - If qualityLevel === "low": return null (no postprocessing)
   - Bloom: luminanceThreshold 0.6, luminanceSmoothing 0.025, intensity 1.5
   - Vignette: darkness 0.5, offset 0.3 (only on "high" quality)
   - NOTE: Do NOT add chromatic aberration for now -- it's GPU-heavy and spec says optional

2. Update `StudioScene.tsx`:
   - Add `<StudioEffects />` inside Canvas (after scene content, before Preload)
   - Add Drei `<PerformanceMonitor>` component:
     - onDecline callback: setQualityLevel("low")
     - onIncline callback: setQualityLevel("high")
     - flipflops={3}, averages={20}
   - Add `onPointerMissed` prop on Canvas: if cameraState === "focused", call unfocus()
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-app/packages/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Bloom makes neon lights glow selectively (luminanceThreshold 0.6), Vignette adds cinematic darkening on high quality, PerformanceMonitor auto-downgrades on low FPS</done>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` -- no type errors
2. `yarn dev` -- /collection: mouse movement causes subtle parallax in browse state
3. Hover a book: scale grows, spine glows brighter, cursor changes
4. Click a book: camera zooms, cover opens, detail panel area ready
5. Press Escape or click empty space: camera returns to browse
6. Neon lights have bloom glow, book surfaces do not
</verification>

<success_criteria>
- Mouse parallax only active in browse state (no GSAP race condition)
- Hover glow: scale 1.08, emissive intensity increase on spine
- Click-to-focus: camera zooms to book, cover flips open
- Unfocus: Escape or click-away returns to browse
- Bloom selective: neon lights glow, book surfaces don't
- PerformanceMonitor auto-downgrades quality on FPS drops
</success_criteria>

<output>
After completion, create `.planning/phases/m7-03-the-decoded-studio/m7-03-03-SUMMARY.md`
</output>
