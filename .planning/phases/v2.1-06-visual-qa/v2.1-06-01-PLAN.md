---
phase: v2.1-06-visual-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/tests/visual-qa.spec.ts
  - packages/web/playwright.config.ts
autonomous: true

must_haves:
  truths:
    - "Playwright test includes all core application pages"
    - "Four breakpoints are tested (mobile, tablet, desktop, desktop-lg)"
    - "Test can be run with a single command"
  artifacts:
    - path: "packages/web/tests/visual-qa.spec.ts"
      provides: "Visual QA test suite with full page coverage"
      contains: "VIEWPORTS"
    - path: "packages/web/playwright.config.ts"
      provides: "Playwright configuration for visual testing"
      contains: "testDir"
  key_links:
    - from: "packages/web/tests/visual-qa.spec.ts"
      to: "packages/web/playwright.config.ts"
      via: "test configuration"
      pattern: "@playwright/test"
---

<objective>
Enhance Playwright visual testing infrastructure for comprehensive page coverage

Purpose: Establish automated visual QA infrastructure that captures all pages at all breakpoints for design validation against decoded.pen
Output: Updated visual-qa.spec.ts with complete page routes and optimized test configuration

**Scope Note:** This phase covers 10 core static pages that represent the primary user journeys. Excluded from this phase:
- Dynamic detail routes (/posts/[id], /images/[id]) - require mock data seeding
- Modal parallel routes (@modal/) - rendered as overlays, not standalone pages
- Debug pages (/debug) - internal tooling
- Lab pages (/lab/*) - experimental features under development
- Example pages (/examples) - developer reference
These can be added in a future phase with proper test data fixtures.
</objective>

<execution_context>
@/Users/kiyeol/.claude-work/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/web/tests/visual-qa.spec.ts
@packages/web/playwright.config.ts
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand page route coverage in visual-qa.spec.ts</name>
  <files>packages/web/tests/visual-qa.spec.ts</files>
  <action>
Update the PAGES array to include all 10 core application pages that should be visually tested:

Current pages (7):
- home (/)
- explore (/explore)
- feed (/feed)
- search (/search?q=dress)
- profile (/profile)
- login (/login)
- request-upload (/request/upload)

Add missing pages:
- images (/images) - image discovery grid
- request (/request) - request landing page
- request-detect (/request/detect) - AI detection results (needs mock data handling)

Update page configuration with descriptive comments for each route.

Keep existing VIEWPORTS unchanged:
- mobile: 375x812
- tablet: 768x1024
- desktop: 1280x800
- desktop-lg: 1440x900

Add waitForSelector logic for pages that have dynamic content to ensure content is loaded before screenshot.

Example structure for PAGES array:
```typescript
const PAGES = [
  { name: "home", path: "/", waitFor: ".hero-section" },
  { name: "explore", path: "/explore", waitFor: "[data-testid='explore-grid']" },
  // ... etc
];
```

For pages without specific selectors, use waitForLoadState("networkidle") with 500ms buffer (existing pattern).
  </action>
  <verify>
Run `yarn playwright test tests/visual-qa.spec.ts --list` from packages/web to list all tests.
Expected: 40 tests (10 pages x 4 viewports)
  </verify>
  <done>
PAGES array includes all 10 core application routes with appropriate wait conditions
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright config for visual QA optimization</name>
  <files>packages/web/playwright.config.ts</files>
  <action>
Update playwright.config.ts to optimize for visual screenshot testing:

1. Add screenshot-specific settings:
```typescript
use: {
  baseURL: "http://localhost:3000",
  trace: "on-first-retry",
  // Visual QA optimizations
  screenshot: "only-on-failure",
  video: "off", // Disable video for screenshot-only tests
}
```

2. Increase timeout for visual tests (pages may have animations):
```typescript
timeout: 30 * 1000, // 30 seconds per test
expect: {
  timeout: 10 * 1000, // 10 seconds for assertions
}
```

3. Keep existing webServer config (reuses dev server):
```typescript
webServer: {
  command: "yarn dev",
  url: "http://localhost:3000",
  reuseExistingServer: true,
  timeout: 120000,
}
```

4. Ensure output directory is configured for screenshots:
```typescript
outputDir: "test-results/",
```

Do NOT change the projects array (keep chromium only for screenshot consistency).
  </action>
  <verify>
Run `yarn playwright test tests/visual-qa.spec.ts --project=chromium` from packages/web.
All tests should pass and screenshots should be generated in docs/qa-screenshots/
  </verify>
  <done>
Playwright config optimized for visual testing with appropriate timeouts and settings
  </done>
</task>

</tasks>

<verification>
1. `yarn playwright test tests/visual-qa.spec.ts --list` shows 40 tests (10 pages x 4 viewports)
2. Config has visual-testing-appropriate timeouts
3. Existing tests still pass
</verification>

<success_criteria>
- visual-qa.spec.ts covers all 10 core application pages
- 4 breakpoints tested per page (mobile, tablet, desktop, desktop-lg)
- Playwright config optimized for screenshot testing
- Tests can be run with `yarn playwright test tests/visual-qa.spec.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/v2.1-06-visual-qa/v2.1-06-01-SUMMARY.md`
</output>
