---
phase: m7-01-magazine-frontend-mock
plan: 04
type: execute
wave: 2
depends_on: ["m7-01-01"]
files_modified:
  - packages/web/app/collection/page.tsx
  - packages/web/app/collection/layout.tsx
  - packages/web/lib/components/collection/CollectionClient.tsx
  - packages/web/lib/components/collection/BookshelfView.tsx
  - packages/web/lib/components/collection/ShelfRow.tsx
  - packages/web/lib/components/collection/IssueSpine.tsx
  - packages/web/lib/components/collection/IssuePreviewCard.tsx
  - packages/web/lib/components/collection/EmptyBookshelf.tsx
  - packages/web/lib/components/collection/index.ts
autonomous: true

must_haves:
  truths:
    - "User can navigate to /collection and see a 3D bookshelf with magazine spines"
    - "Tapping/hovering a spine pops it forward with cover preview"
    - "Only one spine can be popped out at a time"
    - "Empty state shows when no issues exist with CTA to generate first issue"
    - "Spines appear with staggered fade-in animation on page load"
    - "Shelf rows animate in on scroll via ScrollTrigger"
  artifacts:
    - path: "packages/web/app/collection/page.tsx"
      provides: "Collection page server component"
    - path: "packages/web/lib/components/collection/BookshelfView.tsx"
      provides: "3D perspective bookshelf container"
      min_lines: 40
    - path: "packages/web/lib/components/collection/IssueSpine.tsx"
      provides: "3D rotated spine with pop-out interaction"
      min_lines: 50
    - path: "packages/web/lib/components/collection/IssuePreviewCard.tsx"
      provides: "Expanded cover with metadata overlay"
    - path: "packages/web/lib/components/collection/EmptyBookshelf.tsx"
      provides: "Empty shelf CTA to /magazine/personal"
  key_links:
    - from: "packages/web/lib/components/collection/CollectionClient.tsx"
      to: "packages/web/lib/stores/magazineStore.ts"
      via: "loadCollection from store"
      pattern: "useMagazineStore|loadCollection"
    - from: "packages/web/lib/components/collection/IssueSpine.tsx"
      to: "gsap"
      via: "GSAP pop-out animation on tap/hover"
      pattern: "gsap\\.to|translateZ"
    - from: "packages/web/lib/components/collection/BookshelfView.tsx"
      to: "gsap/ScrollTrigger"
      via: "ScrollTrigger for shelf row entry animation"
      pattern: "ScrollTrigger"
---

<objective>
Build the 3D Bookshelf collection page (SCR-COL-01) with CSS perspective, GSAP-driven spine interactions, and shelf row scroll animations.

Purpose: The bookshelf is the user's personal magazine library. It uses 3D CSS transforms with GSAP for a distinctive "pull from shelf" interaction that differentiates the collection from a flat grid.

Output: Working /collection route with 3D bookshelf, spine pop-out, preview card, empty state, and scroll-triggered shelf animations.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/m7-01-magazine-frontend-mock/m7-01-CONTEXT.md
@.planning/phases/m7-01-magazine-frontend-mock/m7-01-01-SUMMARY.md
@specs/screens/collection/SCR-COL-01-bookshelf.md
@packages/web/lib/components/magazine/types.ts
@packages/web/lib/stores/magazineStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Bookshelf Components (BookshelfView, ShelfRow, barrel export)</name>
  <files>
    packages/web/lib/components/collection/BookshelfView.tsx
    packages/web/lib/components/collection/ShelfRow.tsx
    packages/web/lib/components/collection/index.ts
  </files>
  <action>
    All components are "use client".

    **1. BookshelfView.tsx:**
    - Props: `{ issues: MagazineIssue[], activeIssueId: string | null, onSelectIssue: (id: string | null) => void }`
    - Container with CSS `perspective: 800px` (mobile) / `1200px` (desktop via media query or Tailwind `md:`)
    - Apply `transform-style: preserve-3d` to enable 3D context
    - Group issues into rows: 3-4 per row on mobile, 5-6 on desktop
    - Render `<ShelfRow>` for each group
    - GSAP ScrollTrigger on each ShelfRow: `{ translateY: 30, opacity: 0 }` -> `{ translateY: 0, opacity: 1, duration: 0.5 }`, `once: true`
    - Register ScrollTrigger plugin
    - GSAP context scoped to container, reverted on unmount
    - Background: subtle dark gradient or matte texture (CSS gradient simulating wood shelf: `bg-gradient-to-b from-[#1a1a1a] to-mag-bg`)

    **2. ShelfRow.tsx:**
    - Props: `{ issues: MagazineIssue[], activeIssueId: string | null, onSelectIssue: (id: string | null) => void }`
    - Flex row with even spacing for spines
    - Bottom border styled as shelf edge: `border-b-4 border-[#2a2a2a]` with subtle shadow (`shadow-[0_4px_8px_rgba(0,0,0,0.5)]`)
    - Minimum height for shelf appearance (~180px mobile, ~220px desktop)
    - forwardRef for GSAP targeting from parent

    **3. index.ts** -- Barrel export:
    ```typescript
    export { BookshelfView } from './BookshelfView';
    export { ShelfRow } from './ShelfRow';
    export { IssueSpine } from './IssueSpine';
    export { IssuePreviewCard } from './IssuePreviewCard';
    export { EmptyBookshelf } from './EmptyBookshelf';
    export { CollectionClient } from './CollectionClient';
    ```
    Note: IssueSpine, IssuePreviewCard, EmptyBookshelf, CollectionClient will be created in Task 2 and Task 3. Create the barrel export with all entries now -- TypeScript will error until Task 2/3 complete, which is expected within the same plan execution.
  </action>
  <verify>
    1. BookshelfView.tsx and ShelfRow.tsx exist
    2. BookshelfView uses GSAP ScrollTrigger
    3. ShelfRow uses forwardRef
    4. index.ts lists all 6 component exports
  </verify>
  <done>
    - BookshelfView renders 3D perspective container with shelf rows
    - ShelfRow renders flex row with shelf-edge styling
    - ScrollTrigger animates shelf rows on scroll entry
    - Barrel export prepared for all collection components
  </done>
</task>

<task type="auto">
  <name>Task 2: Spine Interaction Components (IssueSpine, IssuePreviewCard, EmptyBookshelf)</name>
  <files>
    packages/web/lib/components/collection/IssueSpine.tsx
    packages/web/lib/components/collection/IssuePreviewCard.tsx
    packages/web/lib/components/collection/EmptyBookshelf.tsx
  </files>
  <action>
    All components are "use client".

    **1. IssueSpine.tsx:**
    - Props: `{ issue: MagazineIssue, isActive: boolean, onSelect: (id: string | null) => void }`
    - Default pose: `rotateY(-15deg)`, `translateZ(0)`, width ~60px, height ~160px
    - Background color from `issue.theme_palette.accent`
    - Display: Volume number vertically (writing-mode: vertical-rl), issue date
    - **Tap/hover interaction:**
      - On click (mobile) or mouseenter after 200ms (desktop): call `onSelect(issue.id)`
      - When `isActive`: GSAP animate to `translateZ(60px)`, `rotateY(-5deg)`, duration 0.4s, ease `back.out(1.7)`
      - When deactivated: reverse animation (0.3s)
      - Show `<IssuePreviewCard>` when active
    - Use GSAP `.to()` for forward animation, `.to()` for reverse (not timeline -- direct per-spine)
    - Click outside (handled by parent) or selecting different spine deactivates current

    **2. IssuePreviewCard.tsx:**
    - Props: `{ issue: MagazineIssue, onOpen: () => void, onDelete: () => void }`
    - Absolutely positioned next to popped-out spine
    - Shows: cover image (from issue.cover_image_url), volume number ("Vol.0X"), generation date, theme keywords as small tags
    - Action buttons: "Open" (accent bg), "Delete" (muted/danger)
    - "Open" logs intent for now (no real navigation in mock)
    - "Delete" logs intent (no real API)
    - Styled with `bg-[#111] border border-mag-accent/30 rounded-lg`

    **3. EmptyBookshelf.tsx:**
    - Empty shelf visual (empty row with dashed border or subtle shelf outline)
    - Bookshelf icon (use `BookOpen` from lucide-react)
    - Text: "Your bookshelf is empty" + "Start collecting your personal editions"
    - CTA button: "Generate First Issue" linking to `/magazine/personal`
    - Styled with mag theme colors
  </action>
  <verify>
    1. `npx tsc --noEmit --project packages/web/tsconfig.json` passes
    2. All 3 component files exist
    3. IssueSpine uses GSAP for pop-out animation (grep for `gsap.to`)
    4. EmptyBookshelf links to `/magazine/personal`
  </verify>
  <done>
    - IssueSpine shows 3D rotated spine with GSAP pop-out on tap/hover
    - IssuePreviewCard shows cover and metadata when spine is active
    - EmptyBookshelf provides CTA to generate first issue
    - All GSAP animations cleaned up properly
  </done>
</task>

<task type="auto">
  <name>Task 3: Collection Page + Route</name>
  <files>
    packages/web/app/collection/page.tsx
    packages/web/app/collection/layout.tsx
    packages/web/lib/components/collection/CollectionClient.tsx
  </files>
  <action>
    **1. `packages/web/app/collection/layout.tsx`:**
    - Simple layout with dark theme: `bg-mag-bg min-h-screen text-mag-text`
    - Export metadata: title "My Collection | Decoded"

    **2. `packages/web/lib/components/collection/CollectionClient.tsx`:**
    - "use client" component that orchestrates the collection page
    - On mount: call `useMagazineStore().loadCollection()`
    - Read `collectionIssues`, `isLoading`, `activeIssueId` from store
    - Local state for `activeIssueId` (or use store's `setActiveIssueId`)
    - **Header:** Minimal top bar with back button (`<` arrow), "My Collection" title, issue count badge
    - **Loading state:** Skeleton shelf rows (3 rows with pulsing spine outlines)
    - **Empty state:** Render `<EmptyBookshelf />`
    - **Loaded state:** Render `<BookshelfView issues={collectionIssues} activeIssueId={activeIssueId} onSelectIssue={setActiveIssueId} />`
    - Click outside any spine (on the bookshelf background) clears activeIssueId
    - Entry animation: staggered spine fade-in (handled inside BookshelfView)

    **3. `packages/web/app/collection/page.tsx`:**
    - Server component, simply renders `<CollectionClient />`
    - No data fetching at server level (mock data loaded client-side from store)
  </action>
  <verify>
    1. `npx tsc --noEmit --project packages/web/tsconfig.json` passes
    2. Files exist: page.tsx, layout.tsx, CollectionClient.tsx
    3. CollectionClient uses useMagazineStore and renders BookshelfView or EmptyBookshelf
    4. `yarn dev` -- /collection route loads without errors
  </verify>
  <done>
    - /collection route renders 3D bookshelf from mock data
    - Loading state shows shelf skeleton
    - Empty state shows with CTA when no issues
    - Spine interactions work (pop-out on tap, retract on deselect)
    - Preview card shows for active spine
    - Dark theme applied via layout
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project packages/web/tsconfig.json` -- zero errors
2. `yarn dev` -- /collection loads and shows 3D bookshelf with 5 mock issues
3. Tapping a spine triggers pop-out animation
4. Only one spine popped at a time
5. Empty state renders when store has no issues
6. Shelf rows animate in on scroll
</verification>

<success_criteria>
- 3D bookshelf renders with CSS perspective and GSAP-driven spine interactions
- IssueSpine: rotateY(-15deg) default, pops to translateZ(60px) on select with back.out easing
- IssuePreviewCard shows cover, volume number, date on pop-out
- EmptyBookshelf CTA routes to /magazine/personal
- ScrollTrigger reveals shelf rows on scroll
- Dark theme consistent with magazine pages
</success_criteria>

<output>
After completion, create `.planning/phases/m7-01-magazine-frontend-mock/m7-01-04-SUMMARY.md`
</output>
